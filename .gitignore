upstream/
build/
*.bin
*.elf
*.hex
.DS_Store
node_modules/
__pycache__/
*.pyc
.venv/

# Копії SD-картки пульта — великі, у git їм не місце
sd-backups/

# imgui пише розкладку вікна симулятора в поточний каталог
imgui.ini

# ESP-IDF: друга тека збірки (під C3) і згенерований sdkconfig.
# Джерело правди — sdkconfig.defaults*, вони в git.
firmware/esp32/build-c3/
firmware/esp32/sdkconfig
firmware/esp32/sdkconfig.c3
firmware/esp32/sdkconfig.old
firmware/esp32/sdkconfig.c3.old

# Пінований компілятор для прошивки пульта (tools/setup-toolchain.sh)
# — ~150 МБ, тягнеться з ARM щоразу наново, у git їм не місце
toolchain/
