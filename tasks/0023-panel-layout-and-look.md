# 0023. Вигляд і розкладка панелей: за зауваженнями боса

- **Створено:** 2026-08-02
- **Етап:** 3
- **Статус:** нова
- **Виконавець:** сесія команди

## Мета
<!-- ВЛАСНИК: асистент. Команда цей розділ не редагує. -->

Довести до ладу те, що бос назвав, користуючись пультом із телефона в
задачі 0022. Панелі працюють, але з'їдають місце й виглядають не так, як
має виглядати пульт.

⚠️ **Це не оздоблення.** Найдорожчий ресурс на телефоні — висота екрана, і
зараз її забирають смужка стану вгорі та адресний рядок браузера знизу.
Решта — розкладка й кольори — про те, щоб рука знаходила кнопку, не
придивляючись.

## Що вже є і на що спиратись

- Уся розкладка живе в `webui/panels.js` (наміри, порядок, джойстик) і
  `webui/style.css`; сцена — у `webui/index.html`.
- Панелі будуються **з `HELLO`**, не з коду. Це правило задачі 0022
  лишається чинним і тут.
- `tools/webui_built_check.py` піднімає стенд без заліза й водить справжній
  браузер. Він і є місцем, куди додаються перевірки цієї задачі.
- Повний перелік зауважень боса зі словами, якими вони сказані, —
  `tasks/0022-side-panels.md`, розділ «Рецензія», підрозділ «Відгук боса за
  критеріями 7.1–7.3».

## Критерії приймання
<!-- ВЛАСНИК: асистент. -->

⚠️ **Спільне правило доказу для всього розділу.** Числа знімаються з
**живої сторінки** через `tools/webui_built_check.py` — обчисленими
розмірами й кольорами елементів, а не оком по знімку. Знімок додається як
ілюстрація, доказом він не є.

⚠️ **Кожна перевірка робиться у двох орієнтаціях** — вікно 900×420 і
420×860, — якщо в критерії не сказано інше.

### 1. Висота повертається сторінці

- [ ] 1.1. Смужка стану **згорнута за замовчуванням і в згорнутому стані не
      займає окремого ряду** — ані в альбомній орієнтації, ані в книжковій.
      Доказ: висота смужки в згорнутому стані, знята з живої сторінки, у
      кожній орієнтації.
- [ ] 1.2. В альбомній орієнтації смужка стану **переїжджає вниз**.
- [ ] 1.3. ⚠️ **Те, що вже показало свою вартість, лишається досяжним** —
      пам'ять моста, швидкість каналу, частота кадрів, фокус клавіатури.
      Вимога 6.2 задачі 0022 не скасовується: вона переформульовується з
      «видно завжди» на «видно за одне торкання». Назвати в «Результаті», де
      саме воно тепер.
- [ ] 1.4. Сторінка вміє запускатися **без браузерної обгортки**: доданий
      опис застосунку (`manifest`), після якого її можна покласти на робочий
      стіл телефона. Доказ: обчислений `display: standalone` у виданому
      файлі й запис у `webui/README.md`, як цим користуватись.
- [ ] 1.5. Кнопка «на весь екран» присутня й працює там, де браузер це
      вміє. ⚠️ **На пристроях Apple цього способу немає** — кнопка має
      **зникати**, а не показуватись мертвою. Доказ: перевірка в
      `webui_built_check.py`, що кнопки немає, коли браузер не оголошує
      підтримки.

### 2. Альбомна орієнтація

- [ ] 2.1. Ліва й права панелі **однакової ширини** — за зразком нинішньої
      лівої. Доказ: обчислені ширини збігаються.
- [ ] 2.2. Між `SYS` і наступною кнопкою — **подвійний відступ**. Те саме
      між `MDL` і джойстиком. Доказ: обчислені відступи, звірені зі
      звичайним.
- [ ] 2.3. Кнопки джойстика **завширшки як `MDL`**; нинішні пропорції
      всередині (квадрат і коло) зберігаються.
- [ ] 2.4. ⚠️ **Панель розгортається впритул до зображення екрана**, без
      порожнього проміжку; кнопки тягнуться до екрана, а надлишок простору
      лишається ззовні. Доказ: відстань між краєм панелі й краєм зображення
      дорівнює нулю.
- [ ] 2.5. ⚠️ **Зображення екрана стоїть по центру, доки місця вистачає.**
      Коли не вистачає — панель тісни́ть зображення, і воно може стояти не
      по центру, якщо друга панель згорнута. Це нинішня поведінка, і вона
      правильна: доказом є **обидва** випадки, знятих на різних розмірах
      вікна.
- [ ] 2.6. ⚠️ **Окремої кнопки розгортання під нижньою панеллю немає** —
      вона з'їдає висоту. Замість неї в кнопок розгортання бічних панелей
      **відібрано чверть висоти**, і там стоять кнопки розгортання нижньої.
      Доказ: обчислені висоти обох частин.

### 3. Книжкова орієнтація

- [ ] 3.1. Розгортання — за тим самим правилом, що в альбомній (пункти 2.4
      і 2.5): впритул до зображення, зі зсувом за потреби; над зображенням
      лишається вільне місце.
- [ ] 3.2. Усі кнопки — **в одну колонку**, як у бічних панелях, а не
      сіткою.
- [ ] 3.3. Усі кнопки **однакової ширини**, як у бічних панелях.
- [ ] 3.4. Відступи від `SYS` і від `MDL` до сусідніх — **подвійні**.
- [ ] 3.5. Замість смужки стану — **третя панель знизу посередині**. Вона
      займає весь вільний простір між бічними, а бічні мають ширину рівно
      таку, щоб умістити кнопки й відступи, і не більшу. Доказ: обчислені
      ширини трьох панелей у сумі дорівнюють ширині вікна.

### 4. Кольори

- [ ] 4.1. Фон панелей — **цілком чорний**. Доказ: обчислений колір тла.
- [ ] 4.2. Написи й значки на кнопках — **насичений помаранчевий, ніби
      неон**. Виняток — `SYS` і `MDL`.
- [ ] 4.3. `SYS` і `MDL` — **самі помаранчеві, помірної яскравості, текст на
      них чорний**. Доказ: обчислені кольори тла й тексту обох груп.
- [ ] 4.4. ⚠️ **Різниця між групами лишається помітною** й після зміни
      кольорів: `SYS` і `MDL` — широкі світлі, решта — вузькі темні. Це
      вимога 2.3 задачі 0022, і вона не скасовується.

### 5. Нічого не зламано

- [ ] 5.1. Усі наявні перевірки зелені: `panels_test.js`, `app_test.js`,
      `wait_test.js`, `proto_test.js`, `tools/proto_test.py`,
      `tools/wait_crosscheck.py`.
- [ ] 5.2. `tools/webui_built_check.py` зелений разом із новими перевірками
      цієї задачі; число перевірок назване в «Результаті».
- [ ] 5.3. ⚠️ **Панелі так само будуються з `HELLO`.** Перевірки
      `panels_test.js` про **чужий** пульт (інші мітки, справжні стрілки,
      без енкодера) лишаються зеленими без правок під новий вигляд.
- [ ] 5.4. Сторінка не поважчала настільки, щоб вільна пам'ять моста впала
      нижче межі тривоги. Доказ: `min_page` при холодному завантаженні, три
      прогони з розкидом, у тих самих умовах, що дали 62 512 Б у задачі
      0022.
- [ ] 5.5. `code-reviewer` дав вердикт «готово до коміту».

### 6. Головна перевірка — бос користується

- [ ] 6.1. Бос проходить той самий сценарій із телефона і каже, чи стало
      зручніше. Відгук його словами.
- [ ] 6.2. Названо, що лишилось незручним. Не «тепер добре» — конкретні
      місця.

⚠️ **Додано 2026-08-02, після початку роботи сесії. Критеріїв вище не
змінював.**

- [ ] 6.3. **Не блокує приймання.** Раз бос усе одно буде біля стенда:
      затиснути джойстик у довгому списку меню й сказати приблизно, скільки
      рядків проскочило за секунду. Це закриває критерій 3.2 задачі 0022
      (клацань за секунду знаємо — 14, пунктів меню не знаємо). Не вийшло
      чи забулось — задача приймається без цього, і питання йде в кінець
      черги.

### 7. Дописано 2026-08-03 за відгуком боса

⚠️ **Додано після проходу на стенді. Критеріїв 1–6 не змінював.** Це
дрібні правки вигляду, і вони лишаються в цій задачі, бо це її предмет.

- [ ] 7.1. **Синя рамка навколо зображення екрана прибрана.** ⚠️ Це ознака
      фокуса (`style.css:121`), тому вимога 5.4 задачі 0022 «видно, чи є
      фокус» **не скасовується**: її бере на себе смужка «клавіатура ✓».
      Доказ: рамки немає при фокусі, а смужка міняє стан — обидва з живої
      сторінки.
- [ ] 7.2. **У книжковій орієнтації середня панель має власну кнопку
      розгортання.** ⚠️ Це **виправлення критерію 2.6**, який забороняв
      окрему кнопку: заборона стосується **альбомної** орієнтації, де
      висоти бракує. У книжковій місця достатньо, і бос просить окрему.
- [ ] 7.3. **Бічні кнопки розгортання повертаються до нормального
      розміру** — чверть висоти в них більше не забирають. Доказ:
      обчислена висота, порівняна з тією, що дав критерій 2.6.
- [ ] 7.4. **Кнопки на бічних панелях — мінімальний відступ** від сусідніх
      елементів і від країв вікна. Доказ: обчислені відступи.

### 8. Дописано 2026-08-03 за другим колом відгуку

⚠️ **Сесія, яка робила розділи 1–7, закрита. Ці критерії адресовані
наступній.**

- [ ] 8.1. ⛔ **Виправити критерій 3.1: у книжковій орієнтації зображення
      екрана стоїть по центру вікна.** Посунути його має право **лише**
      панель, якій бракує місця, і лише за умови, що з іншого боку місце є.
      Згортання **всіх** панелей повертає зображення в центр. Доказ:
      обчислені відступи згори й знизу при всіх згорнутих панелях — рівні
      між собою.
- [ ] 8.2. **Три кнопки розгортання стоять в один ряд і мають однаковий
      вигляд.** Ширина кожної дорівнює ширині її панелі — **і тоді, коли
      панель згорнута теж**. Доказ: обчислені ширини кнопки й панелі
      збігаються в обох станах.
- [ ] 8.3. **Ширина кнопки розгортання не залежить від того, згорнута
      панель чи ні.** Доказ: обчислена ширина в обох станах однакова.
- [ ] 8.4. **Між кнопками розгортання — мінімальний відступ**, в обох
      орієнтаціях, щоб межа між ними була видима. Доказ: обчислений
      відступ більший за нуль.
- [ ] 8.5. ⚠️ **Розділи 1–7 лишаються виконаними.** Прогін приладу
      перевірки зелений цілком, а не лише в новій частині.

### 9. Дописано 2026-08-03 за третім колом відгуку

- [ ] 9.1. ⛔ **Повернути прив'язку панелей до нижнього краю зображення
      екрана.** Панелі розгортаються **від зображення**, а не від нижнього
      краю вікна. Наслідок, за яким це перевіряється: **усі три кнопки
      розгортання стоять на одній висоті** незалежно від того, скільки
      вмісту в панелях. Доказ: обчислені верхні координати трьох кнопок
      збігаються, при різному наповненні панелей.
- [ ] 9.2. **Висота кнопок дорівнює ширині кнопок бічних панелей в
      альбомній орієнтації** — одне число на обидві орієнтації. Доказ:
      обчислена висота кнопки й обчислена ширина кнопки бічної панелі
      збігаються.
- [ ] 9.3. ⚠️ **Нічого, крім названого в 9.1 і 9.2, не змінюється.**
      Правило `CLAUDE.md`, розділ «Виправлення не переписує погоджене».
      Якщо якась із погоджених домовленостей **прямо суперечить** цим двом
      пунктам — назвати суперечність у «Відхиленнях», а не розв'язувати
      мовчки.
- [ ] 9.4. ⛔ **Прогін приладу проти живого моста** (`--url`), а не лише
      проти стенда без заліза. Минуле коло знайшло дві вади приладу, які
      без моста не проявлялись. Потрібна ввімкнена мережа на ПК — просити
      боса, а не вмикати самому.

## Обмеження
<!-- ВЛАСНИК: асистент. -->

- ⚠️ **Прошивка пульта не чіпається.** Жодного рядка в
  `firmware/edgetx-patch/`.
- ⚠️ **Нуль рядків специфіки TX16S у клієнті.** Кнопки, їх кількість і
  підписи — з `HELLO`. Розкладка описує **бажане місце для ролі**, а не
  буквальний напис; правило задачі 0022 лишається чинним.
- Дозволені файли поза `remote_ui/`: `webui/**`, `tools/**`,
  `firmware/esp32/**` (лише віддача нових файлів і звірене число шляхів у
  `ws_bridge.c`), `tasks/0023-*.md` (тільки «Результат»),
  `state/PROGRESS.md`, `state/handoffs/`, `state/DECISIONS.md` (тільки
  додаванням), `docs/img/**`.
- ⚠️ **`docs/` (крім `img/`) не чіпати.** Потрібна правка там — сказати
  босу, зробить асистент.
- ⚠️ **Введення тексту з фізичної клавіатури в цю задачу не входить.** Бос
  його просив, але воно може зачепити прошивку пульта, а це окрема розмова
  й окрема задача.
- ⚠️ **Множник з'єднань (`max_open_sockets`) не чіпати.** Він відкладений
  свідомо й потребує заміру пропускної здатності до і після — теж окрема
  задача.
- ⚠️ Кожна правка `webui/` коштує прошивання моста: клієнт живе в його
  двійковому файлі.

---

## Результат
<!-- ВЛАСНИК: команда. Асистент цей розділ не редагує. -->

**Зроблено:** *(розділи 1–6; дописане розділом 7 — наприкінці)*

Сцена клієнта перебудована в **одну сітку** з трьох учасників — ліва панель,
зображення, права панель — плюс смужка стану як четвертий елемент тієї самої
сітки. Розмітка одна на обидві орієнтації, різницю робить CSS.

- **Смужка стану згорнута за замовчуванням** і в згорнутому стані не займає
  нічого. Розгортається ручкою `≡`, яка є **з обох боків** — під кожним
  великим пальцем. В альбомній вона лягає внизу на всю ширину, у книжковій —
  третьою панеллю між бічними.
- **Окремої кнопки розгортання смужки немає.** У ручок згортання бічних
  панелей відібрано чверть, і там стоять ручки смужки — рівно як просив бос.
- **Панелі стоять упритул до зображення**, а зображення — по центру вікна,
  доки місця вистачає. Коли не вистачає, панель його тіснить.
- **Кольори:** тло панелей цілком чорне, написи й значки — неоновий
  помаранчевий, `SYS` і `MDL` навпаки: помаранчеві з чорним написом.
- **Запуск без браузерної обгортки:** опис застосунку (`display: standalone`),
  значок, мітки для iPhone і кнопка «Весь екран» — остання лише там, де
  браузер повноекранний режим справді вміє.
- **Сторож пам'яті моста з 0021 не сховався разом зі смужкою:** при падінні
  нижче межі тривоги смужка розгортається сама, рівно один раз на подію.

**Файли:**

| Файл | Що з ним |
|---|---|
| `webui/index.html` | сцена в одну сітку, ручки поділені, смужка з `#bar-row` і `#info` всередині |
| `webui/style.css` | уся розкладка обох орієнтацій, кольори, `min-width: max-content` у `.pad` |
| `webui/app.js` | `fitCanvas` рахує розкладку; смужка, кнопка «Весь екран», самостійне розгортання при низькій пам'яті |
| `webui/manifest.webmanifest`, `webui/icon.png` | **нові**: опис застосунку й значок 192×192 (512 Б) |
| `webui/minify.mjs`, `firmware/esp32/main/CMakeLists.txt` | два нові файли в конвеєрі збірки |
| `firmware/esp32/main/ws_bridge.c` | віддача двох нових шляхів; стеля `max_uri_handlers` тепер рахується з самого переліку |
| `tools/webui_built_check.py` | **74 нові перевірки** обчисленими розмірами й кольорами; дві вади самого приладу, знайдені живим мостом |
| `tools/webui_serve.py` | двійник моста віддає поле `heap`; типи для `.webmanifest` і `.png` |
| `tools/webui_drive.py` | розгортає смужку перед клацанням по кнопці «Стан» |
| `tools/make_icon.py` | **новий**: генератор значка, щоб той не був даниною без джерела |
| `webui/README.md` | як покласти сторінку на робочий стіл; чому смужка згорнута |
| `docs/img/client/0023-0*.png` | знімки обох орієнтацій — **ілюстрація, не доказ** |

**Коміти:** `19d59ba` (розділи 1–6), `e547e49` (розділ 7), `e74b033` (критерій
5.4 і прилад проти живого моста), `43bfccb` (розділ 8).

**Заміри:**

Усі числа зняті з **живої зібраної сторінки** обчисленими розмірами й
кольорами через `tools/webui_built_check.py`, у двох вікнах — 900×420 і
420×860.

| Що | Число |
|---|---|
| висота згорнутої смужки | **0 px** в альбомній; у книжковій лишається сама ручка (44 px), ряду не додає |
| скільки висоти вона забирає розгорнута (альбомна) | 76 px; зображення 385 → 344 |
| ширина лівої й правої панелі | **110 = 110** |
| відступ після `SYS` і після `MDL` | **8 px** проти звичайних 4 (мінімальні, критерій 7.4) |
| ширина кнопки джойстика / `MDL` | **76 = 76**, напрямки квадратні |
| проміжок панель ↔ зображення | **0 px** з обох боків, в обох орієнтаціях |
| ручка панелі / ручка смужки (альбомна) | **258 / 86** — рівно втричі; у книжковій поділу немає, обидві по 44 |
| книжкова: ліва + смужка + права | **84 + 252 + 84 = 420** при вікні 420 |
| тло панелей і смужки | `rgb(0, 0, 0)` |
| написи на клавішах і джойстику | `rgb(255, 125, 26)` |
| `SYS` і `MDL` | тло `rgb(194, 98, 15)`, напис `rgb(0, 0, 0)` |
| перевірок у приладі | **90 на стенді, 42 проти живого моста**, невдалих 0 (було 16) |
| сторінка на дроті | 18 645 → **20 859 Б** gzip (+2 214, +11.9 %) |

**⚠️ Заміряна вада, знайдена рецензією і виправлена.** Звуження вікна в
альбомній розчавлювало панелі: сітка стискала колонку панелі під **ще не
виправлену** ширину зображення, `fitCanvas` читав стиснуте число, вважав, що
місця вдосталь, і лишав зображення завеликим. Заміряно на 900 → 760:

| ширина вікна | панелі | клавіша | горизонтальна прокрутка |
|---|---|---|---|
| 900 | 110 / 110 | 84 | немає |
| 760 | **40 / 40** | **14** | немає |
| 640 | **0 / 0** | **8** | **є** |

Лікування — `min-width: max-content` у `.pad`: ширина панелі більше не
залежить від того, що робить сусідня колонка, тобто `fitCanvas` міряє те саме
число до й після власної правки. Після нього ті самі ширини вікна дають
панель 110 і клавішу 84 скрізь, прокрутки немає.

⚠️ **Перевірка 2.5 йшла просто крізь цю ваду й казала «зелено».** Вона
звужувала вікно, але після інших перевірок — смужка вже була розгорнута,
зображення обмежувала висота, і стискання по ширині не наставало. Тепер
звуження перевіряється **окремим свіжим вікном у типовому стані**
(`check_resize_sweep`), і це п'ятий за проєкт випадок шаблону «перевірка, яка
не може провалитись».

**⚠️ Друга заміряна вада, теж від рецензії: сторож пам'яті замовкав від
блимання зв'язку.** Смужка розгортається сама при низькій купі моста —
рівно один раз на подію, інакше опитування раз на секунду робило б її
незакривною. Перше виправлення знімало засувку при **будь-якій** пропущеній
відповіді моста, і цього досить, щоб гарантія зникла: низька купа береться
від тиску трафіку, а **той самий тиск зриває опитування**. Заміряно: після
двосекундного обриву закрита смужка відчинялась знову. Тепер засувка
знімається лише після **п'яти** мовчазних періодів поспіль (≈5 с) — це вже
не блимання, а перезапуск моста. Обидві половини правила перевіряються
окремо.

⚠️ **Спільне в обох вадах — не код, а коментар:** обидва рази поруч стояло
пояснення, яке обіцяло гарантію, якої в коді не було. Друга встигла потрапити
ще й у `state/DECISIONS.md`, звідки записи не переписують; там воно виправлене
разом із назвою хибної проміжної правки.

**Мутаційний прогін** (рецензія навмисне ламала код і дивилась, чи падають
перевірки; десять мутацій за три кола): повернення `min-width: 0` валить
`2.4 звуження вікна`; зняття розгортання смужки валить обидві перевірки
сторожа; зняття лише засувки — рівно перевірку «раз на подію»; поріг
`HEAP_ALARM_REARM_POLLS` 5 → 1 — рівно «блимання не подає тривогу наново»;
прибрати скидання лічильника — рівно «лічильник скидається живим мостом»;
`display: none` → `visibility: hidden` валить три перевірки 1.1.

### ✅ Критерій 5.4 закритий заміром на стенді, 2026-08-03

Міст прошито (`idf.py -p /dev/ttyUSB0 -b 115200 flash`; ⚠️ на типовій
швидкості падає — **третій випадок поспіль**) і звірено байтами: усі вісім
файлів у флеші збігаються зі зібраними.

**Три холодні завантаження справжнім браузером, міст щойно перезавантажений,
жодного вводу** — умови ті самі, що дали 62 512 Б у 0022:

| прогін | `min_page` | завантаження | сторож |
|---|---:|---:|---|
| 1 | 78 464 | 377 мс | мовчить |
| 2 | **66 324** | 298 мс | мовчить |
| 3 | 66 324 | 286 мс | мовчить |

⚠️ **Числа монотонні, і це властивість приладу, а не збіг:** мінімуми за
етапами живуть від старту моста, `POST /api/heap/reset` їх **не чіпає**
(`heap_watch.c:157`) — скидається лише вікно й перевірка на витік. Тобто
рядок — це «найгірше, що бачив міст досі», і 66 324 Б — найглибша яма за три
завантаження.

**Критерій виконано з запасом удвічі:** 66 324 при межі тривоги 32 768, і
навіть **краще за 0022** (62 512), хоча сторінка поважчала на 10 %. Причина
не в сумі байтів: у купі відкладається найбільший **окремий** файл на
з'єднання, а він не виріс.

**Заразом заміряні сусідні етапи**, яких критерій не просив:

| дослід | `min_stream` | `min_page` | сторож |
|---|---:|---:|---|
| один клієнт, 25 с енкодера й протягів | 80 768 | 66 324 | мовчить |
| **два** клієнти, що витісняють одне одного (27 сеансів, 26 втрачених) | 78 956 | 69 240 | мовчить |
| завантаження сторінки **поверх** живого потоку, чотири рази | 82 376 | 71 652 | мовчить |

### ⚠️ Одне просідання до 22 КБ лишилось непоясненим — і це записано, а не заметено

У **першому** сеансі після прошивання (міст пропрацював 1128 с разом із
вкладкою боса й моїми прогонами) `/api/stats` показав `min_stream` = **22 160
Б**, тобто нижче межі тривоги, і сторож спрацював (`warned: true`).

**Три навмисні спроби відтворити не дали нічого** — три рядки таблиці вище,
найгірше 78 956. Тобто найпростіше пояснення («винні два клієнти») **заміром
спростоване**, а справжня причина не встановлена. Умови того сеансу
відрізнялись ще й тим, що бос користувався клієнтом руками, а прилад
перевірки встиг відкрити з десяток вікон.

⚠️ **Умова, за якої це відкривається наново:** `min_stream` або `min_window`
знову опустився нижче 32 768 при **одному** клієнті. Тоді дивитись треба в
розклад «хто з'їв» із `/api/stats` у ту саму мить, а не в загальні числа.

### ⚠️ Прогін проти живого моста знайшов дві вади в самому приладі

Обидві такі, що на стенді без заліза проявитись не могли — п'ятий і шостий за
проєкт випадок шаблону «перевірка, яка не може провалитись там, де треба».

1. **Опис застосунку не розбирався.** Міст ставить `Content-Encoding: gzip`
   **безумовно**, а `urllib` сам не розпаковує: перевірка падала з
   `'utf-8' codec can't decode byte 0x8b`. На стенді файл віддається
   нестисненим, тому там вона була зелена. Той самий обхід уже стояв в
   `assert_serving_built` — у `check_manifest` його бракувало.
2. **Прилад сам собі заважав.** Він відкривав свіжі вікна, не відпустивши
   головного, а міст обслуговує **рівно одного** клієнта: новий WebSocket
   витісняє попередній, той перепідключається й витісняє новий — і жоден не
   доживає до `HELLO`. Двійник пульта відповідає миттєво й це ховав. Тепер на
   час перевірок у свіжих вікнах головна сторінка стоїть на `about:blank`.

Після виправлень: **72 перевірки проти живого моста, нуль невдалих** (шість
перевірок вводу в режимі `--url` не робляться — двійника там немає), і
панелі там побудовані зі справжнього `HELLO` пульта:
`['SYS','RTN','PAGE>','PAGE<','TELE','MDL']`.

⚠️ **Після розділу 7 повний прогін `--url` більше не доходить до кінця**, і
це чесно варте запису. Він спотикається не на клієнті, а на власному
переході між вікнами: свіжий клієнт, відкритий одразу після закритого, не
дістає `HELLO` від пульта за 25 секунд (та сама пастка, що й після
перезавантаження моста). Тому вигляд і розкладку проти заліза знято **одним
вікном** — 42 перевірки, нуль невдалих, включно з усіма 7.1–7.4. Не знято
проти заліза: `check_default_bar`, `check_resize_sweep`,
`check_heap_watchdog`, `check_no_fullscreen` — вони проходили живий міст
минулого кола (72/72), а зачепив їх розділ 7 лише в частині смужки, яку
однобічний прогін і перевіряє.

⚠️ **Сама пастка — предмет не цієї задачі.** Вона тієї ж природи, що й
гойдалка двох клієнтів, яку бос виніс окремо: обидві про те, хто й коли
володіє каналом до пульта.

⚠️ **Пастка стенда:** одразу після перезавантаження моста перший клієнт
`HELLO` не отримує, наступний — отримує. Спіймано двічі з двох. Не досліджено;
на замір не впливає, бо досить перепідключитись.

### ✅ Розділ 7 — дописане босом після проходу на стенді

| критерій | що зроблено | доказ із живої сторінки |
|---|---|---|
| 7.1 | синьої рамки фокуса навколо зображення немає | тінь і обведення `none` при фокусі на зображенні |
| 7.1 | вимога 5.4 задачі 0022 не зникла, а переїхала | плашка «клавіатура ✓» при фокусі, «клавіатура —» без нього |
| 7.2 | у книжковій смужка має **власну** ручку, видиму й у згорнутому стані | ручка є при `barCollapsed`, клац по ній розгортає |
| 7.2 | в альбомній власної ручки немає — 2.6 чинний | при **розгорнутій** смужці власної немає, бічні є |
| 7.3 | у книжковій ручка бічної панелі ціла | ручка 84×44 = брусок 84×44 |
| 7.3 | обидві ручки не дрібніші за ціль для пальця | 44 і 44 при межі 44 |
| 7.4 | мінімальний відступ від країв і між кнопками | 4 / 4 px від тіла панелі, 4 px між кнопками, 8 після широкої |

⚠️ **Три перевірки розділу 7 спершу не могли провалитись, і знайшла це
рецензія, а не я.** Кожну виправлено й **перевірено мутацією** — тобто
зламаним кодом, на якому вона мусить упасти:

| мутація | що падає тепер |
|---|---|
| згорнута смужка в книжковій показує весь вміст | `1.1 у згорнутій смужці видно саму ручку` |
| брусок ручки 8 px замість 44 | `7.3 обидві ручки не дрібніші за ціль для пальця` |
| власна ручка з'явилась і в альбомній | `7.2 в альбомній власної ручки немає` |

⚠️ **Четверта залежність від порядку кроків** у тому самому приладі: `7.2` в
альбомній справджувалась лише тому, що попередня перевірка встигала відчинити
смужку, а в згорнутій власну ручку ховає `display: none`. Тепер смужка
відчиняється явно. Одну таку залежність я спіймав сам (`7.1` знімала фокус
кнопкою, схованою в згорнутій смужці, і падала проти живого моста), другу —
рецензія.

⚠️ **Ознаки фокуса в типовому стані на екрані більше немає взагалі.** 7.1
прибрав єдину, яка була видна завжди, а нова живе в смужці, згорнутій за
замовчуванням. Задача це дозволяє (7.1 разом із переформульованою 1.3), але
сказати це вголос треба: тепер це «видно за одне торкання», а не «видно».

### ✅ Критерій 6.3 закритий заміром — і закриває 3.2 задачі 0022

Бос віддав стенд, тож порахував сам, через клієнта: екран пульта видно в
браузері, тому «скільки рядків проскочило» — це знімок до й знімок після.
Список — сторінка `Hardware` у налаштуваннях пульта, найдовший із безпечних:
курсор по ньому лише ходить, значень не змінює (жодного `ENTER` не
натискалось, і після виходу всі перемикачі ті самі).

| дослід | клацань | пунктів меню |
|---|---:|---:|
| п'ять окремих натискань джойстика, 500 мс | 5 | **5** |
| колесо миші, проміжки 500 / 200 / 120 / 70 мс | 5 | **5** у кожному |
| **утримання 1 с** (наш розгін) | **6** | **6** |

**Відповідь на 3.2: пунктів меню за секунду рівно стільки, скільки клацань** —
приблизно **6 за першу секунду** утримання й **≈14 за секунду** усталено
(проміжок нашого розгону впирається в стелю 70 мс). Прискорення EdgeTX
додає крок **значенню**, яке редагують, а курсору в списку — ні: одне
клацання рухає рівно на один пункт.

⚠️ **Пастка заміру, у яку я встиг потрапити.** Перший дослід був утриманням
**2 с** — 21 клацання, а курсор зсунувся рівно на **один** пункт, і це
виглядало як важка вада клієнта. Пояснення виявилось інакшим: **список
закільцьований**, і 21 клацання обійшло його по колу плюс один. Тобто
«положення до / положення після» в кільцевому списку **не є заміром**, поки
кількість кроків менша за довжину списку. Перевірка коротшим утриманням (1 с,
6 клацань → 6 пунктів) розрізнила два пояснення за одну хвилину.

⚠️ **Найтісніший запас часу в усьому приладі — саме в перевірках сторожа.**
Період опитування 1 с; блимання на 2 с має запас три періоди, довге мовчання
на 7 с — два періоди до порогу 5. Три прогони поспіль стабільні, але якщо
колись замиготить, шукати треба тут, а не в клієнті.

**Відхилення від задачі і чому:**

1. ⚠️ **Критерій 4.4, слово «широкі», виконано не буквально.** Критерії 2.1
   («панелі однакової ширини»), 2.3 («кнопки джойстика завширшки як `MDL`») і
   3.3 («усі кнопки однакової ширини») разом означають, що всі кнопки в
   панелі мають однакову ширину. Тобто `SYS` і `MDL` **не можуть** бути
   ширшими за решту. Тому різницю між породами тепер несуть колір і поверхня:
   вузькі — темні ребристі з неоновим написом, широкі — суцільні помаранчеві з
   чорним. Так само було й у 0022 (там усі кнопки в панелі теж розтягувались
   до її ширини, а критерій 2.3 прийняли за класом `key-wide`), тож нового
   тут лише колір. **Якщо бос мав на увазі саме різну ширину — це переробка на
   одну змінну в CSS, але тоді треба скасувати 3.3.**
2. **Значок `icon.png` — не проханий, але без нього критерій 1.4 не працює.**
   Chrome на Android без значка робить не застосунок, а закладку, і вона
   відкривається в тій самій обгортці з адресним рядком. Ціна — 512 Б.
3. **Кнопка названа словом «Весь екран», а не значком.** Типовий значок
   повноекранного режиму (U+26F6) є не в кожному шрифті, а свого ми не
   возимо: на стенді він вийшов порожнім прямокутником.
4. **`max_uri_handlers` більше не число, а `sizeof(uris)/sizeof(uris[0])`.**
   Обмеження задачі дозволяє «звірене число шляхів»; звіряти щоразу руками —
   те саме, що не звіряти, а перебір упав би не при збірці, а на старті моста
   в полі.

**Що не вийшло:**

- ✅ **Критерій 5.4 більше не борг** — заміряний на стенді 2026-08-03, числа
  в розділі «Заміри». Лишилось непоясненим одне просідання до 22 КБ у
  першому сеансі; три спроби відтворити не дали нічого, і це записано там
  само разом з умовою, за якої питання відкривається наново.
- ⏳ **Критерії 6.1 і 6.2 — за босом:** пройти той самий сценарій із телефона
  й сказати, чи стало зручніше й що лишилось незручним.
- ✅ **Розділ 7 зроблений** — окремим підрозділом у «Замірах» нижче.
- ✅ **Критерій 6.3 закритий** — бос віддав стенд, і я порахував сам через
  клієнта: 6 пунктів за першу секунду утримання, ≈14 за секунду усталено.
  Числа й пастка кільцевого списку — у розділі «Заміри». Це заразом закриває
  критерій **3.2 задачі 0022**, останній її відкритий.
- **Два питання свідомо не чіпались**, обидва винесені обмеженнями задачі в
  окремі роботи: введення тексту з фізичної клавіатури і множник з'єднань
  `max_open_sockets`.

### ✅ Розділ 8 — доопрацювання за другим колом відгуку, 2026-08-03

**Головне — 8.1, і це не нова вимога, а невиконаний критерій 3.1.**

Причина, чому це пройшло непоміченим, у самому коді: центрування в альбомній
рахував `fitCanvas` числом, а в книжковій його не рахував **ніхто** — там
стояв один рядок CSS `align-items: flex-end`, тобто «притиснути до панелей».
Доки панелі розгорнуті, притиснуте й відцентроване збігаються з точністю до
пікселя (місця й так бракує), тому вада була видима рівно в одному стані —
коли всі панелі згорнуті. Перевірка ж міряла типовий стан.

**Лікування:** у книжковій вирівнювання змінене на `flex-start`, а зсув рахує
`fitCanvas` — тим самим правилом, що в альбомній, лише по вертикалі. Бажане:
центр вікна. Дозволене: не залізти на панелі.

| стан панелей | відступ згори | відступ знизу | центр картинки / центр вікна |
|---|---:|---:|---|
| **усі згорнуті** | **311** | **311** | **430 / 430** |
| розгорнуті | 282 | 340 | 401 / 430, до панелі 0 px |

Тобто друга половина правила боса теж заміряна: посунути зображення має
право лише панель, якій бракує місця, і посуває вона його рівно настільки,
наскільки бракує.

**8.2–8.4 — ряд ручок згортання.** Три ручки в книжковій стоять в один ряд
(верх і висота однакові в обох станах), оформлення в них тепер спільне —
одне правило CSS замість двох наборів чисел:

| що | згорнуті | розгорнуті |
|---|---|---|
| ширини ручок | **80 / 248 / 80** | **80 / 248 / 80** |
| ширини панелей | 84 / 252 / 84 | 84 / 252 / 84 |
| верх ручок | 816 / 816 / 816 | 520 / 520 / 520 |
| висота ручок | 44 / 44 / 44 | 44 / 44 / 44 |
| проміжок між ручками | 4 і 4 px | 4 і 4 px |
| альбомна: ширина бічної ручки | 26 / 26 | 26 / 26 |
| альбомна: проміжок у бруску | 4 і 4 px | 4 і 4 px |

⚠️ **Відхилення, назване вголос: ручка вужча за свою панель на 4 пікселі.**
Критерії 8.2 («ширина ручки = ширина панелі») і 8.4 («між ручками є
відступ») разом **буквально нездійсненні**, поки панелі стоять упритул одна
до одної — а цього вимагає критерій 3.5, і сума 84 + 252 + 84 = 420 при вікні
420 його доводить. Узятись проміжку більше нема звідки, тож він відібраний у
самих ручок: по два пікселі з боку, разом рівно `--gap`. Вибрано видиму межу,
бо саме її бос і просив («щоб було видно межу між ними»). Зворотне рішення —
ручки впритул і без межі — коштує один рядок CSS.

⚠️ **Три ручки в один ряд коштували ще однієї зміни, не названої в
критеріях.** Коробка смужки стояла `align-self: end`, тобто на низу свого
ряду, і її ручка опинялась нижча за бічні на всю висоту панелі. Тепер `start`.
Згорнута смужка при цьому й далі береться по вмісту, тобто коштує саму ручку
— перевірка 1.1 книжкової лишилась зеленою без правок.

⚠️ **Третя зміна вигляду поза критеріями: у книжковій зникла риска над
смужкою.** Доти `border-top` не малювався лише в згорнутої, тепер його немає
й у розгорнутої: лінія над однією з трьох однакових кнопок читалась як
помилка. Якщо бос захоче її назад — один рядок CSS.

⚠️ **Дві перевірки розділів 2 і 7 довелось поправити** — не послабити, а
дописати в них те, що змінив розділ 8: у бруску ручки з'явився проміжок
(`2.6 обидві частини заповнюють ручку` — тепер сума частин **плюс** проміжок),
і ручка стала на 4 px вужча за брусок (`7.3`). Обидва числа стоять у перевірці
явно, а не сховані в послабленому допуску.

Знімки з живої сторінки — `docs/img/client/0023-05-portrait-collapsed.png`
(усі панелі згорнуті, картинка по центру), `0023-06-portrait-expanded.png`
(панелі тіснять її вгору) і `0023-07-portrait-bar.png` (смужка розгорнута,
ряд ручок цілий). ⚠️ Ілюстрація, не доказ: доказ — числа вище.

**8.5 — прогін зелений цілком:** перевірок **110** (було 90), невдалих 0.
Старі набори теж: `panels_test` 29, `app_test` 38, `wait_test` 32,
`proto_test` 84, `tools/proto_test.py` 91, `wait_crosscheck` 22 сценарії.

Сторінка на дроті: 20 859 → **20 900 Б** gzip (+41 Б, +0.2 %).

### ⚠️ Рецензія коду знайшла чотири блокери, і всі чотири — у приладі, а не в клієнті

Код розкладки (`style.css`, `app.js`) зауважень не мав. Впали перевірки — і
рівно тим шаблоном, який у цьому приладі ловлять уже вшосте: **перевірка, яка
не може провалитись**, і **коментар, що обіцяє гарантію, якої в коді немає**.

1. ⛔ **Друга перевірка 8.1 була переказом першої.** «Рівні відступи згори й
   знизу» і «центр картинки = центр вікна» — це та сама нерівність, лише з
   удвічі слабшим допуском: другої червоної при першій зеленій не існує.
   Гірше, коментар пояснював, що вона ловить випадок «картинка на все вікно»,
   а насправді той випадок відсікала якраз перша (`top > 1`). Тепер замість
   переказу стоять два справді різні твердження: **сцена займає все вікно**
   (без нього відступ у `body` — а на iPhone `safe-area-inset-top` не нуль —
   поглинувся б мовчки) і **згорнута панель зображення більше не тримає**
   (проміжок до панелі 267 px там, де стара вада давала нуль).
2. ⛔ **Стара перевірка 3.1 стверджувала скасовану поведінку** — «панель
   упритул завжди», — і зеленою була випадково, із запасом **29 пікселів**:
   при 420×860 місця бракує рівно настільки. Чужий пульт із на одну клавішу
   коротшою панеллю зробив би її червоною на цілком справному коді. Тепер
   вона стверджує саме правило: **або по центру, або впритул, третього немає**.
3. ⛔ **«Розгорнута панель тіснить зображення вгору» трималась на тих самих
   29 пікселях.** Тепер вікно на час цієї перевірки навмисно нижче (420×620),
   і місця бракує на півтори сотні пікселів, а не на десятки: перевірка міряє
   правило, а не те, скільки клавіш віддав `HELLO` двійника.
4. ⛔ **Коментар про порядок перевірок називав гарантією не той механізм.**
   Він казав «розділ 8 іде останнім», хоча альбомна його частина стоїть
   усередині прогону, а стан тримає `set_pads(page, False)` наприкінці кожної
   частини. Тепер так і написано, а книжкова частина стан теж повертає сама.

### Мутаційний прогін розділу 8: десять мутацій, кожна валить своє

Перевірка, яка не падає на зламаному коді, нічого не доводить. Тому кожна
нова перевірка перевірена **навмисно зламаним кодом** — шість мутацій до
рецензії й чотири після неї, на переписані перевірки:

| що зламано | що впало |
|---|---|
| **стара вада**: картинку притиснуто вниз, зсув не рахується | обидві перевірки 8.1 |
| **хибне лікування**: центр ряду замість центру вікна | 4 перевірки, серед них переписана 3.1 |
| ширина бруска знову за вмістом | 8.3 |
| смужка знову притиснута до низу ряду | 8.2 «в один ряд» (розгорнуті) |
| згорнута панель з'їжджає вниз ряду | 8.2 «в один ряд» (розгорнуті й **одна згорнута**) |
| прибрані бічні поля ручок | 8.2 і 8.4 в усіх станах, плюс 7.3 |
| прибраний проміжок у бруску (альбомна) | 2.6 і обидві 8.4 альбомної |
| інший радіус у ручки смужки | 8.2 «однаковий вигляд» |
| згорнута смужка обростає відступом | 1.1 книжкової плюс дев'ять інших |

⚠️ **Дві мутації показали межі перевірок, а не лише їхню силу.** «Смужка
притиснута до низу ряду» валить перевірку лише в **розгорнутому** стані: коли
всі панелі згорнуті, ряд і так 44 px заввишки, і різниці між верхом і низом
немає. Тобто пара станів тут не для повноти — без неї перевірка була б
безсилою. Так само переписана 3.1 лишається **зеленою** на старій ваді, і це
правильно: з розгорнутими панелями стара поведінка збігалася з правильною.

Дрібніші зауваження тієї ж рецензії, закриті тим самим комітом: у перевірці
1.1 книжкової лишалось **десять пікселів мертвого запасу** від полів, які
розділ 8 прибрав (смужка з `padding: 8px` проходила б зеленою); коментар до
альбомної 8.2 обіцяв доказ рівності, яку дає сам flexbox, а ловить вона
насправді витік бічних полів у другу орієнтацію; і **проміжний стан «одна
панель згорнута, друга ні» не перевірявся жодним числом** — а він
найризикованіший для «трьох ручок в один ряд», бо висоту ряду задає розгорнута
сусідка. Тепер кожне число знімається **тричі**.

**Хто що робив:** (помічники й моделі насправді)

- **Сесія команди (Opus 5)** — уся розкладка, кольори, прошивка моста,
  прилад перевірки, заміри, знімки. Помічників `webui-dev` і `esp32-firmware`
  не залучав: робота йшла суцільним шматком по тих самих чотирьох файлах, і
  передавати їх туди-сюди коштувало б дорожче за виграш.
- **`code-reviewer` (Opus 5)** — **три кола**, і кожне знайшло своє.
  Перше — **блокер**, який усі мої перевірки проходили зеленими: розчавлені
  панелі при звуженні вікна. Друге — **мутаційний прогін** (навмисне
  ламання коду, щоб довести, що перевірки падають) і другий блокер: засувка
  сторожа пам'яті гасла від будь-якого зриву опитування. Третє — перевірка
  виправлень.
- **Бос** — зауваження, з яких зроблена задача (`tasks/0022`, розділ
  «Рецензія»), і критерії 6.1–6.2 попереду.

**Розділ 8, друга сесія команди (Opus 5):** розкладка, прилад, мутаційні
прогони, збірка й прошивання моста. Помічників не залучав із тієї самої
причини — робота йшла по тих самих трьох файлах. **`code-reviewer` (Opus 5)** —
одне коло, **чотири блокери**, усі в приладі перевірки; код клієнта зауважень
не мав. Мутаційних прогонів два: шість мутацій на нові перевірки й чотири на
переписані після рецензії.

---

### ✅ Розділ 9 — повернута прив'язка панелей і одне число на брусок, 2026-08-03

**9.1. Панелі знову прив'язані до нижнього краю зображення.** Скарга боса
відтворилась числами до першого рядка правки: верх ручок згортання стрибав
разом із наповненням панелей, а між картинкою й ними стояла діра.

| стан панелей | верх ручок, було | діра до картинки, було | стало |
|---|---:|---:|---:|
| усі згорнуті | 816 | **267 px** | ручки під картинкою, діра **0** |
| смужка розгорнута | 588 | 39 px | діра **0** |
| усі розгорнуті | 520 | 0 | діра **0** |

Тепер надлишок місця лежить **під** панелями (285 px при згорнутих), а не між
ними й зображенням. **Центрування з критерію 8.1 не постраждало:** при всіх
згорнутих панелях згори 311 px, знизу 311 px — те саме число, що й до правки.

**9.2. Товщина бруска ручки — одне число на обидві орієнтації:** змінна
`--rail` = 26 px. В альбомній це ширина вертикального бруска, у книжковій —
висота горизонтального ряду; доти висота в книжковій задавалась окремо (44 px)
і росла з кожним колом відгуку.

**9.4. Прогін проти живого моста — 114 перевірок, невдалих 0.** Міст прошито
зібраним клієнтом (834 544 Б, хеші звірені esptool).

| | стенд без заліза | живий міст |
|---|---:|---:|
| перевірок | **120** (було 110) | **114** |
| невдалих | 0 | 0 |

Сторінка на дроті 20 900 → **20 974 Б** gz (+74 Б). Старі набори зелені:
29 / 38 / 84 / 32, `proto_test.py` OK, `wait_crosscheck` 22 сценарії.

### ⚠️ Відхилення від задачі і чому

**Критерій 7.3 прямо суперечить 9.2, і суперечність розв'язана на користь
9.2.** Висота ручок у книжковій трималась на міркуванні «44 px — ціль для
пальця, як у `.key`», записаному під критерій 7.3. Бос назвав інше число, і
воно менше. Правило `CLAUDE.md` вимагає назвати такий випадок уголос, а не
розв'язувати мовчки — ось він. Площа ручки при цьому лишається більшою за
клавішу: 80 × 26 проти 76 × 44.

Більше нічого не змінювалось (критерій 9.3). Сітка книжкової лишилась тією
самою, що була погоджена; прив'язку дає окреме число, а не переписана
розкладка.

### ⚠️ Перший підхід був вадою, і спіймав її прилад, а не око

Природним рішенням 9.1 виглядало «обидва ряди `auto`, вільне місце під ними»
(`align-content: start`). Воно **працює на вигляд і ламається в обчисленні**:
у двох `auto` рядах сітка починає стискати саму панель — `.pad-body` має
`min-height: 0` і `overflow: auto`, тобто найменший її розмір нульовий, — і
`fitCanvas` читає стиснуту висоту замість природної. Обчислення стає
самозалежним, розкладка гуляє без упину.

⚠️ **Прогін падав не червоною перевіркою, а тайм-аутом Playwright «element is
not stable»** — діагнозом, за яким причини не видно. Тому додано окрему
перевірку: два заміри через 0.7 с мають збігтися.

Робоче рішення лишає сітку недоторканою: надлишок віддається трьом учасникам
нижнього ряду як `margin-bottom`, який рахує `fitCanvas`. Число береться з
`offsetHeight` панелі, а в нього наш відступ не входить, тож обчислення
сходиться за один прохід.

### ⚠️ Дві мої власні перевірки не могли спрацювати, і обидві спіймала мутація

Шаблон «перевірка, яка мовчки не працює» цього разу зловився **не рецензією, а
мутаційним прогоном**: обидві падали з нулем **у кожній** мутації, включно з
тими, до яких не мали стосунку.

1. **Смужка від довгого підпису не вищає** — плашки обрізаються
   (`text-overflow: ellipsis`).
2. **Зонд ішов лише в ліву панель**, а висоту ряду задає **права**: у ній
   джойстик, 322 px проти 274. Ліва просто заповнювала вільне місце, і жодне
   число не рухалось.

Третю прогалину мутація показала прямо: **«зсув не знімається при поверненні в
альбомну» проходило зеленим через увесь прогін.** Телефон повертають у руках,
і 285 зайвих пікселів під панелями відірвали б смужку стану від низу вікна —
а книжкові перевірки йдуть останніми й цього не бачать. Додано перевірку в
альбомній.

**Мутаційний прогін: сім мутацій, кожна валить рівно свої перевірки** — від
«зсуву немає» (4 червоних) до «спільне число є, але безглузде» (2 червоних).

### ⚠️ Прогін проти живого моста впав п'ять разів поспіль, і жоден раз — через клієнта

Це найдорожча частина роботи, тому причини названі окремо.

1. **Міст обслуговує рівно одного клієнта, а слот звільняється не миттєво.**
   Свіже вікно падало за 25 с очікування `HELLO`, тоді як одиночне отримувало
   його за **0.3 с**. Прилад тепер повторює спробу з перезавантаженням вікна
   (до трьох разів); якщо `HELLO` не приходить зовсім, виняток іде далі, як і
   раніше.
2. **Витіснення дає помилки в консолі** — обрив рукостискання WebSocket. Це
   штатна поведінка моста, і клієнт відповідає на неї перепідключенням. Такі
   помилки тепер відсіюються **з лічильником**: їхнє число друкується в
   діагностиці кожної перевірки, тобто сховати обрив, що став масовим,
   фільтр не дає. Перевірено мутацією: удавана `console.error` у клієнті
   валить усі шість перевірок.
3. ⚠️ **Сторож пам'яті сам розгортав смужку — і валив «смужка згорнута за
   замовчуванням».** Це не вада: клієнт так і задуманий (задача 0021), а
   `min_window` справді просів до **32 428 Б** при межі 32 768. Просідання
   набігло за десяток прогонів поспіль; після перезавантаження моста
   `min_window` = **51 816**, сторож мовчить.
4. **Стан моста деградує з накопиченням сесій:** при `evicted` 215 прогін не
   дійшов і до половини, при 60 пройшов цілком. Чистий прогін вимагає свіжого
   моста.
5. **Wi-Fi комп'ютера після перезавантаження моста тримає стару асоціацію:**
   SSID і адреса на місці, жоден пакет не проходить. Лікується
   перепідключенням; бос дозволив зробити це самому.

⚠️ **Прошивання знову пішло тільки з `-b 115200`** — сьомий випадок поспіль.
Гіпотеза про хибно виміряний кварц із минулої передачі зміни лишається
неперевіреною: кабель не міняли.

### Хто що робив

**Розділ 9, третя сесія команди (Opus 5):** розкладка, прилад, мутаційні
прогони, збірка й прошивання моста, прогін проти живого стенда. Помічників не
залучав: робота йшла по трьох файлах, і вся вона — про числа, які треба було
міряти самому. `code-reviewer` не викликався — прошивка пульта в цій задачі не
чіпалась, а власні вади ловились мутаційним прогоном (три штуки, усі описані
вище).

---

## Рецензія
<!-- ВЛАСНИК: асистент. Заповнюється ПІСЛЯ самостійної перевірки в репозиторії,
     а не за текстом розділу «Результат». -->

**Чим перевірено:**

| Критерій | Вердикт | Доказ |
|---|---|---|
| | | |

### Відгук боса за критеріями 6.1–6.2, знятий 2026-08-03

Записано асистентом зі слів боса одразу після проходу на стенді. Знімки
обох орієнтацій — у розмові.

**Головне: те, що просилось у розділах 1–4, зроблено, і бос на це не
скаржиться.** Названі три групи нового.

**1. ⚠️ Два клієнти воюють між собою по колу.** Коли підключається другий,
обидва циклічно втрачають зв'язок і по черзі отримують кадри.

> **Перевірив за кодом, бос має рацію, і моє попереднє твердження було
> неповним.** Міст справді витісняє старого клієнта
> (`ws_bridge.c:322-333`), але клієнт **сам вертається**: `app.js:17`
> `RECONNECT_MS = 1000`, `app.js:720` `scheduleReconnect()`. Тобто
> витіснений повертається через секунду й витісняє того, хто його витіснив.
> Витіснення без домовленості на боці клієнта дає не одного господаря, а
> нескінченну гойдалку.
>
> ⚠️ **Це заразом гіпотеза до непоясненого просідання пам'яті до 22 КБ** у
> першому сеансі стенда. Спростування «винні два клієнти» знімалось на двох
> клієнтах, але, найімовірніше, **спокійних**, а не в гойдалці: гойдалка —
> це безперервне створення й закриття з'єднань. Перевіряти окремим замірем,
> не вважати доведеним.

**Що просить бос — явне переймання керування, а не мовчазне витіснення:**

- клієнт, що підключається, коли пульт уже зайнятий, бачить **«Є активне
  підключення до пульта»** і кнопку **«Перейняти керування»**;
- натискання попереджає, що іншого клієнта буде відключено, і дає дві
  кнопки — **«Скасувати»** і **«Підключитись»**;
- відключений клієнт бачить те саме вікно очікування з тією самою кнопкою
  «Перейняти керування»;
- ⚠️ **той, хто чекає, трафіку не споживає взагалі.**

Навіщо саме так, словами боса: забуту вкладку на іншому пристрої не
доведеться йти й закривати — керування переймається з будь-якого пристрою,
а повернувшись до першого, його переймають назад. Активний клієнт завжди
один, решта чекають, колізій немає за побудовою.

**2. Вигляд — три зауваження.**

- ⚠️ **Синя рамка навколо зображення екрана — прибрати.** Бос бачить її не
  завжди, і це збиває з пантелику. *Перевірив: це не межа блоку, а
  **ознака фокуса** — `style.css:121`, `#screen-wrap:focus`. Тобто вона й
  має з'являтися не завжди. Прибирати можна, але **не мовчки**: вимога 5.4
  задачі 0022 (видно, чи є фокус) лишається чинною, і її бере на себе
  смужка «клавіатура ✓».*
- **У книжковій орієнтації третя панель має власну кнопку розгортання**, а
  не тулить свої кнопки до бічних: місця там достатньо. Бічні кнопки
  розгортання при цьому **завеликої дрібності** — їх треба повернути до
  нормального розміру.
- **Кнопки на бічних панелях — мінімальний відступ** від інших елементів і
  від країв екрана.

**3. Прошивання моста тільки на зниженій швидкості** — бос припускає
**старий кабель**, а не міст і не програму. Гіпотеза дешева й перевіряється
іншим кабелем за хвилину.

### Що з цього куди йде

- **6.1 і 6.2 закриті.** Критерій просив пройти сценарій і назвати, що
  лишилось незручним, — пройдено й названо.
- **Пункт 2 (вигляд) дописано в цю ж задачу** розділом 7 нижче. Підстава:
  це рівно її предмет, правки дрібні, а сесія, яка щойно писала цю верстку,
  тримає її в пам'яті. Нова задача тут коштувала б дорожче за саму роботу.
- **Пункт 1 (переймання керування) — окрема задача.** Це не вигляд, а
  поведінка з'єднання: зачіпає і міст, і клієнта, і потребує власних
  критеріїв. Оформлю після закриття сесії.
- **Пункт 3 (кабель)** — перевірка іншим кабелем при наступному
  прошиванні; окремої задачі не варте.

### Друге коло відгуку боса, 2026-08-03

Знято після прошивання моста з розділом 7. Знімки книжкової орієнтації —
у розмові.

⚠️ **Одне із чотирьох зауважень — не нова вимога, а невиконаний критерій.**

**1. ⛔ Зображення екрана падає вниз, коли згорнути всі панелі** (книжкова
орієнтація). Це порушення критерію **3.1**, який поширює на книжкову
орієнтацію правило 2.5: зображення стоїть **по центру вікна**, і посунути
його може лише панель, якій бракує місця, за умови, що з іншого боку місце
є. В альбомній орієнтації це працює, у книжковій — ні.

⚠️ **Формулювання боса, яке варто зберегти дослівно, бо воно точніше за
моє:** «він по центру вікна, і посувати його може тільки панель, якщо їй не
вистачає місця, а з іншої сторони екрану воно є».

**2. Три кнопки розгортання — в один ряд, одного стилю.** Завширшки кожна
займає стільки, скільки займає її панель, **навіть коли панель згорнута**.

**3. Кнопки розгортання бічних панелей стають надто вузькими, коли панель
згорнута** — ширину зафіксувати. (Пов'язане з пунктом 2 і випливає з нього.)

**4. Між кнопками розгортання — мінімальний відступ**, в обох орієнтаціях,
щоб було видно межу між ними.

### Вердикт першого кола

**На доопрацювання.** Розділи 1–7 зроблені, і бос на них не скаржиться, але
критерій 3.1 закритим вважати не можна: доказом його виконання були числа з
альбомної орієнтації, а в книжковій поведінка інша. Це не причіпка до
дрібниці — центрування зображення бос формулював як вимогу двічі, і обидва
рази окремо наголошував, що посувати екран має право лише панель.

Решта трьох зауважень — нові, дописані розділом 8.

### Третє коло відгуку боса, 2026-08-03

⚠️ **Одне зауваження — регресія, якої ніхто не просив.** Друге — про
розмір, що росте з кожним колом.

**1. ⛔ Прив'язку панелей переписано без вимоги.** Було: панелі прив'язані
до **нижнього краю зображення екрана** і розгортаються від нього. Стало:
розгортаються від **нижнього краю вікна**, через що кнопки розгортання
зависають на різній висоті — залежно від того, скільки вмісту в панелі.

Такої вимоги в задачі немає. Розділ 8 просив поставити три кнопки в один
ряд однакового вигляду й дати середній панелі власну кнопку — про точку
відліку не йшлося взагалі, а стара поведінка була погоджена й працювала.

**2. Висота кнопок росте з кожним колом.** Потрібно: **висота кнопки
дорівнює ширині кнопок бічних панелей в альбомній орієнтації.** Одне число
на обидві орієнтації, щоб воно перестало повзти.

**3. Правило, назване босом окремо і записане в `CLAUDE.md`** (розділ
«Виправлення не переписує погоджене»): правка змінює тільки те, чого
вимагали. Погоджене лишається, доки прямо не суперечить новій вимозі, а
суперечність називається вголос.

### Вердикт другого кола

**На доопрацювання.** Пункти 8.2–8.4 за знімками виглядають зробленими, але
разом із ними приїхала зміна, якої не просили, і вона зачіпає те, що бос
уже приймав. Формальної рецензії за доказами **поки не роблю навмисно**:
розкладка рухається третє коло поспіль, і рецензувати мішень, яка їде, —
марна робота. Зроблю суцільну, коли вигляд стане на місце.

⚠️ **Що вже перевірено й не потребує повтору:** обмеження задачі дотримані —
`git diff e263c1a..HEAD -- firmware/edgetx-patch/ upstream/ docs/` дає
**лише** нові знімки в `docs/img/client/`, тобто прошивку пульта не чіпали
й у `docs/` поза `img/` не лізли.

⛔ **Прогін проти живого моста не робився** (мережа ПК вимкнена). Минуле
коло знайшло дві вади приладу, які проявились **тільки** проти моста, тому
цей прогін лишається обов'язковим до приймання.

**Вердикт:** на доопрацювання

**Що піде в наступну задачу:**
