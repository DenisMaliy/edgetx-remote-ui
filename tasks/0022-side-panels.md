# 0022. Бокові панелі: пультом можна користуватися з телефона

- **Створено:** 2026-08-03
- **Етап:** 3
- **Статус:** нова
- **Виконавець:** сесія команди

## Мета
<!-- ВЛАСНИК: асистент. Команда цей розділ не редагує. -->

Закрити **третю ціль MVP**, єдину з трьох, яка досі не зроблена: бокові панелі
з клавішами й енкодером у клієнті на телефоні.

Зараз пультом можна керувати лише тиканням у зображення екрана. Клавіш і
енкодера на телефоні немає взагалі — колесо миші й права кнопка, додані в
0019, працюють тільки в браузері на комп'ютері.

⚠️ **Це те, заради чого проєкт починався.** Останні півтора десятка задач
покращували картинку; ця робить пульт **придатним до вжитку** без свого
екрана.

## Що вже є і на що спиратись

- `HELLO` віддає **перелік клавіш із підписами** — 7 на TX16S
  (`RTN, Enter, PAGE<, PAGE>, MDL, TELE, SYS`). ⚠️ **Брати звідти, не
  зашивати.** Правило проєкту: специфіки конкретного пульта в коді — нуль
  рядків.
- Протокол уміє все потрібне: `KEY`, `ENC`, `TRIM`, повний стан вводу
  раз на 250 мс, тайм-аут відпускання.
- Довге натискання, автоповтор і прискорення енкодера дає **сам EdgeTX** —
  клієнт має лише чесно тримати стан «натиснуто», а не слати одиничні тики.

## Критерії приймання
<!-- ВЛАСНИК: асистент. Розкладку задала людина 2026-08-03. -->

### 0. Спершу — регресія, яку помітила людина

- [ ] 0.1. ⚠️ **Стрілки на клавіатурі раніше працювали, зараз ні.** Знайти,
      **коли й чим** зламалось (`git log` по `webui/`), а не просто дописати
      наново. Зламане непомітно означає, що щось не покрите перевіркою — і це
      важливіше за саму стрілку.
- [ ] 0.2. Причина названа в «Результаті» комітом.

### 1. Зображення екрана

- [ ] 1.1. Співвідношення сторін пульта зберігається, зображення
      **максимально велике** в доступному вікні.
- [ ] 1.2. Розмір перераховується, коли панелі згортають чи розгортають, і
      коли міняється орієнтація.

### 2. Розкладка — задана людиною

**Альбомна:** кнопки з боків, порядок як на самому пульті, щоб не ламати
звичку руки. Порядок звірений із фотографією пульта, наданою людиною
2026-08-03: `PAGE>` справді стоїть **над** `PAGE<`, і це не описка.

| бік | зверху вниз |
|---|---|
| ліва панель | `SYS`, `RTN`, `PAGE>`, `<PAGE`, `TELE` |
| права панель | `MDL`, джойстик |

**Портретна:** один рядок знизу, поділений на дві частини — ліва й права, з
тим самим наповненням і тим самим порядком.

- [ ] 2.1. Обидві орієнтації працюють, розкладка відповідає таблиці.
- [ ] 2.2. ⚠️ **Порядок кнопок і склад беруться з `HELLO`, а не зашиваються.**
      Таблиця вище описує **бажане розташування для цього пульта**; клавіша,
      якої немає у вітанні, не показується, а невідома клавіша не ламає
      панель. Пульт з іншим набором має дати робочі панелі без правки коду.
- [ ] 2.3. ⚠️ **`SYS` і `MDL` виглядають інакше за решту.** Це не оздоблення:
      на самому пульті вони — широкі світлі кнопки **над** екраном, ліворуч і
      праворуч, тоді як `RTN`, `PAGE>`, `PAGE<`, `TELE` — вузькі чорні
      ребристі на лівому ребрі. Панель має цю різницю відтворювати, бо рука
      шукає їх окремо від решти.
- [ ] 2.4. Панелі згортаються й розгортаються: бокові — вбік, нижня — вниз.
- [ ] 2.5. Стан «згорнуто/розгорнуто» запам'ятовується між відкриттями
      сторінки. Інакше кожне перепідключення повертає розкладку до типової,
      а перепідключень у нас багато.

### 3. Джойстик замість енкодера

⚠️ **Двічі переглянуто 3 серпня, чинним є це формулювання.** Проміжний
варіант прив'язував вліво-вправо до `PAGE<`/`PAGE>` (бо сторінки гортаються
горизонтально) — **скасовано рішенням людини**: усередині сторінки
горизонтального руху на цьому пульті немає, тож стрілка вліво не має
природного сенсу, а той, хто звик до пульта, знає `PAGE<` і `PAGE>` як
**окремі кнопки**. Розмивати їх стрілками означає псувати саме ту звичку,
заради якої ми відтворюємо розкладку.

**Правило, яке нічого не знає про конкретний пульт.** Джойстик і клавіатура
виражають **намір**; чим його виконати, вирішує перелік клавіш із `HELLO`:

| намір | є справжні стрілки | стрілок немає (TX16S) |
|---|---|---|
| вгору / вниз | клавіші-стрілки | **клацання енкодера** |
| вліво / вправо | клавіші-стрілки | **не показуються** |
| вибрати | `Enter` | `Enter` |
| назад | `RTN` | `RTN` |

⚠️ **Вліво-вправо ніколи не стають сторінками** — ні на джойстику, ні на
клавіатурі. Сторінки перемикаються **лише** своїми кнопками `PAGE<` і
`PAGE>` на панелі, а на клавіатурі — `PageUp` і `PageDown`.

- [ ] 3.1. Правило вище реалізоване й перевірене **на нашому пульті**
      (гілка «стрілок немає»): джойстик дає вгору, вниз і центр.
- [ ] 3.2. ⚠️ **Прискорення EdgeTX працює:** швидкі рухи дають більший крок.
      Це вже є в прошивці; клієнт має слати клацання з правдивими проміжками,
      а не пачкою.
- [ ] 3.3. Утримання напрямку дає **автоповтор**, і дає його EdgeTX, а не
      клієнт.
- [ ] 3.4. Заміряно: скільки клацань за секунду віддає джойстик і чи
      вистачає цього, щоб пройти довгий список.

### 4. Клавіші: натиснуто означає натиснуто

- [ ] 4.1. Натискання й **відпускання** передаються окремо: утримання кнопки
      на телефоні = утримання клавіші на пульті. Довге натискання й автоповтор
      дає EdgeTX.
- [ ] 4.2. ⚠️ **Утримання 10 секунд не розтискається само.** Перевіряється
      незалежним каналом (режим USB-джойстика), а не оком.
- [ ] 4.3. Палець, що зісковзнув із кнопки, **відпускає** її.

### 5. Клавіатура й миша — застосунок працює і з ПК

- [ ] 5.1. Коли зображення екрана має фокус, клавіатура виражає **ті самі
      наміри**, що й джойстик, і йде тим самим правилом розділу 3: стрілки
      вгору/вниз, `Enter` — вибрати, `Esc` — назад.
- [ ] 5.2. `PageUp`/`PageDown` — сторінки. ⚠️ Стрілки **вліво/вправо** на
      пульті без стрілок лишаються **без дії**: прив'язувати їх до сторінок
      заборонено рішенням людини (див. розділ 3).
- [ ] 5.3. Миша: ліва кнопка — дотик, колесо — енкодер, права — `RTN`.
      Частина цього вже є з 0019; звірити, що не суперечить новому.
- [ ] 5.4. Видно, чи є фокус. Інакше «клавіатура не працює» неможливо
      відрізнити від «фокус в іншому місці» — а це вже сталося (пункт 0.1).

### 6. Діагностична панель

- [ ] 6.1. Переїжджає **вгору**, згорнута за замовчуванням, розгортається
      вниз — за тим самим правилом, що й інші панелі.
- [ ] 6.2. Те, що потрібне під час роботи, лишається видимим і згорнутою:
      мінімум вільної пам'яті й ознака швидкості вже показали свою вартість.
      Що саме лишити — рішення сесії, назвати в «Результаті».

### 7. Головна перевірка — людина користується

- [ ] 7.1. ⚠️ **Людина проходить сценарій цілком, із телефона, не торкаючись
      комп'ютера:** зайти в налаштування моделі, змінити значення джойстиком,
      вийти, перемкнути модель. Відгук її словами.
- [ ] 7.2. Те саме з ПК — клавіатурою й мишею, не торкаючись екрана.
- [ ] 7.3. Названо, що незручно. Не «все добре» — конкретні місця.

### 8. Не зламати те, що працює

- [ ] 8.1. Затримка й частота кадрів не погіршали проти чисел 0020.
- [ ] 8.2. ⚠️ **Сторож мовчання не спрацьовує при живому клієнті** —
      перевірити під час утримання клавіші. Це заразом умова, за якої ми
      колись відкриємо закриту задачу 0015.
- [ ] 8.3. Вільна пам'ять моста не впала нижче межі з 0021.

## Обмеження
<!-- ВЛАСНИК: асистент. -->

- ⚠️ **Прошивка пульта не чіпається.** Протокол уже вміє все потрібне; якщо
  здасться, що ні — це зупинка й розмова, а не тихе розширення.
- ⚠️ **Нуль рядків специфіки TX16S у клієнті.** Клавіші, їх кількість,
  підписи, наявність сенсора — усе з `HELLO`.
- Дозволені файли: `webui/**`, `firmware/esp32/**` (лише віддача нових
  файлів), `tools/**`, `tasks/0022-*.md` (тільки «Результат»),
  `state/PROGRESS.md`, `state/handoffs/`, `state/DECISIONS.md` (тільки
  додаванням), `docs/img/**`.
- ⚠️ **`docs/` (крім `img/`) не чіпати.**
- ⚠️ Кожна правка `webui/` коштує прошивання моста — клієнт живе в його
  бінарнику.
- `code-reviewer` перед комітом — правило `CLAUDE.md`; дозвіл людина дає
  окремо.

---

## Результат
<!-- ВЛАСНИК: команда. Асистент цей розділ не редагує. -->

⚠️ **Усе, що можна довести без стенда, доведено на стенді без заліза.** Побічним
продуктом задачі став `tools/fake_radio.py` — програмний двійник **пульта**;
разом із наявним `webui_serve.py` він дає повний ланцюг «браузер → міст →
пульт» на самому ПК. Числа нижче зняті справжнім браузером через Playwright,
а не міркуванням.

⛔ **Критерії з людиною й зі стендом (7.1–7.3, 8.1–8.3) не закриті** — вони й не
могли бути закриті тут. Найважливіше з них — 8.3, і воно **під загрозою**:
розділ «Що не вийшло».

### 0. Регресія зі стрілками: її не було

⚠️ **Відповідь протилежна до припущення задачі, і це важливо, бо задача
шукала зламане.** Клавіатури в браузерному клієнті **не було ніколи**, тож і
ламатися не було чому.

Доказ — історія, а не читання коду:

```
git log --all -S 'keydown'                 → жодного коміту
git log --all -S 'ArrowUp'                 → жодного коміту
git log --all -G "addEventListener\('key"  → жодного коміту
```

Стрілки живуть — і працюють досі — у **клієнті ПК на tkinter**,
`tools/test_client.py`: `LABEL_TO_KEYSYM` і `keysym_to_enc`, коміт `ce1171e`
від 26.07, відтоді не змінювані (`git log -S 'keysym_to_enc'` дає рівно один
коміт). Перевірено виконанням, а не оком: `Input` піднято із заглушками замість
вікна й з'єднання, `HELLO` — набір TX16S; `on_key_press("Up")` дає `enc −1`,
`"Down"` — `enc +1`, `"Escape"` — `RTN`.

Тобто людина порівнювала **два різні клієнти**: у вікні на ПК стрілки були, у
браузері — ні. Критерій 0.2 («причина названа комітом») відповіді не має за
побудовою: немає коміту, який щось зламав.

⚠️ **Але висновок задачі лишається чинним, просто інший:** непокритим було не
одне місце, а весь `app.js` — і `app_test.js`, що з'явився в 0020, покривав
лише очікування кадру. Тепер клавіатура є і в браузері (розділ 5), і вона
покрита двічі: `app_test.js` (наміри й утримання без DOM) і прогін у
справжньому браузері.

### Зроблено

**Головне правило — намір, а не клавіша** (`webui/panels.js`). Джойстик і
клавіатура кажуть «вгору», «вибрати», «назад»; чим це виконати, вирішує перелік
клавіш із `HELLO`. На пульті зі справжніми стрілками намір «вгору» — клавіша
`UP`; на TX16S стрілок немає, і той самий намір виконує клацання енкодера.
⚠️ **Вліво-вправо не виконує ніхто і кнопок під них не з'являється** — рішення
людини від 03.08 виконане буквально, у тому числі на клавіатурі.

**Утримання замість клацання** (`webui/app.js`, розділ «Утримання»). Пульту
йдуть окремо натискання й відпускання; довге натискання, автоповтор і
прискорення лишаються за EdgeTX. Один намір можуть тримати кілька рук (палець
на панелі + стрілка на клавіатурі), тому не прапорець, а **множина тримачів**:
відпускання йде на останньому.

**Сім шляхів відпустити, і всі перевірені:** підняли палець; система перебила
(`pointercancel`); палець зісковзнув із кнопки; захоплення вказівника
загубилось (`lostpointercapture`, `pointerleave`); вкладку сховали; вікно
втратило фокус; зв'язок обірвався. Плюс два, які знайшла рецензія: **перебудова
панелі новим `HELLO`** і **згортання панелі другою рукою** — обидва прибирають
кнопку разом з обробником, тобто відпускати стало б нікому.

**Розкладка з `HELLO`, а не з коду.** `SIDE_ORDER` описує **бажане місце для
ролі**, а не буквальний напис: пульт, що зве «назад» міткою `EXIT`, отримує її
там само, де TX16S отримує `RTN`. Клавіша, якої в `HELLO` немає, не
показується; клавіша, якої немає в таблиці, лягає в кінець лівої панелі й
позначається тьмяніше. `SYS` і `MDL` — широкі світлі, решта — вузькі чорні
ребристі, як на самому пульті.

**Смужка стану переїхала вгору**, панелі — вниз і вбік; стан «згорнуто»
переживає перезавантаження (`localStorage`).

### Файли

- `webui/panels.js` — **новий**: наміри, розкладка, джойстик, розгін енкодера
- `webui/panels_test.js` — **новий**: 29 перевірок, з них половина — про **чужий**
  пульт (інші мітки, справжні стрілки, без енкодера)
- `webui/app.js` — утримання, наміри, клавіатура, фокус, згортання панелей;
  ⚠️ прибрано `console.log` на кожну плитку й кінець кадру (гаряча ділянка)
- `webui/index.html`, `webui/style.css` — сцена «панель — екран — панель»
- `webui/app_test.js` — заглушка DOM доросла до `classList`/`localStorage`;
  два нові розділи: множина тримачів і наміри
- `tools/fake_radio.py` — **новий**: двійник пульта, журнал вводу рядками JSON
- `tools/webui_probe.js`, `tools/README.md` — новий файл сторінки в переліках
- `firmware/esp32/main/{ws_bridge.c,CMakeLists.txt}` — віддача `/panels.js`

### Заміри

**Розгін енкодера, справжній браузер, утримання джойстика 2 с** (проміжки між
клацаннями, як їх бачить пульт, мс):

```
261 214 175 144 119 96 104 46 70 71 71 70 70 103 38 71 72 68 71 70
```

- **21 клацання за 2 с**, медіана проміжку **71 мс** → стеля **14 клацань/с**
  (критерій 3.4);
- проміжки **правдиві**: перший неспішний (тик = один пункт), далі
  скорочуються до стелі. Пачки немає — найменший проміжок 38 мс, і це джитер
  таймера браузера, а не наш крок;
- ⚠️ **чому не нуль:** прискорення EdgeTX рахується з `dt` між клацаннями
  (гачок 7, задача 0008), тож пачка застигла б на максимальному кроці.

**Утримання 10 секунд** (критерій 4.2, на двійнику):

- тривалість **10.02 с**, за неї рівно **одне** натискання й **одне**
  відпускання — самовільного розтискання немає;
- **40 пакетів `INPUT_STATE`**, проміжок **рівно 250 мс**, маска клавіші
  ненульова в **усіх сорока** — тобто сторож мовчання не спрацював жодного разу
  (це і є структурна частина критерію 8.2).

**Розкладка й розмір картинки** (Playwright, обидві орієнтації):

| | альбомна 900×420 | портретна 420×860 |
|---|---|---|
| ліва панель | SYS, RTN, PAGE>, PAGE<, TELE | той самий порядок, рядками |
| права | MDL + джойстик | MDL + джойстик |
| картинка | 573×325 | 412×233 |
| співвідношення | 1.763 (пульт 1.765) | 1.768 |
| згорнули панель | місця під картинку 624 → 704 | висоти 519 → 705 |

⚠️ **Сама картинка при згортанні росте не завжди, і це правильно:** в альбомній
її обмежує **висота**, у портретній — **ширина**. Перерахунок відбувається
щоразу (`fitCanvas` + `ResizeObserver`), просто інша вісь буває зайнята.

**Наміри на TX16S** (рядок із панелі «Стан», знятий у прогоні):

```
вгору=енкодер −  вниз=енкодер +  вліво=—  вправо=—
вибрати=Enter  назад=RTN  сторінка ←=PAGE<  сторінка →=PAGE>
```

**Тести:** `proto_test` 84, `wait_test` 32, `panels_test` 29, `app_test` 38 —
усі зелені; `python3 tools/proto_test.py` 91 тест; `wait_crosscheck` — 22
сценарії, сліди двох клієнтів збіглися повністю. Прошивка моста збирається,
`/panels.js` вбудований стисненим.

### 6.2 — що лишилось видимим при згорнутій панелі

Рішення сесії: **зв'язок, кадри (з часткою цілих), зайнятість потоку, фокус
клавіатури, пульт, пам'ять моста, швидкість каналу, режим очікування**. Тобто
все, що вже показало свою вартість у 0019–0021, плюс один новий — фокус:
без нього «клавіатура не працює» не відрізнити від «фокус в іншому місці», а
це рівно та плутанина, з якої почалась ця задача (розділ 0).

### Відхилення від задачі і чому

- **Автоповтор напрямку на джойстику дає клієнт, а не EdgeTX** (критерій 3.3).
  Інакше неможливо за побудовою: на TX16S напрямок виконує **енкодер**, а він
  не «натиснутий» — він «клацнув», і повторювати прошивці нема чого. Тому
  клієнт шле клацання сам, із правдивими проміжками, і **прискорення все одно
  дає EdgeTX** — воно рахується з `dt`. На пульті зі справжніми стрілками
  критерій виконується буквально: клавіша тримається рівнем, повтор — з
  прошивки.
- **Джойстик зроблений хрестовиною з п'яти кнопок**, а не аналоговою ручкою з
  розбором зон. Кожна кнопка сама розбирається зі своїм пальцем — захоплення,
  зісковзування, відпускання, — тобто найдорожча частина критерію 4 виходить із
  будови, а не з нашої арифметики по координатах.
- **`tools/fake_radio.py` не просила жодна задача.** Без нього ця робота
  приїхала б до людини неперевіреною: симулятор EdgeTX не збирається (немає
  `clang`), а кожна правка `webui/` коштує прошивання моста.

### Що не вийшло

⚠️ **Критерій 8.3 під загрозою, і число вже відоме — сторінка поважчала на
третину.**

| | було gz | стало gz |
|---|---:|---:|
| `index.html` | 1 890 | 2 615 |
| `proto.js` | 10 320 | 10 320 |
| `wait.js` | 6 311 | 6 311 |
| `panels.js` | — | 5 995 |
| `app.js` | 23 207 | 28 773 |
| `style.css` | 1 652 | 3 828 |
| **разом на дроті** | **43 380** | **57 842** (**+33 %**) |

Задача 0021 встановила: **ціна сторінки в купі моста дорівнює її розміру на
дроті**, і після стиснення найглибша точка була саме віддача сторінки —
43 428 Б вільних. Пряма арифметика дає **≈29 КБ** після цієї задачі, тобто
**нижче межі тривоги 32 768 Б** і нижче потоку пікселів (38 976 Б): найгірший
випадок повертається до сторінки. Це передбачення, а не замір — заміряти можна
лише на стенді, і це **перше, що варто зробити** після прошивання: холодне
завантаження сторінки, потім `пам'ять` у смужці й розклад «хто з'їв» у панелі.

Куди дивитись, якщо запас справді зник: коментарі — 3.3 КБ із 8.6 КБ у
`style.css` і стільки ж пропорційно в `app.js` (українською, тобто в UTF-8
кожна літера по два байти). Свідомо **не** різав: пояснення в цьому проєкті —
частина коду, а рішення «чим пожертвувати» більше за цю задачу.

Не закриті й потребують людини та стенда:

- **7.1–7.3** — сценарій із телефона й із ПК, відгук словами, що незручно;
- **8.1** — затримка й кадри проти чисел 0020 (⚠️ прибрані `console.log` на
  гарячій ділянці могли їх **покращити**, тож порівнювати треба наново);
- **8.2** — сторож мовчання на живому мості (структурно закрито, заміром — ні);
- **4.2** незалежним каналом (USB-джойстик): тут доведено двійником, а задача
  просила саме незалежний канал;
- **3.2** у пунктах меню за секунду: клацань за секунду ми знаємо (14), а
  скільки з них стане пунктів після прискорення EdgeTX — видно лише на пульті.

---

## Додано 2026-08-02, сесія на стенді: 8.3 провалився, полікований, 8.1 / 8.2 / 4.2 закриті

### 8.3 — провал підтверджений, потім закритий мініфікацією

⚠️ **Передбачення «≈29 КБ» було оптимістичним. Замір дав 20.5 КБ.**

Три холодні завантаження справжнім браузером (свіжий контекст = порожній кеш,
міст щойно після перезавантаження):

| | min_page, Б | сторож |
|---|---:|---|
| межа тривоги | 32 768 | — |
| прогін 1 | 21 276 | **спрацював** |
| прогін 2 | 21 276 | спрацював |
| прогін 3 | **20 504** | спрацював |

⚠️ **Заразом упала арифметика 0021 «ціна сторінки в купі = вага на дроті».**
Сторінка важила 57.8 КБ, а з'їла **85.2 КБ** (105.7 → 20.5). Тобто після
вбудовування gzip ціна визначається вже не сумою байтів, а тим, що браузер
тягне файли **шістьма з'єднаннями одночасно**, і кожне бере свій буфер TCP.
Правило 0021 треба вважати чинним лише до gzip.

**Лікування — мініфікація на збірці** (дозвіл людини, дано наперед на випадок
провалу). Українські коментарі з джерела **не різались**: вони лишились у
`webui/*.js` і зникають лише з примірника у флеші.

Числа зняті з `build/esp-idf/main/webui-gz/` — тобто з того, що справді
вбудовується, а не з окремого прогону мініфікатора:

| файл | було gz | стало gz |
|---|---:|---:|
| `index.html` | 2 615 | 2 772 |
| `proto.js` | 10 320 | 2 842 |
| `wait.js` | 6 311 | 915 |
| `panels.js` | 5 995 | 1 923 |
| `app.js` | 28 773 | 8 744 |
| `style.css` | 3 828 | 1 449 |
| **разом на дроті** | **57 842** | **18 645** (**−68 %**) |

⚠️ **Сторінка стала легшою, ніж була до появи панелей** (43 380 Б). Прошивка
моста схудла на 38 КБ, завантаження сторінки — 331 → 175 мс.

⚠️ **`index.html` єдиний **поважчав**, і це не недогляд, а виняток із правила.**
Мініфікатор його лише копіює (esbuild HTML не вміє, а різати коментарі
регулярним виразом — спосіб колись зачепити `<!--` усередині рядка), отже
коментарі саме цього файлу їдуть у флеш. Спершу сюди був дописаний цілий
абзац пояснення — він коштував **236 Б на дроті**, і при цьому три документи
поруч стверджували, що коментарі у флеш не потрапляють. Знайшла друга
рецензія; абзац скорочено до вказівника, лишилось 157 Б.

Замір після прошивання, п'ять холодних завантажень на двох збірках:

| | min_page, Б |
|---|---:|
| межа тривоги | 32 768 |
| до мініфікації, найгірше з трьох | 20 504 |
| **після, найгірше з п'яти** | **62 512** |
| найкраще з п'яти | 85 764 |

Сторож (`warned`) — **false** в усіх прогонах. **Критерій 8.3 закрито.**

Найгірша точка лишилась за сторінкою (62.5 КБ) проти потоку пікселів
(78–88 КБ), тобто порядок із 0021 не змінився — змінився запас.

### Як це зроблено і чому саме так

- `webui/minify.mjs` + `webui/package.json` — **esbuild, версія закріплена
  точним числом** (`0.25.10`, без «^»), плюс `pnpm-lock.yaml`. Умова людини.
- `webui/pnpm-workspace.yaml` — `allowBuilds: esbuild: false` **явно**. pnpm 10+
  лишає тут незакрите питання, а незакрите питання означає, що збірка
  поводиться по-різному на різних машинах. Перевірено: двійник esbuild
  приходить готовим у платформному пакеті, postinstall не потрібен.
- **Збірка не має тихого відкату.** Немає інструмента — `FATAL_ERROR` із
  назвою команди лікування. Мовчазний відкат до немініфікованого зібрався б і
  провалив 8.3 так само тихо, як провалив його вперше.
- ⚠️ **`--charset=utf8`, не типовий ascii.** Типово esbuild перетворює кирилицю
  на `\uXXXX`. Після gzip різниця мала (16 031 проти 15 873 Б), але
  екранований файл нечитний, коли доводиться дивитись у те, що поїхало в пульт.
- `--target=esnext` — синтаксис не перетворюється взагалі. Джерело вже працює
  в потрібних браузерах; будь-яке зниження рівня мови — ризик без вигоди.

**Чому мініфікація тут безпечна — і чому перше пояснення було хибним.**

⚠️ Спершу тут стояло: «кожен наш модуль загорнутий в IIFE, тож
перейменовуються лише внутрішні імена». **Це неправда, і знайшла її
рецензія `code-reviewer`.** Обгортку мають `proto.js`, `wait.js` і
`panels.js`, а `app.js` — ні: увесь його верхній рівень лежить у глобальній
області, і це найбільший файл із шести. Перевірено читанням: `app.js:1` —
`'use strict';`, далі 1 680 рядків без обгортки.

**Справжня гарантія — `bundle: false`, і більше ніщо.** Для незібраного файлу
верхній рівень і є глобальна область, тому esbuild його не перейменовує.
Перевірено на виході: `"use strict";const P=RemoteUI,Wait=RemoteUIWait,
Panels=RemoteUIPanels,RECONNECT_MS=1e3,…` — усі імена цілі.

⚠️ **Різниця не термінологічна.** Хибне пояснення виглядало як дозвіл чіпати
`bundle`, а зміна одного цього слова на `true` убила б сторінку повністю.
Тому в `minify.mjs` біля рядка стоїть заборона, а не опис.

Окремо перевірено: в `index.html` немає вбудованих обробників (`onclick=`), у
коді немає `constructor.name`, `eval` і `.toString()`, а всі `.name` — це поля
клавіш із `HELLO`.

### ⚠️ Зелені тести перевіряють не той файл — і це закрито інструментом

Умова людини разом із дозволом: прогін Playwright робити проти **зібраної**
сторінки. `panels_test.js`, `app_test.js`, `proto_test.js` читають джерело,
тож зламане мініфікатором пройшло б повз них зеленим.

- `tools/webui_built_check.py` — **новий**: сам мініфікує, сам піднімає стенд
  без заліза (`fake_radio.py` + `webui_serve.py --dir`), проганяє справжній
  Chromium і сам гасить стенд у `finally` (пастка 0020: драйвер попередньої
  сесії прожив 1 год 14 хв і мовчки ділив дріт). **16 перевірок, усі зелені.**
- `tools/webui_serve.py` — прапорець `--dir`, щоб стенд віддавав зібране.

Перевіряється те, що ламає саме мініфікатор: глобальні `RemoteUI*` не
перейменовані, публічна поверхня ціла, панелі будуються з `HELLO`, джойстик
дає `enc`, клавіша дає пару «натиснуто/відпущено», клавіатура працює,
вліво-вправо не роблять нічого, і жодної помилки JS.

Старі тести теж зелені: `proto_test` 84, `wait_test` 32, `panels_test` 29,
`app_test` 38, `proto_test.py` 91, `wait_crosscheck` 22 сценарії.

### 8.1 — не погіршало

Ті самі жести, що в 0020, числа з моста (`uart.*` — від політики показу
клієнта не залежать), швидкість каналу 2 625 000:

| орган керування | кадр/с (0020) | кадр/с (зараз) | плиток/с | КБ/с |
|---|---|---|---:|---:|
| спокій | 0.0 | **0.0** | 0.0 | 0.5 |
| протяг пальцем | 2.6–4.3 | **4.3** | 180.4 | 38.9 |
| енкодер 8 клац/с | 13.6 | **13.3** | 376.0 | 81.8 |

Затримка «ввід → показаний кадр», енкодер, n=100:

| | 0020 | зараз |
|---|---:|---:|
| медіана | 115 мс | **131 мс** |
| 90 % | ≈920 мс | **229 мс** |
| макс | — | 260 мс |

⚠️ **Медіана на 16 мс гірша (+14 %), і це записано, а не сховано.** Провали ж
скоротились учетверо: 90-й відсоток 920 → 229 мс. Кадрів цілих 148 зі 148,
`показано за строком` — 0. `crc_errors`, `dropped`, `oversized`,
`ws.tiles_dropped`, `send_dropped` — нулі за весь прогін.

### 8.2 — сторож мовчання не спрацював жодного разу

На живому мості, за весь сеанс із утриманнями клавіш:
`uart.silence_resets` **0**, `session.silence_timeouts` **0**,
`input_gap.max_ms` **266** (усі 1 426 проміжків ≤ 300 мс, жодного понад).
Умова відкриття задачі 0015 наново **не настала**.

### 4.2 — доказ узятий з екрана пульта, але не тим спостереженням, яке просили

Критерій переписаний людиною 2026-08-02: режим USB-джойстика не годиться
(віддає канали **після мікшера**, клавіш меню там не видно взагалі), доказ
брати з екрана — тримати `PAGE` десять секунд і дивитись на автоповтор.

⚠️ **Автоповтору немає, і клавіша тут ні до чого — EdgeTX не повторює `PAGE`.**
Доведено контролем, а не здогадом:

| дослід | змін екрана | кадрів від пульта |
|---|---:|---:|
| один дотик 150 мс | 2 (на 0.45 і 0.68 с) | 3 |
| **утримання 9.8 с** | **2** (на 0.68 і 0.90 с) | 4 |

Тобто утримання дало **рівно те саме, що короткий дотик**, і далі дев'ять
секунд тиші. Запропоноване спостереження двох пояснень не розрізняє.

**Але той самий дослід доводить потрібне — сильніше, і теж з екрана пульта.**
Підстава в коді: `onInputState()` (`remote_ui/input.h`) **заміщує маску
клавіш рівнем**, і клієнт шле `INPUT_STATE` безумовно раз на 250 мс.

Отже якби пульт хоч раз відпустив клавішу за ті 10 секунд, наступний
`INPUT_STATE` за ≤250 мс виставив би її знову — а це для `keysPollingCycle()`
перехід 0 → 1, тобто **нове натискання**, і сторінка перемкнулася б ще раз.
Ми ж бачили **рівно одне** перемикання за 9.8 с, при щільності зразків 200 мс.

**Висновок: за 10 секунд утримання пульт зареєстрував рівно одне натискання —
жодного самовільного відпускання.** Критерій 4.2 закрито, каналом, який
просила людина.

### 7.1–7.3 закриті людиною

✅ **Сценарій пройдено — з телефона й з ПК.** Відгук словами знятий 2026-08-02,
записаний у розділі «Рецензія», підрозділ **«Відгук людини за критеріями
7.1–7.3, знятий 2026-08-02»** (коміт `eed90eb`). Тут не переказується:
власник того розділу — асистент, а зміст живе в одному місці.

⚠️ **Перелік незручностей звідти — матеріал наступної задачі, а не
невиконані критерії 0022.** Рішення людини від 2026-08-02.

### Досі не закрито

- **3.2** скільки пунктів меню за секунду дає розгін EdgeTX. Клацань за
  секунду знаємо — 14; скільки з них стане пунктів після прискорення в
  прошивці, видно лише на пульті.

---

## Рецензія
<!-- ВЛАСНИК: асистент. Заповнюється ПІСЛЯ самостійної перевірки в репозиторії,
     а не за текстом розділу «Результат». -->

**Чим перевірено:** `git diff ded71aa..HEAD` по всіх теках; `git log --all -S`
по `webui/` і `tools/`; читання `webui/panels.js`, `webui/app.js`,
`firmware/esp32/main/ws_bridge.c`; власний запуск усіх тестів
(`proto_test.js`, `wait_test.js`, `panels_test.js`, `app_test.js`,
`tools/proto_test.py`) і власний прогін `tools/webui_built_check.py` проти
**зібраної** сторінки — 16 перевірок, невдалих 0.

| Критерій | Вердикт | Доказ |
|---|---|---|
| 0.1 | ✅ інакше, ніж очікувалось | Перевірив сам: `git log --all -S 'keydown' -- webui/` дає **один** коміт — `bbf8d6c`, цієї ж задачі. Клавіатури в браузері не було ніколи, ламатись не було чому. `git log --all -S 'keysym_to_enc' -- tools/` дає `ce1171e` від 26.07 — стрілки живі в клієнті на `tkinter`. Порівнювались два різні клієнти |
| 0.2 | ⛔ знято | Предмета не існує: немає коміту, який щось зламав. Критерій поставлено на хибному підозрюваному — п'ятий такий випадок у проєкті |
| 1.1 | ✅ | `app.js:305` `fitCanvas()`; заміряно 573×325 при співвідношенні 1.763 проти 1.765 у пульта |
| 1.2 | ✅ | `fitCanvas` кличеться з `ResizeObserver` (`app.js:1663`), зі згортання (`app.js:1251`) і з повороту |
| 2.1 | ✅ | Заміряна розкладка обох орієнтацій у «Результаті»; мій прогін дав ту саму ліву панель `SYS, RTN, PAGE>, PAGE<, TELE` і праву `MDL` + джойстик |
| 2.2 | ✅ | `panels.js:142` `planPanels()` будує з `hello.keys`; клавіші, якої немає у вітанні, не буде; невідома лягає в кінець лівої (`panels.js:183`). Половина з 29 перевірок `panels_test.js` — про **чужий** пульт |
| 2.3 | ✅ | `panels.js:95` `WIDE_LABELS`; мій прогін: «широкі кнопки виділені окремо `['SYS','MDL']`» |
| 2.4 | ✅ | `app.js:1224` `padGlyph()` — вбік в альбомній, вниз у портретній |
| 2.5 | ✅ | `app.js:1237` ключ `collapsed.<id>` у `localStorage`, з тихим відкотом, коли сховище заборонене |
| 3.1 | ✅ | Мій прогін: «джойстик побудований, 3 кнопки», «джойстик вгору → enc». `panels.js:126` бере енкодер лише коли справжніх стрілок немає |
| 3.2 | ⛔ **відкрито** | Клацань за секунду знаємо (14), пунктів меню — ні. Заміряти можна лише на пульті |
| 3.3 | ⚠️ відхилення прийняте | Автоповтор дає клієнт, не EdgeTX. Підстава в коді: `panels.js:191` — енкодер не «натиснутий», а «клацнув», прошивці нема чого повторювати. Прискорення все одно рахує EdgeTX із `dt` (гачок 7, задача 0008). На пульті зі стрілками критерій виконається буквально |
| 3.4 | ✅ | 21 клацання за 2 с, медіана проміжку 71 мс, стеля 14 клацань/с; найменший проміжок 38 мс — джитер таймера, не наш крок |
| 4.1 | ✅ | Мій прогін: «клавіша: рівно натиснуто + відпущено», дві дії з `pressed: true/false` |
| 4.2 | ✅ | Перевірив підставу в коді сам: `remote_ui/input.h:31-33` — `onInputState()` заміщує маску **рівнем**, `INPUT_STATE` іде безумовно раз на 250 мс. Отже одне самовільне відпускання дало б перехід 0 → 1 і **друге** перемикання сторінки; за 9.8 с бачили рівно одне |
| 4.3 | ✅ | `panels.js:245` `setPointerCapture`, `panels.js:260-261` `lostpointercapture` + `pointerleave`; мій прогін: «клавіша не лишилась натиснутою» |
| 5.1 | ✅ | Мій прогін: «клавіатура: стрілка вниз → enc», «Esc → клавіша назад» |
| 5.2 | ✅ | Мій прогін: «вліво/вправо не роблять нічого — дій додалось: 0» |
| 5.3 | ✅ за кодом | `app.js:1077` колесо → енкодер із перекладом одиниць `deltaY`, `app.js:1047` права кнопка → «назад» із окремою обробкою `pointercancel`. Живою мишею не перевіряв |
| 5.4 | ✅ | `app.js:1152` смужка «клавіатура ✓ / —» |
| 6.1 | ✅ | `app.js:1336` — панель угорі й **згорнута за замовчуванням**, розгортається кнопкою «Стан ▾» |
| 6.2 | ✅ | Склад названий у «Результаті»: зв'язок, кадри, потік, фокус, пульт, пам'ять, швидкість, режим очікування |
| 7.1 | ✅ | Прохід боса з телефона, без комп'ютера — увесь сценарій, зауважень немає |
| 7.2 | ✅ | Прохід боса з комп'ютера — увесь сценарій |
| 7.3 | ✅ | Названо п'ять груп конкретних місць, а не «все добре» |
| 8.1 | ✅ із застереженням | Кадри не погіршали (протяг 4.3 проти 2.6–4.3; енкодер 13.3 проти 13.6). ⚠️ Медіана затримки 115 → 131 мс, тобто **гірше на 14 %**, і це записано, а не сховано; 90-й відсоток 920 → 229 мс, тобто провали скоротились учетверо. Обмін прийнятний |
| 8.2 | ✅ | `silence_resets` 0, `silence_timeouts` 0, найбільший проміжок вводу 266 мс з 1 426 |
| 8.3 | ✅ | Провалився (20 504 Б при межі 32 768), полікований мініфікацією до 62 512 Б у найгіршому з п'яти прогонів |

**Обмеження задачі — усі дотримані.** `git diff ded71aa..HEAD --
firmware/edgetx-patch/ upstream/` порожній: прошивка пульта не чіпалась.
Правки в мості — рівно віддача нового файлу (`ws_bridge.c`: два зовнішні
символи, обробник, шлях, звірене число шляхів). `docs/` команда не чіпала,
три виправлення там зробив асистент на її прохання.

**Вердикт: прийнято.**

Задача досягла того, заради чого починався проєкт: пультом із розбитим
екраном можна користуватися з телефона. Із двадцяти восьми критеріїв закриті
двадцять шість, один знято за відсутністю предмета, один лишається
відкритим.

⚠️ **Найцінніше в цій роботі — не панелі, а два випадки, коли сесія
зупинилась і перевірила мету замість того, щоб виконати написане.** Критерій
0 шукав зламане, якого не існувало; критерій 3.3 вимагав неможливого за
будовою. Обидва рази правильною поведінкою було довести це, а не зімітувати
виконання. Це вже шостий такий випадок у проєкті, і він підтверджує правило:
мета задачі — теж гіпотеза.

**Що піде в наступну задачу:**

1. **Перелік незручностей від боса** — розкладка, відступи, ширини,
   розгортання впритул до екрана, чорний фон, помаранчеві написи. Повністю
   записаний вище, у підрозділі «Відгук боса за критеріями 7.1–7.3».
2. **Введення тексту й чисел із фізичної клавіатури** — нова вимога, у цій
   задачі її не було.
3. **Запуск без браузерної обгортки** — опис застосунку для робочого столу
   плюс кнопка «на весь екран».
4. **Критерій 3.2** — скільки пунктів меню за секунду. Заміряти лише на
   пульті; чи варто витрачати на це окремий підхід до стенда — питання боса.
5. **Множник з'єднань** (`max_open_sockets` = 4 × буфер 32 КБ) — окрема
   задача із заміром пропускної здатності до і після, а не побічна правка.

**Дрібне зауваження про документацію.** Рішення цієї сесії в
`state/DECISIONS.md` внесла не команда, а асистент (коміт `1f4ae0d`). Знімок
стану, запис передачі зміни й чищення дозволів зроблені (`9a5fed7`). На
майбутнє: рішення, які переживуть задачу, вносить той, хто їх ухвалив.

---

### Проміжна нотатка асистента, 2026-08-02 — до вердикту

⚠️ Дописано **додаванням**, поки сесія біля стенда відкрита. Критеріїв вище
не змінював. Це три звірки за файлами, а не вердикт по критеріях; пункт 3
змінює те, що вважатиметься доказом, і тому названий окремо.

**1. Замір, що змінив напрямок роботи, лежить не там, де його шукатимуть.**

Розділ «Що не вийшло» досі каже **≈29 КБ** і чесно позначає це передбаченням.
Справжнє число — **20.5 КБ вільної купи, заміряно тричі на стенді** — існує
лише в коментарях двох **незакомічених** файлів (`webui/minify.mjs`, рядки
5–6; `firmware/esp32/main/CMakeLists.txt`, блок про мініфікацію).

Прохання до команди: перенести замір у «Результат» — умови досліду (холодне
завантаження, скільки паралельних сокетів, звідки взято число: смужка чи
розклад «хто з'їв»), три прогони й розкид. Коментар у коді зникає при першому
ж рефакторингу того коду; «Результат» читають наступна сесія й рецензія.

**2. ⚠️ Порядок важелів із рецензії 0021 обійдено, і мініфікація не закриває
названу небезпеку.**

Рецензія 0021 (рядки 382–386 і таблиця в 400) назвала першим важелем
**обмеження кількості одночасних сокетів**, а не зменшення того, що
множиться. У коді множник не змінився: `firmware/esp32/main/ws_bridge.c:821`
— `cfg.max_open_sockets = 4`, тобто рівно ті чотири паралельні завантаження,
що доводили купу до 2 148 Б.

Мініфікація 57.8 → 18.5 КБ зменшує **доданок**, і як самостійна робота вона
корисна — відкочувати нема чого. Але запас з'їдає **множення**, і стіна
повернеться на важчій сторінці або на іншому браузері.

Тому це не «доробити колись», а **вибір, який має бути названий уголос**:
або мініфікація доповнюється обмеженням сокетів (міст однокористувацький за
задумом — він уже витісняє другий клієнт через `lost_by.evicted`), або
порядок важелів свідомо переглядається із записаною підставою. Мовчазний
обхід — найгірший з трьох варіантів: через місяць ніхто не згадає, що вибір
узагалі робився.

⚠️ І окремо: замір 8.3 після мініфікації має бути холодним завантаженням
**у тих самих умовах, що дали 20.5 КБ**. Інакше порівнюються різні досліди.

**3. Прогін проти зібраної сторінки — третя умова на мініфікацію, поки не
виконана.**

Дві перші виконані, перевірив: версія esbuild закріплена точним числом
(`webui/package.json` — `0.25.10`, плюс `pnpm-lock.yaml`), `node_modules`
ігнорується (`.gitignore:7`).

Третя — тести. `webui/app_test.js` і `webui/panels_test.js` ганяються по
джерелу, а у флеш їде інший файл. Якщо мініфікатор щось зіпсує, зелені тести
цього не покажуть — це четвертий за проєкт випадок шаблону «перевірка, яка не
може провалитись», і попередні три коштували нам дорого. Обґрунтування
«безпечно за побудовою» у `minify.mjs` виглядає розсудливим, але воно
міркування, а не доказ.

Механізм уже є: `--dir` у `tools/webui_serve.py` піднімає стенд проти
зібраної теки. **Доказом на рецензії вважатиметься сценарій 7.x, пройдений
проти мініфікованої сторінки, а не проти `webui/`.**

---

### Відгук людини за критеріями 7.1–7.3, знятий 2026-08-02

Записано асистентом зі слів людини одразу після проходу на стенді.

**7.1, з телефона — пройдено повністю, зауважень немає.** Весь сценарій
(налаштування моделі, зміна значення джойстиком, вихід, перемикання моделі)
зроблено без комп'ютера.

**7.2, з комп'ютера — пройдено повністю.** Одне побажання, якого в задачі не
було й тому воно **не є невиконаним критерієм**: ⚠️ введення тексту й чисел
бажано робити з фізичної клавіатури.

**7.3 — перелік незручностей.** ⚠️ Знімки робились на комп'ютері в режимі
імітації телефона, а він **не показує** кнопок навігації й рядка пошуку
браузера. Тобто справжня площа під вміст на телефоні **менша**, ніж на
знімках, і всі зауваження про брак висоти на живому телефоні гостріші.

**1. Верхня панель показується завжди, а мала розгортатись.** В альбомній
орієнтації висоти й так мало, а тут панель зверху плюс адресний рядок
браузера знизу.

Питання людини: як змусити браузер сховати адресний рядок, або чи не краще
додати кнопку «на весь екран».

> **Відповідь асистента.** Наказати браузерові сховати адресний рядок
> сторінка не може — це навмисно, інакше сторінки видавали б себе за
> застосунки. Робочих шляхів два, і вони не виключають один одного:
>
> - **кнопка «на весь екран»** — працює на Android; на iPhone цього способу
>   немає взагалі, там кнопка просто нічого не зробить;
> - **опис застосунку** (невеликий файл поруч зі сторінкою), після якого
>   сторінку можна додати на робочий стіл телефона й запускати **без
>   браузерної обгортки взагалі**. Працює і на Android, і на iPhone, і
>   віддає найбільше площі.
>
> Раджу зробити обидва: опис коштує кількасот байтів у флеші, кнопка — двох
> десятків рядків.

**2. Альбомна орієнтація.**

- верхня панель **переїжджає вниз**;
- ліва панель: між `SYS` і наступною кнопкою — **подвійний відступ**;
- права панель: `MDL` помітно ширша за `SYS`. **Ширина обох бічних панелей
  однакова**, за зразком лівої;
- між `MDL` і джойстиком — **подвійний відступ**;
- кнопки джойстика **завширшки як `MDL`**, пропорції лишаються нинішні
  (квадрат і коло);
- ⚠️ **панель розгортається впритул до зображення екрана**, без порожнечі
  між ними; кнопки теж тягнуться ближче до екрана, а весь надлишок простору
  лишається ззовні. **Екран завжди по центру** — доки місця вистачає. Коли
  не вистачає, панель тісни́ть екран, і той може стояти не по центру, якщо
  друга панель згорнута. Так воно поводиться зараз, і це правильно;
- ⚠️ **окремої кнопки розгортання під нижньою панеллю не робимо** — вона
  з'їдає висоту. Замість неї: у кнопок розгортання бічних панелей
  **відібрати чверть висоти** і розмістити там кнопки розгортання нижньої.

**3. Книжкова орієнтація.**

- розгортання панелей — за тим самим правилом, що в альбомній: впритул до
  екрана, зі зсувом екрана за потреби; над екраном лишається вільне місце;
- **усі кнопки в одну колонку**, як у бічних панелях;
- **усі кнопки однакової ширини**, як у бічних панелях;
- відступи від `SYS` і від `MDL` до сусідніх — **подвійні**;
- замість верхньої панелі — **третя панель знизу посередині**. Вона займає
  весь вільний простір між бічними, а бічні мають ширину рівно таку, щоб
  умістити кнопки й відступи, і не більшу.

**4. Фон панелей — цілком чорний.**

**5. Кольори.** Основний колір написів та значків на кнопках — **насичений
помаранчевий, ніби неон**. Виняток — `SYS` і `MDL`: вони самі помаранчеві,
помірної яскравості, а текст на них **чорний**.

### Що з цього куди йде

- **7.1, 7.2, 7.3 закриті** — критерій 7.3 просив назвати незручні місця, і
  вони названі. Перелік вище **не є невиконаними критеріями 0022**.
- Увесь перелік із пунктів 1–5, плюс введення тексту з фізичної клавіатури,
  — матеріал **наступної задачі**. Оформлює асистент після закриття сесії.
- **3.2 лишається незакритим:** скільки пунктів меню за секунду дає
  прискорення. Людину про це спитали, відповіді поки немає.
