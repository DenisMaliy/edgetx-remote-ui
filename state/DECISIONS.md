# Журнал рішень

Короткі рядки. Великі рішення оформлюються як ADR у `docs/adr/` і додаються
сюди посиланням.

| Дата | Рішення | Причина | Де докладно |
|---|---|---|---|
| 2026-07-25 | Транспорт — AUX1, не USB CDC | у AUX1 є DMA на передачу; USB-C лишається вільним для накопичувача й VCP | `docs/06-hardware.md` |
| 2026-07-25 | Захоплення екрана — порівняння двох кадрових буферів | не залежить від LVGL API; переживе перехід LVGL 8 → 9 | `docs/02-architecture.md` |
| 2026-07-25 | Стиснення — RLE16, не LZ4 | інтерфейс однотонний, RLE дає 5–20× майже безкоштовно; LZ4-кодувальника в EdgeTX немає | `docs/02-architecture.md` |
| 2026-07-25 | Ввід — підмішування у драйвери, не в GUI | безкоштовно отримуємо довгі натискання, автоповтор, прискорення енкодера | `docs/05-hooks.md` |
| 2026-07-25 | Два коміти: нові файли / гачки | мінімізує конфлікти при rebase на нову версію EdgeTX | `CLAUDE.md` |
| 2026-07-25 | Міст ESP32 — PlatformIO, Arduino core 3.x + IDF для UART | швидкий старт без втрати контролю над гарячим шляхом; чистий IDF лишається запасним | [ADR-0001](../docs/adr/0001-esp32-framework.md) |
| 2026-07-25 | Дві сесії (асистент / команда), міст через `tasks/` | асистент отримує доступ до репозиторію і рецензує за доказами, а не за звітом; історія рішень лишається в git | [ADR-0002](../docs/adr/0002-assistant-team-bridge.md) |
| 2026-07-25 | Розробка на ESP32 DevKit v1, швидкість 921600 | два ядра знімають ризик таймінгів; 921600 вкладається у стелю наявного CP2102 і достатній за трафіком | [ADR-0003](../docs/adr/0003-bridge-board-and-baudrate.md) |
| 2026-07-25 | Ризик завад Wi-Fi ↔ RF-модуль знято | використовується зовнішній модуль через JR-відсік; зовнішня антена більше не обов'язкова | `docs/06-hardware.md` |
| 2026-07-25 | Міст обов'язково потоковий, без накопичення кадру | 255 КБ не влазять у пам'ять жодної з наявних плат | [ADR-0003](../docs/adr/0003-bridge-board-and-baudrate.md) |
| 2026-07-25 | Міст — чистий ESP-IDF, без Arduino | `esp_http_server` уміє WebSocket сам; ESPAsyncWebServer/AsyncTCP дають випадкові падіння; менше пам'яті для C3 | [ADR-0004](../docs/adr/0004-bridge-framework-espidf.md), замінює ADR-0001 |
| 2026-07-25 | Базова гілка EdgeTX — 2.12, не main | на пульті 2.12; вміст SD-картки прив'язаний до версії, збірка від main викличе попередження | `CLAUDE.md`, `tools/setup-upstream.sh` |
| 2026-07-25 | Крок роз'єму AUX1 — 2.54 мм | виміряно; підходять звичайні перемички Dupont | `docs/09-bom.md` |
| 2026-07-25 | На пульті виявлено EdgeTX 2.8.0 (f6d140eb), завантажувач 2.7.1 — не 2.12, як вважалося | зчитано з пам'яті пульта через DFU і звірено з тим, що віддав завантажувач | `tasks/0001-blind-flash-proof.md` |
| 2026-07-25 | Від плану не відступаємо: ціль лишається 2.12, пульт оновлюємо з 2.8.0 | 2.8 не підтримується, патч на мертвій гілці дорожчий за одне оновлення | `tasks/0001-blind-flash-proof.md` |
| 2026-07-25 | Заливка прошивки — тільки через DFU (`dfu-util`), не через меню завантажувача з картки | меню вимагає зрячої навігації; DFU працює на вимкненому пульті й не залежить від екрана | `tasks/0001-blind-flash-proof.md` |
| 2026-07-25 | Цільова версія — EdgeTX **v2.12.2**, офіційна англійська збірка; пакет картки `c480x272.zip` (edgetx-sdcard v2.12.1) | окремі мовні збірки після 2.10.6 не випускаються, а мова інтерфейсу нам байдужа — екрана нема | `tasks/0002-upgrade-to-212.md` |
| 2026-07-25 | Версію прошивки доводимо через `strings` по зліпку флеша, а не тільки через `radio.yml` | доказ не залежить від того, чи пульт дійшов до коректного вимкнення; метод перевірено на зліпку 2.8 | `docs/07-recovery.md` |
| 2026-07-25 | Будь-яка правка `RADIO/radio.yml` з ПК — разом із `manuallyEdited: 1` | інакше пульт визнає контрольну суму хибною, відкотить правку і покаже невидиме нам вікно | `docs/06-hardware.md` |
| 2026-07-25 | Пульт оновлено: EdgeTX **2.12.2 (3673c518)**, картка — пакет 2.12.1. Новий еталон зліпка `7b6d1ce1e126f13732f080d228917ba7` | ціль плану досягнута, патч тепер пишеться проти версії, що справді стоїть у пульті | `tasks/0002-upgrade-to-212.md`, `docs/07-recovery.md` |
| 2026-07-25 | Порядок оновлення: спершу картка, потім прошивка | картку можна писати лише з увімкненого пульта; так перший запуск 2.12 бачить правильну картку і мігрує налаштування один раз | `tasks/0002-upgrade-to-212.md` |
| 2026-07-25 | Калібрування стіків при оновленні переносити руками **не потрібно** | у 2.12 читач `calib` має таблицю старих імен для кожної цілі; перевірено — усі числа збіглися, змінилися лише назви входів | `radio/util/hw_defs/legacy_names.py`, `docs/07-recovery.md` |
| 2026-07-25 | `upstream/edgetx` і `tools/setup-upstream.sh` закріплено на **тезі** `v2.12.2`, не на гілці `2.12` | голова гілки їде далі за реліз; збіг версії з пультом має бути гарантією, а не випадковістю | `tools/setup-upstream.sh` |
| 2026-07-25 | Перевірки версії SD-картки в EdgeTX 2.12 більше немає | рядок `WRONG_SDCARDVERSION` лишився тільки в перекладах, у коді його ніхто не викликає — стара мітка картки невидимих вікон не викличе | `radio/src/translations/` (упевнились пошуком по 2.12.2) |
| 2026-07-25 | Режим USB-джойстика придатний як **незалежний канал читання стану пульта** з ПК | виміряно на 2.12.2: 8 осей, 24 кнопки, діапазон 0…2048; трипозиційний перемикач дає рівно три рівні 0/1024/2048 | `tools/axprobe.py`, `tools/jsinfo.py` |
| 2026-07-25 | Джойстик віддає **канали після мікшера**, не сирі положення органів керування | щоб орган з'явився на ПК, у моделі має бути мікшер на канал; типова модель має лише 4 — звідси наші «4 рухомі осі» на старті | `radio/src/usb_joystick.cpp` (2.12.2) |
| 2026-07-25 | Налаштування USB-джойстика доступні **з ПК**, правкою `MODELS/*.yml` і `RADIO/radio.yml` | екран для цього не потрібен; з'явилося у 2.9, у 2.8 не було зовсім — ще одна причина, чому оновлення було правильним | `yaml_datastructs_x10.cpp`, `usb_joystick.h` |
| 2026-07-26 | Симулятор збирається **без Qt**: ціль `simu` (SDL2 + imgui), прапорець `-DDISABLE_COMPANION=ON` | з наявним `qt6-base` CMake береться будувати Companion і падає на відсутньому `Qt6SerialPort`; Qt потрібен лише плагіну, якого ми не використовуємо | `docs/11-simulator.md`, `tasks/0003-simulator-build.md` |
| 2026-07-26 | Симулятор запускається **тільки на копії** картки (`build/sim-card`) | він у неї пише: `radio.yml`, `labels.yml`, поточна модель, компіляція Lua у `.luac` | `docs/11-simulator.md` |
| 2026-07-26 | `EDGETX_VERSION_TAG=v2.12.2` дає збіг рядка версії з пультом, але **прибирає хеш коміту** з файлу | наслідок: збірка з нашим патчем зовні не відрізнятиметься від чистої релізної — ознака нашої збірки обирається в задачі 0004 | `tasks/0003-simulator-build.md`, розділ «Рецензія» |
| 2026-07-26 | Ознака нашої збірки — `EDGETX_VERSION_SUFFIX=remoteui` замість `EDGETX_VERSION_TAG=v2.12.2`, коду не пишемо | без `VERSION_TAG` `stamp.cpp` лишає в рядку версії й `GIT_STR` (8-символьний хеш коміту), а це вже видно і на екрані About, і через `strings` по прошивці — той самий спосіб, яким уже звіряли версію в задачі 0002 | `tasks/0004-protocol-skeleton.md`, розділ «Результат» |
| 2026-07-26 | **Уточнення до рядка вище про `-remoteui`:** ознакою нашої збірки є **сам суфікс**, а не `GIT_STR`. Рішення лишається чинним, змінюється тільки підстава | хеш коміту не може розрізнити «з патчем» і «без патча»: наш код лежить поза деревом EdgeTX і `HEAD` upstream не змінює, тож обидві збірки дадуть однаковий `GIT_STR`. А відсутність хеша, яку спостерігали в задачі 0003, — властивість симулятора: `stamp.cpp:80` ховає `.fwversiondata` за `#if defined(STM32) && !defined(SIMU)`; у справжній прошивці хеш є навіть із `VERSION_TAG` (у задачі 0002 зі зліпка пульта вичитано `edgetx-tx16s-v2.12.2 (3673c518)`) | `tasks/0004-protocol-skeleton.md`, розділ «Рецензія» |
| 2026-07-26 | ⚠️ **Рішення від 25.07 «захоплення екрана порівнянням двох кадрових буферів» на TX16S не працює як записане. Спосіб захоплення — відкрите питання, рішення за асистентом перед кроком 1.4** | прочитано код 2.12.2: на TX16S визначено `LCD_VERTICAL_INVERT` → `direct_mode = 0`; справжній кадр лежить не в `LCD_FIRST/SECOND_FRAME_BUFFER` (це буфери малювання LVGL), а в парі драйвера, повернутій на 180°; і драйвер **сам тримає ці два буфери однаковими** — після кожного кадру копіює оновлені області з переднього в задній, тож різниця завжди нульова | `tasks/0004-protocol-skeleton.md`, розділ «Знахідка щодо кроку 1.4» |
| 2026-07-26 | **Захоплення екрана — гачок `remoteUiOnFlush` у `flushLcd()`** (`gui/colorlcd/lcd.cpp`), на самому початку функції. **Замінює рішення від 25.07 про порівняння буферів** | порівняння неможливе: буфери LVGL не містять кадру, а пару буферів драйвера він сам тримає однаковою. Гачок дає одночасно область (`rect_t` — тип EdgeTX, не LVGL), пікселі й ознаку кінця кадру; залежність від LVGL звужується до одного виклику `lv_disp_flush_is_last()`; симулятор і залізо йдуть тим самим кодом, у тій самій орієнтації | `docs/02-architecture.md`, `docs/05-hooks.md` (пункт 5) |
| 2026-07-26 | Точка гачка — `gui/colorlcd/lcd.cpp`, не `targets/horus/lcd_driver.cpp` | файл спільний для всіх кольорових цілей і для симулятора: один гачок замість дублювання в кожен драйвер | `docs/05-hooks.md` |
| 2026-07-26 | Картинка віддається **неповернутою** (логічна орієнтація) | поворот на 180° робить драйвер TX16S нижче за нашу точку; у симуляторі повороту немає взагалі — беручи з гачка, отримуємо однакове в обох випадках | `docs/02-architecture.md` |
| 2026-07-26 | Сітка плиток лишається, але як одиниця **стиснення й передачі**, а не спосіб пошуку змін | шукати зміни більше не треба — їх приносить гачок | `docs/02-architecture.md` |
| 2026-07-26 | У гачку — тільки `memcpy` і запис у чергу; стиснення й передача поза ним | гачок виконується в `menusTask` (найнижчий пріоритет, витісняється мікшером); DMA2D в EdgeTX не захищений мутексом і використовується з припущенням «усе з menusTask» | `docs/02-architecture.md` |
| 2026-07-26 | Швидкість зведено до одного числа: **921600** у плані, точках дотику й схемі архітектури | три місця стверджували 2 Мбод як факт, хоча ADR-0003 давно вирішив інакше; 2 Мбод лишається запасом під `REMOTE_UI_BAUDRATE` | ADR-0003 |
| 2026-07-26 | Специфіку протоколу уточнено: точка ресинхронізації, відсутність тайм-аутів на шарі кадрування, порядок байтів `LEN` у CRC | питання підняла команда при реалізації 0004; реалізація була правильна, бракувало запису в документі | `docs/03-protocol.md` |
| 2026-07-26 | **Механізм вбудовування — symlink на наш каталог + патч-файл на гачки.** Дві команди: `tools/patch-apply.sh` і `tools/patch-revert.sh`, плюс `tools/patch-update.sh` для зняття правлених гачків назад у патч | розглядалися три варіанти. **Копіювання скриптом** відкинуто: у дереві EdgeTX з'являється другий примірник коду, і рано чи пізно правлять саме його, а потім втрачають. **Увесь наш код у патчі** відкинуто: це означає редагувати патч замість коду, без підсвітки, без збірки тестів. **Symlink** лишає один примірник — той, що в git нашого репозиторію, — і бачиться збіркою одразу. Патч потрібен лише для 14 рядків у двох чужих файлах, де symlink нічим не зарадить. Перевірено двічі: застосував → зібрав → скасував (дерево чисте) → застосував → зібрав | `firmware/edgetx-patch/README.md`, `tasks/0005-screen-capture.md` |
| 2026-07-26 | Каталог `radio/src/remote_ui` вписано в `.git/info/exclude` дерева EdgeTX | інакше symlink висів би в `git status` як невідстежуваний і ховав справжні зміни в чужих файлах; `.git/info/exclude` — місцевий і в чужий репозиторій не потрапляє. `patch-revert.sh` прибирає і його | `tools/patch-apply.sh` |
| 2026-07-26 | Черга плиток — **бітова карта над тіньовим кадром**, без м'ютекса. Гачок: пікселі, потім біт (release); транспорт: біт, потім пікселі (acquire) | гачок працює в `menusTask`, яку витісняє мікшер, і чекати на блокування не має права. Карта фіксованого розміру не переповнюється за побудовою, тому зміна не губиться: найгірше — плитка піде зшита з двох кадрів, а наступним проходом цілісна. Межа гарантії: **забрану** плитку, яку не вдалось відправити, транспорт зобов'язаний повернути (`captureReturnTile`) | `firmware/edgetx-patch/remote_ui/capture.h` |
| 2026-07-26 | Розмір плитки — **32×32** | сира плитка має вміщатись у `PAYLOAD` разом із заголовком, бо на шумі RLE не виграє й плитка йде сирою: 32·32·2 + 9 = 2057 ≤ 4096, а 64×64 дало б 8201 — і різати її в момент відправлення нема куди | `firmware/edgetx-patch/remote_ui/geometry.h` |
| 2026-07-26 | Тіньовий кадр (261 120 Б) розміщується в **SDRAM** (`__SDRAM`) | у внутрішню ОЗП STM32F429 (192 КБ під `.bss`) не влізе — прошивка просто не злінкувалась би. EdgeTX кладе туди ж власні кадрові буфери; на симуляторі макрос порожній. Наслідок: на кольоровій цілі **без** SDRAM підхід доведеться переглядати | `firmware/edgetx-patch/remote_ui/capture.cpp` |
| 2026-07-26 | Тайм-аут скиду декодувальника — **100 мс тиші в каналі** (закриває відкрите питання з `PROGRESS.md`) | найбільший кадр (4103 Б) на 921600 бод іде ~45 мс, тобто вдвічі менше; значення описує протокол, а не трубу, тому спільне для TCP у симуляторі й для майбутнього UART | `firmware/edgetx-patch/remote_ui/transport_simu.cpp` |
| 2026-07-26 | Транспорт у симуляторі — TCP на **127.0.0.1:7616**, порт перекривається змінною `REMOTE_UI_PORT`; потік піднімається статичним конструктором | тільки петля, бо це інструмент розробки, а не мережева служба. Потік стартує сам, щоб не додавати ще один гачок у чужий код: перелік дозволених місць закритий | `firmware/edgetx-patch/remote_ui/transport_simu.cpp` |
| 2026-07-26 | `FRAME_END` не чекає, доки черга плиток спорожніє | клієнт до `FRAME_END` малює в позаекранний буфер; прив'язка «кінець кадру = черга порожня» давала б застиглу картинку саме тоді, коли людина крутить меню й брудні плитки з'являються швидше, ніж ідуть | `docs/03-protocol.md`, знахідка рецензії |
| 2026-07-26 | ⚠️ **Гачок на початку `flushLcd()` правильний для TX16S, але на цілях із прямим режимом LVGL (T18, X12S, V16) він отримає повний екран на кожному флеші, кілька разів на кадр.** Рішення про місце гачка — за асистентом, до кроку 1.7 | у прямому режимі LVGL виставляє `buf_area` в увесь екран (`lv_refr.c`), а ранній вихід у `flushLcd` лишає тільки останній флеш. Тобто обґрунтування в `docs/05-hooks.md` («після раннього виходу гачок мовчав би на решті цілей») хибне: він там не мовчав би, а працював би дешевше. Для TX16S питання не стоїть — цей блок для нього навіть не компілюється | `tasks/0005-screen-capture.md`, розділ «Результат» |
| 2026-07-26 | Гачок захоплення приймає **цілі числа й вказівник**, а не типи LVGL; розбір `lv_area_t`/`lv_disp_drv_t` — на місці виклику | уся залежність від LVGL лишається в 14 рядках патчу, які й так переглядатимуться при кожному оновленні EdgeTX; наш заголовок чистий | `docs/05-hooks.md`, пункт 5 |
| 2026-07-26 | Гачок отримає **додатковий параметр — крок рядка**; реалізація в задачі кроку 1.7 | у прямому режимі (T18, V16, X12S) пікселі лежать у повноекранному буфері з кроком `LCD_W`, а не щільно; чинний код прочитав би там сміття. Специфіка лишається на місці виклику, наш код працює з обома розкладками | `docs/05-hooks.md`, пункт 5 |
| 2026-07-26 | Оцінку трафіку в специфікації виправлено за замірами: дія в меню — **10–16 КБ**, не 3–8 КБ | заміряно на живому симуляторі в задачі 0005; на 921600 бод це 6–9 дій за секунду | `docs/03-protocol.md`, `tasks/0005-screen-capture.md` |
| 2026-07-26 | Вбудовування коду — **symlink на наш каталог + патч на гачки**, три скрипти (`apply`/`revert`/`update`) | копіювання дало б другий примірник коду, який розійдеться; «увесь код у патчі» означало б редагувати патч замість коду. Перевірено: після `revert` дерево EdgeTX чисте, цикл пройдено двічі зі збіркою | `firmware/edgetx-patch/README.md`, `tasks/0005-screen-capture.md` |
| 2026-07-26 | Розбір протоколу на боці ПК — **один спільний модуль** `tools/remote_ui_proto.py` для тестового клієнта і для `capture_check.py` | два примірники декодувальника в `tools/` розійшлися б, а звірка з еталонними векторами перевіряла б лише один із них | `tools/remote_ui_proto.py`, `tasks/0006-test-client.md` |
| 2026-07-26 | Порядок вітання клієнта — **`PING` → `HELLO` → `REFRESH`**, а не `REFRESH` одразу | до `HELLO` клієнт не знає розміру екрана й викидає всі плитки. По TCP непомітно (пульт вітається сам), на послідовному порту вікно лишалось порожнім при живому з'єднанні | `tools/test_client.py`, `tasks/0006-test-client.md` |
| 2026-07-26 | ⚠️ **`REFRESH` на нерухомому екрані не породжує `FRAME_END`**: прошивка шле його лише коли зріс лічильник кадрів захоплення. Рішення, як правити, — за асистентом, до кроку 2.4 | по TCP сховано примусовим `FRAME_END` при підключенні; на UART поняття «підключився» немає, тому клієнт, що чесно чекає `FRAME_END`, не покаже нічого, доки пульт сам щось не перемалює. У клієнті поки запобіжник: показ через 300 мс тиші з окремим лічильником | `tasks/0006-test-client.md`, `firmware/edgetx-patch/remote_ui/transport_simu.cpp` |
| 2026-07-26 | Вікно клієнта — **tkinter + PPM у `PhotoImage`**, без сторонніх бібліотек | 3.5 мс на кадр 480×272, тобто вікно не є вузьким місцем; `pyserial` лишається необов'язковим і потрібен лише для `--serial` | `tools/test_client.py` |
| 2026-07-26 | **`REFRESH` завжди завершується `FRAME_END`**, навіть коли екран не змінювався. Виправляти в прошивці, не в клієнті | найтиповіший випадок — підключення до пульта, що стоїть на нерухомому екрані; при прив'язці `FRAME_END` до лічильника кадрів клієнт отримав би всі плитки й не показав нічого. Знайдено в задачі 0006 на послідовному транспорті | `docs/03-protocol.md`, задача 0006 |
| 2026-07-26 | Запобіжник у клієнті (показ через 300 мс тиші) лишається як **діагностика**, а не як розв'язок | лічильник «без FRAME_END» має лишатись видимим, щоб вада прошивки не заметалась під килим після виправлення | `tools/test_client.py` |
| 2026-07-26 | Пікова потреба в каналі — **230 КіБ/с** при швидкій прокрутці, 162 КіБ/с на Lua-скрипті, тоді як 921600 бод дають ~90 КіБ/с | заміряно в задачі 0006. Вузьким місцем стає дріт, а не пульт і не клієнт | задача 0006, `docs/02-architecture.md` |
| 2026-07-26 | Відповідь на брак каналу — **плавна деградація, не збільшення швидкості**: плитка лишається брудною і йде наступним кадром | кадри просто рідшають, але кожен показаний кадр цілісний; рвати картинку заради частоти не можна. 2 Мбод перевіряємо на кроці 2.5 на справжньому мості, де CP2102 у ланцюзі вже немає | ADR-0003, задача 0006 |
| 2026-07-26 | Емульований ввід **додається** до фізичного (`\|=`), ніколи не заміщує | пульт не має права стати залежним від клієнта: фізичні клавіші працюють завжди, хоч би що робив застосунок і чи він живий. Це межа проєкту, а не оптимізація | `docs/05-hooks.md` пункт 6, задача 0007 |
| 2026-07-26 | Ввід відпускає **читач** (задачі EdgeTX) за тишею в каналі, а не писар (потік транспорту). Поріг — **1000 мс** | мертвий або завислий у `send()` потік транспорту не лишить клавішу натиснутою. Гарантія сильніша, ніж «транспорт помітив обрив»: на UART поняття «з'єднання закрито» немає взагалі | `firmware/edgetx-patch/remote_ui/input.h`, задача 0007 |
| 2026-07-26 | ⚠️ **Періодичність `PING` — вимога протоколу, а не звичка клієнта: 250 мс, поки щось утримується.** Було «раз на 2 с» — небезпечно | поріг відпускання 1000 мс = чотири пропущені періоди. Клієнт із періодом 2 с отримає розтиснуту під пальцем клавішу. Число читатимуть автори моста ESP32 і браузерного клієнта, тому воно виправлене в специфікації одразу, не чекаючи задачі | `docs/03-protocol.md`, задача 0007 |
| 2026-07-26 | **Клієнт періодично повторює повний стан вводу** (маски клавіш і тримерів, стан дотику) — рівень, а не перехід. Формат проєктує `protocol-architect` | тайм-аут ловить тишу всього каналу, але не втрачений поодинокий пакет «відпущено» при живому `PING`; побита `LEN` з'їдає до 4096 байтів разом із валідними кадрами, тобто втрата — не гіпотеза. Повтор стану самолікує її за один період | `docs/03-protocol.md`, задача 0008 |
| 2026-07-26 | Енкодер у повтор стану **не входить** і лишається накопичувальною позицією, а не приростом | втрата клацання коштує клацання, а не залипання; заміщення накопичувача рівнем дало б фантомний оберт назад — саме те, що обійшла задача 0007 | `firmware/edgetx-patch/remote_ui/input.h`, задача 0008 |
| 2026-07-26 | Гачок 7 **розширено на один рядок**: дозволено оновлювати позначку часу клацання енкодера часом приходу пакета `ENC` | без цього `dt` завжди нульове, EdgeTX вважає обертання нескінченно швидким і прискорення застигає на максимумі: з другого клацання підряд крок стрибає на +25, «покрутити на кілька одиниць» неможливо | `docs/05-hooks.md` пункт 7, задача 0008 |
| 2026-07-26 | **`INPUT_STATE` шлеться раз на 250 мс безумовно**, а не «поки щось утримується»; `PING` повертається до 2 с | ⚠️ **виправляє помилку асистента, зроблену того ж дня.** Двошвидкісна таблиця не працює за побудовою: лікувальний пакет надходить від клієнта, який вважає, що не утримує нічого — «відпущено» він уже надіслав, воно й загубилось. Тобто швидкий режим вмикався б рівно тоді, коли він не потрібен. За 2 с очікування `keys.cpp` устигає довге натискання (320 мс) і автоповтор (480 мс). Побічно дешевше: 80 Б/с у порожньому напрямку замість 4 `HELLO` на секунду (~1 КБ/с) у завантаженому | `docs/03-protocol.md`, `protocol-architect` у задачі 0008 |
| 2026-07-26 | ⚠️ **Тайм-аут відпускання відсувають лише пакети, що несуть ввід** (`KEY`, `ENC`, `TOUCH`, `TRIM`, `INPUT_STATE`). `PING`, `REFRESH` і невідомі типи — ні | на етапі 2 між телефоном і пультом стоїть міст ESP32, і він власний посередник: цілком може слати `PING` далі після того, як телефон від'єднався. При правилі «будь-який пакет» клавіша, утримувана в мить розриву Wi-Fi, лишилася б натиснутою назавжди, і людина цього не побачила б. Ціна — новий клієнт зі старою прошивкою не втримає клавішу; це видно одразу й нікому не шкодить | `docs/03-protocol.md`, задача 0008 |
| 2026-07-26 | Формат `0x87 INPUT_STATE`: 13 байтів — маска клавіш (4), маска напрямків тримерів (4), прапорці дотику (1), x і y (по 2). Клавіші й тримери присвоєнням, дотик **тільки переходом**, засувку пакет не чіпає | присвоєння повз чергу переходів стерло б непрочитаний `DOWN` і лишило б LVGL натиснутим назавжди; засувка існує саме тому, що клавіші читаються раз на 10 мс і короткий тик інакше пропав би | `docs/03-protocol.md`, `protocol-architect` у задачі 0008 |
| 2026-07-26 | Міст ESP32, втративши телефон, зобов'язаний **негайно надіслати обнулений `INPUT_STATE`** | тайм-аут — остання перешкода, а не спосіб роботи; розрив Wi-Fi має відпускати ввід за мілісекунди, а не за секунду | `docs/03-protocol.md`, крок 2.6 |
| 2026-07-26 | Транспорт на залізі крутить **власна задача FreeRTOS**, пріоритет 2 (`MENUS_TASK_PRIO+1`), період 2 мс, стек 2 КБ у CCM; створюється лише при переведенні порту в наш режим | мікшер (4) і звук (3) витісняють її **завжди**, тобто радіоканалу транспорт зачепити не може за побудовою, а не за домовленістю. Малювання (1) витісняє вона, тож приймання вводу не стає заручником перемальовки. Службову задачу таймерів не взято: у ній `per10ms()` разом із нашим гачком клавіш. ⚠️ Щабель 2 не вільний — затримка службової задачі не зникає, а обмежується тактом; заміряти на кроці 2.5 | `firmware/edgetx-patch/remote_ui/transport_uart.cpp`, задача 0009 |
| 2026-07-26 | Режим `REMOTE_UI` доступний **тільки на AUX1**, VCP виключено | у драйвері USB CDC `sendBuffer` і `getByte` нульові, а `usbSerialPutc()` при переповненні мовчки затирає кільце — режим виглядав би доступним і видавав зіпсовані кадри. USB CDC як другий транспорт (етап 5.1) отримає власну прив'язку й тоді повернеться в умову | `docs/05-hooks.md` пункт 4, задача 0009 |
| 2026-07-26 | Перевірка порту **продубльована** в гачку 4 і в `portUsable()` транспорту — свідомо | `isSerialModeAvailable()` фільтрує лише меню, а `initSerialPorts()` бере режим прямо з `radio.yml` без перевірок. У проєкті, де `radio.yml` правлять наосліп, рядок `mode: REMOTE_UI` під `AUX2` доїхав би до драйвера без `txDMA` і клієнт бачив би суцільні помилки CRC. Обидва місця міняються разом — попередження стоїть в обох | `docs/05-hooks.md` пункти 3 і 4, задача 0009 |
| 2026-07-26 | Три виправлення в переліку гачків **за кодом**: одна YAML-таблиця замість двох; обов'язковий `params.direction = ETX_Dir_TX_RX`; AUX1 без VCP | перелік досі писався за орієнтирами й уперше звірявся з дійсністю там, де його застосовували. Найдорожче — напрямок: без нього пульт говорив би, але не чув, і це коштувало б циклу «прошити — не працює — думати» на пульті без екрана | `docs/05-hooks.md`, задача 0009 |
| 2026-07-26 | ⚠️ **Над послідовним портом першим говорить клієнт; пульт мовчить у порожній канал.** Порядок вітання `PING → HELLO → REFRESH` | у послідовного порту немає події «клієнт підключився»: порт відкритий, доки пульт увімкнено, і на іншому кінці може не бути навіть дроту. Сипати туди кадрами пульт не має права. Доти це було наслідком коду, а не записаним правилом, а читатиме розділ автор моста | `docs/03-protocol.md`, задача 0009 |
| 2026-07-26 | ⚠️ Правильний блок для правки `radio.yml` наосліп — `serialPort: AUX1: {mode, power}`. Ключа `serialPortPower`, що стояв у документі, **в EdgeTX не існує** | стара інструкція мовчки не спрацювала б, і людина без екрана не відрізнила б «не налаштувалось» від «прошивка мертва». Звірено зі `struct_serialConfig` і перевірено на симуляторі туди й назад | `docs/06-hardware.md`, задача 0009 |
| 2026-07-26 | **Після виправлення блокерів рецензія запускається ще раз** — правило процесу | у задачі 0009 перше виправлення закрило діру й відчинило гіршу: перевірка порту опинилась після опускання воріт, і одруківка в `radio.yml` тихо вбивала робочий AUX1. Спіймала це лише друга рецензія на вже виправленому коді. Виправлення — це новий код, якого ніхто не читав | задача 0009 |
| 2026-07-26 | ✅ **Цілі MVP №1 і №2 досягнуто в дротовому варіанті.** Пульт із розбитим екраном показує свій екран на комп'ютері й слухається вводу | задача 0010: рукостискання з першої спроби, `PING → HELLO` за 10 мс, нуль помилок CRC за сеанс. Екран «Про EdgeTX», знятий через наш же транспорт, показує `pre-2.12.2-selfbuild (3673c518)` — ланцюг замкнувся | `tasks/0010-first-flash.md`, `docs/img/radio/` |
| 2026-07-26 | ⚠️ **Мікшер циклів не пропускає: `Tmix max` 0.46 мс із періоду 4 мс, і під навантаженням у 88.5% каналу число не зрушило** | головна перевірка безпеки з ТЗ, зроблена заміром на живому пульті, а не міркуванням про пріоритети задач. Спосіб: скинути лічильник, наситити канал, перечитати, **і окремо переконатися, що скидання спрацювало** | задача 0010, `docs/img/radio/03-debug-mixer-under-load.png` |
| 2026-07-26 | Причина підлагування — **насичений дріт**, не процесор і не клієнт | 95 мс на клацання енкодера проти 1149 мс на повний екран, різниця рівно в байтах; розкид між прогонами ±0.4%, а процес, обмежений процесором, так не поводиться. Підтверджує розрахунок задачі 0006 (230 КіБ/с потреби проти ~90 наявних). **Наслідок: оптимізувати захоплення чи стиснення зараз — витрачена робота** | задача 0010 |
| 2026-07-26 | ⚠️ **У процедурах, які пишуть у флеш, відносних шляхів не існує** | у задачі 0010 робочий каталог зсунувся після `cd`, і команда журналу зібралася з неіснуючих шляхів. Впало на `realpath` — за один хід до `dfu-util -D`. `cd` — це стан, а стан у незворотних операціях є вадою, а не зручністю | задача 0010, розділ «Три помилки» |
| 2026-07-26 | ELRS-конфігуратор на цьому залізі **неможливий** — внутрішній модуль TX16S це MULTI, ELRS серед його протоколів немає за побудовою. Замінено аналізатором спектра | заміна вийшла сильнішою за початковий критерій: екран, що бруднить плитки сам і безперервно, на 90.4% каналу — найважчий випадок для транспорту, саме той, заради якого в 0009 з'явилася третя причина закриття кадру. Повний ELRS потребує зовнішнього модуля в JR-відсіку | задача 0010 |
| 2026-07-26 | ⚠️ **«Редагувати картку тільки на вимкненому пульті» — хибна порада, знята з обох документів.** Правити `radio.yml` **прямо в режимі накопичувача** — правильно | пульт записує налаштування **при вході** в режим (`main.cpp:175` → `edgeTxClose(false)` → `storageCheck(true)`) і перечитує **при виході** (`main.cpp:195` → `edgeTxResume()` → `storageReadAll()`). Правка потрапляє точно між ними, затерти її нічим. Хибна порада дарма ускладнювала кожну правку наосліп. Знайдено командою, перевірено за кодом і на живому пульті в задачі 0010 | `docs/07-recovery.md`, `docs/06-hardware.md` |
| 2026-07-26 | ⚠️ **Еталон відкату — `sd-backups/upgrade-2.12.2/dump-after-2.12.2.bin`** (md5 `7b6d1ce1…`), а **не** `firmware-original/` | у `firmware-original/` лежить **2.8.0** із завантажувачем 2.7.1 (усі чотири файли однакові, md5 `bab1d2a3…`). Пульт зараз на 2.12, і вміст картки прив'язаний до версії: відкат на 2.8.0 зажадав би повернути ще й картку — на пульті без екрана, у момент, коли щось уже пішло не так. Помилка асистента в задачі 0010, знайдена командою читанням вмісту, а не назв | `tasks/0010-first-flash.md`, крок 0 |
| 2026-07-26 | Крок 0 перевіряється **зчитуванням пам'яті пульта через DFU і побайтовою звіркою з еталоном**, а не переліком файлів | доводить не «файл існує», а «`dfu-util` працює зараз, і те, що в пульті, дорівнює тому, що ми збираємось повертати». Читання нічого не змінює. Спосіб запропонувала команда | `tasks/0010-first-flash.md`, крок 0 |
| 2026-07-26 | Наша `firmware.bin` містить **і завантажувач** — заливка замінює обидві частини | перевірено: у перших 128 КБ лежить `BOOT` і `edgetx-tx16s-pre-2.12.2-selfbuild`. Зараз у пульті завантажувач 2.7.1 — оновлення з картки його не чіпало. Для відкату наслідків немає: еталонний зліпок накриває весь флеш | `tasks/0010-first-flash.md` |
| 2026-07-26 | Уточнення до рішення про ознаку нашої збірки: версія читається `edgetx-tx16s-pre-2.12.2-selfbuild`, а `remoteui` лежить у двійковому файлі **окремим рядком**, не у версії | мета рішення досягнута — від стокової збірки відрізнити можна (`pre-2.12.2-selfbuild` ≠ `v2.12.2`), але попереднє формулювання було неточне | задача 0010 |
| 2026-07-26 | **Копію `sd-backups/` поза диском не робимо** — рішення людини, тему закрито | асистент піднімав це п'ять разів і перебільшував ризик. Пульт стоїть на **стоковому публічному релізі** 2.12.2, який завантажується наново байт у байт, а наша збірка відтворюється з репозиторію — тобто прошивку відкату зберігати не треба взагалі. DFU лежить у ПЗП процесора, зіпсувати його неможливо. Копія картки разом із калібруванням уже є в `firstBackup/`. Копія на іншому диску рятувала б лише від смерті диска — події, ніяк не пов'язаної з тим, що ми робимо | `tasks/0010-first-flash.md`, крок 0 |
| 2026-07-26 | Порядок першого прошивання — **спершу прошивка без налаштування порту, і тільки потім `radio.yml`** | наша прошивка з режимом `NONE` поводиться як звичайний EdgeTX, тож перший крок перевіряє рівно одну річ — чи живий пульт із нашим кодом. Так помилка одразу локалізується: помер після прошивання — винна прошивка, після правки файлу — винен файл | `tasks/0010-first-flash.md` |
| 2026-07-26 | ⏸ **Крок 1.7 (збірка для інших пультів) відкладено; після етапу 1 йдемо одразу на залізо** | рішення людини: підтримка чужих пультів — далека перспектива і не факт, що знадобиться. Крок 1.7 працює на майбутніх користувачів, етап 2 — на власника цього пульта і відповідає на питання «чи воно взагалі працює на залізі». Відкладення нічого не псує, борги кроку зібрані в `docs/04-plan.md` | `docs/04-plan.md`, пункт 1.7 |
| 2026-07-26 | Координати дотику — **знакові** `int16` LE, і в `0x83`, і в `0x87` | виправляє «беззнакове», записане асистентом того ж дня. Різниця не косметична: пульт обрізає координати по екрану, і `-1` знаково стає нулем, а беззнаково — 65535, тобто **протилежним краєм екрана**. Помилка клієнта на піксель вліво відправила б палець управо | `docs/03-protocol.md`, задача 0008 |
| 2026-07-26 | Правило довжини `INPUT_STATE`: менше 13 байтів — відкинути цілком **і тайм-аут не відсувати**; рівно 13 — застосувати; більше — узяти перші 13 | обрізана маска — це рівень, тобто «решта клавіш відпущена»; потік побитих коротких пакетів інакше тримав би ввід живим, нічого не виправляючи. Довший вантаж — за правилом `HELLO`: поля лише в кінець | `docs/03-protocol.md`, питання автора моста в задачі 0008 |
| 2026-07-26 | **Біт3 у прапорцях `HELLO` = «прошивка розуміє `INPUT_STATE`»**; при нулі клієнт відкочується на `PING` раз на 250 мс, поки щось утримується | міст ESP32 не міг відрізнити стару прошивку від нової, а поводитись із ними треба по-різному. Незадіяні біти прапорців завжди були нулями, тож правило «зміна наявного поля = нова версія» не порушене. Запасний шлях — це скасований варіант асистента, і на старій прошивці він працює, бо там ще діє «будь-який пакет доводить, що клієнт живий» | `docs/03-protocol.md`, задача 0008 |
| 2026-07-26 | Гачок прискорення — **`remoteUiAddEncoderDt(&rotencDt)`**, вказівник, а не `rotencDt += remoteUiTakeEncoderDt()` | з вказівником функція може не робити запису **взагалі**, поки віддалених клацань немає: без клієнта фізична ручка поводиться точно як до Remote UI. Рядок у чужому файлі однаково один | `docs/05-hooks.md` пункт 7, задача 0008 |
| 2026-07-26 | Залишкова гонка `*rotencDt += pending` **лишається без критичної секції** | вікно в три інструкції відкривається лише коли віддалене клацання збігається з фізичним перериванням; найгірший наслідок — одне фізичне клацання із завищеним прискоренням, положення й напрямок не зачіпаються. Критична секція коштувала б виходу за один дозволений рядок або CMSIS у нашому коді. На пульті з розбитим екраном сценарій «людина крутить ручку, поки клієнт шле клацання» майже неможливий. Відкрито до перегляду, якщо на залізі виявиться інакше | `docs/05-hooks.md` пункт 7, задача 0008 |
| 2026-07-26 | Знахідка в upstream — кандидат в **окремий PR у EdgeTX**: `pl18u_key_driver.cpp` пише в `rotencDt` абсолютну позначку в одиницях 10 мс, тоді як STM32 і симулятор накопичують мілісекунди | при тій самій формулі прискорення на PL18 виходить приблизно вдесятеро більшим. Нас не зачіпає (родина збирається з `USE_HATS_AS_KEYS`, де `rotencDt` не читається). Цінне тим, що це виправлення чужої вади без нашого прапорця збірки — добрий привід для першого контакту зі спільнотою. Постане на кроці 1.7 | задача 0008 |
| 2026-07-26 | Дві назви в переліку гачків виправлено за кодом: `remoteUiGetEncoderOffset()` (не `Delta`) і `remoteUiPopTouch(int16_t*, int16_t*, bool*)` (не `lv_indev_data_t*`); гачок у `keys.cpp` додатково закрито `&& !defined(BOOT)` | «delta» штовхає до семантики приросту, що обнуляється; приймання структури LVGL суперечило б рішенню «типи LVGL розбираються на місці виклику»; завантажувач збирає той самий `keys.cpp` і успадковує `-DREMOTE_UI`, але наших джерел не має | `docs/05-hooks.md`, задача 0007 |
| 2026-07-26 | ✅ **Глобального `IDF_PATH` у системі немає** — головна пастка кроку 2.6a не спрацювала й не спрацьовувала | `IDF_PATH` і `IDF_TOOLS_PATH` порожні, у `~/.bashrc`, `~/.zshrc`, `~/.profile`, `~/.zshenv`, `~/.zprofile` про ESP-IDF немає жодного рядка. Розширення ESP-IDF поставлене через EIM 2.0, який активується точково скриптом, а не через профіль оболонки. Прибирати не було чого | крок 2.6a, задача 0011 |
| 2026-07-26 | ⚠️ **Власник збірки моста — `idf.py` на встановленому ESP-IDF v5.5.4, а не PlatformIO. Уточнює ADR-0004, не скасовує його** | суть ADR-0004 — **чистий ESP-IDF без Arduino** — зберігається повністю, змінюється тільки збирач, і сам ADR це прямо дозволяє («переходимо на `idf.py` з розширенням ESP-IDF для VS Code. Код від цього не змінюється»). Підстава — заміром: IDF 5.5.4 і обидва набори інструментів **уже стоять** і в цій сесії зібрали `hello_world` під `esp32` і `esp32c3`, тоді як `framework-espidf` не завантажений **у жодної** з двох платформ PlatformIO — тобто PlatformIO дотягнув би **третю** копію ESP-IDF (~0.7–1 ГБ) і **старішу**: pioarduino 53.03.13 дає 5.3.2, офіційна 6.13.0 дає 5.5.3. Плюс PlatformIO тут — чуже робоче середовище: усі **17** проєктів на диску — `framework = arduino`, жодного `espidf`. Розподіл «PlatformIO = Arduino-проєкти людини, `idf.py` = наш міст» знімає питання назавжди | крок 2.6a, задача 0011, ADR-0004 |
| 2026-07-26 | Розширення VS Code **не чіпаємо**, місця **не звільняємо** — рішення людини | обидва розширення поставлені навмисно і обидва в роботі. Порахували чесно: «близько 12 ГБ», як припускала задача, немає — `pio system prune` вважає зайвими лише 279 МБ кешу, решта 4.8 ГБ пакетів PlatformIO належить установленим платформам. Реально безпечно звільнялося б ~1.6 ГБ (кеш + стара платформа `espressif32@6.13.0` з двома наборами інструментів на 1.3 ГБ, яку не закріплює жоден проєкт), ще 2.2 ГБ — `esp-clang`, але ціною навігації по коду в IDE | крок 2.6a, задача 0011 |
| 2026-07-27 | ⚠️ **Клієнт сам просить `REFRESH` за лічильником `packets_dropped`, з придушенням не частіше ніж раз на 2 с** | ухвалене «міст відкинув — клієнт попросить REFRESH» існувало **тільки на папері**: `FRAME_END` проходить, плитки того ж кадру відкинуто, клієнт показує кадр цілісним, а пульт ту плитку більше не надішле — він шле лише зміни. Прямокутник зі старими пікселями лишався б назавжди. Протокол не міняємо, робимо в клієнті: `/api/stats` уже тягнеться. Придушення обов'язкове, інакше на вузькому каналі `REFRESH` годує сам себе. **Найімовірніша відповідь на чорний прямокутник із задачі 0010** | рецензія коду, задача 0011, знахідка 11 |
| 2026-07-27 | Сторож мовчання **відпускає ввід, але не рве сокет** | це два різні висновки: «ввід застарів» і «телефона немає». Згаслий екран телефона (браузери душать таймери у фоні) коштував би перепідключення плюс повний `REFRESH` — саме там, де міряємо затримку. Плюс запасний шлях для прошивки без біт3 давав би нескінченний цикл перепідключень із повідомленням про не ту причину. Смерть сокета й так ловлять `close_fn` і подія Wi-Fi | рецензія коду, задача 0011, знахідка 5 |
| 2026-07-27 | Невдале відправлення у WebSocket = **викинута пачка**, а не «телефона немає» | серед помилок `httpd_ws_send_frame_async` є банальне переповнення вікна TCP. Інакше: затор → розрив → перепідключення → `REFRESH` на 20–40 КБ у той самий затор → знову розрив. Ухвалена плавна деградація підмінювалася б значно грубішою. Рішення «телефона немає» лишається за сторожем мовчання | рецензія коду, задача 0011, знахідка 6 |
| 2026-07-27 | ⚠️ **Накопичувати результати перевірок через `ok &= …` заборонено — тільки `ok = ok && (… == pdPASS)`** | `xTaskCreate` при відмові віддає не нуль, а `errCOULD_NOT_ALLOCATE_REQUIRED_MEMORY`, тобто **-1** (`projdefs.h:67`), тоді як `pdPASS` — це 1. Побітове «і» дає `1 & -1 == 1`, і накопичувач лишається успішним завжди. Вада сиділа **всередині** перевірки, доданої проти мовчазної відмови: код виглядав захищеним і не був, а міст стартував би без сторожа, повідомляючи «міст працює». Правило ширше за FreeRTOS: крізь `&=` непоміченою проходить **будь-яка** функція, що позначає помилку від'ємним числом | `firmware/esp32/main/main.c`, рецензія задачі 0011 |
| 2026-07-27 | ⚠️ **Пароль точки доступу в публічному репозиторії — питання етапу 3, не цієї задачі** | з опублікованим паролем це та сама відкрита мережа, тож коментар у коді зараз заспокоює даремно і має бути виправлений. Поки стенд на столі — байдуже; до етапу 3 треба або пароль, похідний від MAC, або зміну без перезбирання | рецензія коду, задача 0011 |
| 2026-08-01 | ⚠️ **Заміряно на нашій платі DevKit v1: пін `5V`/`VIN` розв'язки з USB не має — 4.995 В при встромленому USB і нічому іншому.** Правило «USB і 5 В з пульта ніколи одночасно» діє без винятків; два джгути (3 дроти / 4 дроти), різні на вигляд | найгірший із чотирьох варіантів таблиці фази 2 підтвердився заміром, а не припущенням. Ризикує **пульт**, не плата: 5 В з ПК приходять на вихід ключа живлення порту, і при P-канальному ключі паразитний діод у цьому напрямку відкритий — вимкнений пульт отримує живлення повз усі послідовності вмикання. Решта замірів у нормі: `3V3` = 3.285 В, GND↔5V = 7 МОм, GND↔3V3 = 2 кОм **однаково в обидва боки** — резистивний шлях, тобто внутрішній дільник зворотного зв'язку AMS1117; світлодіод живлення виключений симетричністю. Не несправність: 1.65 мА при 3.3 В, і сама напруга в допуску, тоді як замикання просадило б її | `firmware/esp32/BRINGUP.md`, фаза 2 |
| 2026-07-27 | ⚠️ **Накопичувач `ok &= xTaskCreate(...)` не виявляє відмови — перевіряти кожен виклик окремо** | `xTaskCreate` при відмові віддає `errCOULD_NOT_ALLOCATE_REQUIRED_MEMORY` = **`-1`** (`projdefs.h:67`), а `pdPASS` = `1`. `1 & -1 == 1`, тобто побітове «і» відмову проковтує і `abort()` не спрацьовує. Знайдено при перевірці виправлення знахідки 7: правка проти мовчазної відмови сама була мовчазною відмовою, ще й захованою за кодом, який виглядає як перевірка. Шаблон `ok &=` небезпечний скрізь, де функція віддає `-1` | задача 0011, `main.c:252-255` |
| 2026-08-01 | ⚠️ **Прошивка пульта збирається пінованим `arm-none-eabi-gcc` 14.2.rel1, який ставиться скриптом у теку поза git. `USE_UNSUPPORTED_TOOLCHAIN=YES` для збірок, що йдуть у пульт, заборонений** | системний gcc 16.1.0 (Arch) не підходить: EdgeTX пінує 14.2.rel1 і зупиняє збірку жорстким `FATAL_ERROR` ще до старту — послаблювати нема чого, це не попередження. Прапорець обходу існує саме для «на свій ризик»: 16.1 проти 14.2 — два мажорні релізи GCC, інша кодогенерація й інший newlib, а стенд ловить «працює загалом», не рідкісний збій. Пульт керує моделлю в повітрі й не має екрана, тобто ціна помилки несиметрична. Той самий принцип, за яким версію EdgeTX закріплено на **тезі**: збіг має бути гарантією, а не випадковістю. Docker при цьому не потрібен — зникла з ним лише прив'язана версія компілятора, а не єдиний шлях | `tasks/0014-native-firmware-build.md`, рішення людини |
| 2026-08-01 | ✅ **Ланцюг «пульт → ESP32 → Wi-Fi → телефон» працює на живому залізі.** Ворота A і B задачі 0012 пройдені повністю | заміром, не міркуванням: 17.3 МБ трафіку, 95 905 пакетів, **нуль помилок CRC і нуль втрат на всіх трьох лічильниках**. Екран пульта видно на телефоні, сенсор доходить до пульта | `tasks/0012-bringup-bench.md`, `docs/img/bridge/02-radio-screen-over-wifi.png` |
| 2026-08-01 | **Напруга на піні `5V`/`VIN` DevKit при живленні від USB — 4.995 В: розв'язки немає** (перше з чотирьох чисел `BRINGUP.md`) | замір людини перед задачею 0012. Наслідок жорсткий: USB від ПК і 5 В від пульта не з'єднувати ніколи, бо 5 В приходять на **вихід** ключа живлення порту | `firmware/esp32/BRINGUP.md`, фаза 2 |
| 2026-08-01 | ⏸ **Три інші числа `BRINGUP.md` — не міряно: фаза 5 не проводилась** | «чи стартує пульт із конденсатором 1000 мкФ», «напруга на 5 В порту під навантаженням Wi-Fi», «просадка, чи тягне AUX1 150–250 мА» — усі три вимагають четвертого дроту й трійки конденсаторів, яких фізично немає (`docs/09-bom.md`, борг асистента). Задача 0012 навмисно обмежена фазами 0–4 | `tasks/0012-bringup-bench.md`, межі задачі |
| 2026-08-01 | ⚠️ **Вузьке місце — дріт на 921600, а не Wi-Fi. Оптимізувати міст немає сенсу** | заміряно через міст: повна перемальовка **1096 мс** проти дротових 1149, дрібна зміна **88 мс** проти 95 — тобто Wi-Fi не додає затримки взагалі, а подекуди швидший. Скарга «3–6 кадр/с при гортанні» пояснюється тим самим насиченим дротом, що й у 0010. Важіль лишається **2 Мбод** | задача 0012, критерій C5 |
| 2026-08-01 | ✅ **Мікшер циклів не пропускає і з увімкненим Wi-Fi поруч: `Tmix max` 0.47 мс із періоду 4 мс під навантаженням** | приріст проти дротових 0.46 мс — **0.01 мс**. ⚠️ Скидання лічильника доведене логічно, а не натисканням: `Tmix max` — монотонний максимум, а він **зменшився** з 0.48 до 0.46, що можливо лише після справжнього скидання. Це закриває пастку задачі 0010 | задача 0012, критерій C4 |
| 2026-08-01 | ✅ **Чорний прямокутник із задачі 0010 — вада знімача, а не втрата плитки. Питання закрите** | гіпотеза handoff-у `2026-07-26-11` підтвердилась: знімач зупинявся за лічильником кадрів, а повний екран 480×272 — це **135** плиток (15 × 9). Переписаний на очікування **покриття пікселів**, він дає 100.0% (130 560/130 560) стабільно. Урок ширший: доказом цілісності кадру є покриття, а не число `FRAME_END` | задача 0012, критерій C1 |
| 2026-08-01 | ⚠️ **Заміри проти моста вимагають рівно одного клієнта — інакше вони перетворюються на шум** | телефон і клієнт із ПК витісняли один одного: **53 з'єднання і 53 розриви** за сеанс, `BrokenPipeError` посеред кадрів і несправжні «дірки» в картинці. Витіснення — задумана поведінка (одна розмова з пультом), але в `BRINGUP.md` про його вплив на заміри не написано | задача 0012, кроки 17–18 |
| 2026-08-01 | ⚠️ **`tools/release_watch.py` має ваду насичення: тример доїжджає до упору і перестає рухати канал, хоча його тримають** | інструмент міряє «коли канал востаннє ворухнувся», тож після упору він занижує затримку аж до нуля. Видно прямо у виводі: тример 2 обслуговував перші прогони, а далі «канал не ворухнувся» і інструмент перейшов на тример 3. Через це числа C2 мають розкид **0…1829 мс** і доказом не є. Лікування — повертати тример у вихідне між прогонами або міряти органом без упору | задача 0012, критерій C2 |
| 2026-08-01 | ✅ **Відпускання вводу працює на живому ESP32: справжній розрив Wi-Fi — 184 мс проти 1000 мс тайм-ауту прошивки** | найважливіше число задачі: досі це було доведене лише на **програмному двійнику**, а на ESP32 інший мережевий стек і справжня втрата асоціації замість закритого сокета. Міст помітив зникнення телефона **сам**, не покладаючись на останній рубіж у прошивці. ⚠️ Методика: рвався Wi-Fi **на ПК**, бо тример тримає клієнт із ПК, а не телефон | задача 0012, критерій C3 |
| 2026-08-01 | ⚠️ **Клієнт, що перестав читати, втрачає сокет мовчки — міст цього не бачить** | пауза ~3 с у читанні дає `BrokenPipeError` на наступному відправленні, при цьому `ws.errors` і `send_dropped` лишаються **нулями**, тобто шлях плавної деградації в `ws_bridge.c:214-234` не спрацьовує: з'єднання скидає, найімовірніше, сам lwIP через RST після вичерпання повторів. Рішення від 2026-07-27 «сторож мовчання сокет не рве» ухвалене саме заради згаслого екрана телефона — і **його передумова на залізі не виконується**. Рішення за асистентом: чи вважати мовчазний RST вадою | задача 0012, крок 17 |
| 2026-08-01 | ⚠️ **Середовище розробки зникло разом із перевстановленням системи; репозиторій уцілів** | на 2026-08-01 немає ні `~/.espressif`, ні розширень VS Code (ESP-IDF і PlatformIO), ні `docker`, ні `~/Проекти`; `/home` зайнято 14 ГБ зі 167. ESP-IDF v5.5.4 поставлено наново через `git clone` + `install.sh esp32`, **активація тепер `. ~/esp/esp-idf/export.sh`**, а не `activate_idf_v5.5.4.sh`. Збірка моста дала той самий розмір (843 536 Б, 45% розділу вільно), тобто відтворюваність підтверджена. ⚠️ Особу автора git теж довелося задати наново | задача 0012, кроки 0 і 2 |
| 2026-08-01 | ⚠️ **Прошивка пульта збирається пінованим `arm-none-eabi-gcc` 14.2.rel1 (офіційний тарбол ARM), а не системним 16.1.0 і не через docker** | системний компілятор (Arch, 16.1.0) наштовхується на жорсткий `FATAL_ERROR` у `radio/src/CMakeLists.txt:591` — EdgeTX пінує рівно 14.2.rel1, і різниця в два мажорні релізи GCC для прошивки без екрана в літаючій моделі неприйнятна (`USE_UNSUPPORTED_TOOLCHAIN` навмисно не використовується). `tools/setup-toolchain.sh` ставить 14.2.rel1 з офіційного тарбола `armkeil.blob.core.windows.net` у `toolchain/` поза git, звіряє MD5, ідемпотентний. Збірка пройшла: `firmware` [100%] Built, Flash 1562.27 КБ / 485.73 вільно з 2048 — у соту частку кілобайта збігається з docker-еталоном 0009/0010 (1562.20 / 485.80) | задача 0014, ворота A |
| 2026-08-01 | Хостові Python-залежності збірки прошивки (`Pillow==12.3.0`, `lz4==4.4.5`, `libclang==18.1.1`) ставляться **тим самим `tools/setup-toolchain.sh`**, у venv `toolchain/py-fw-build/` (`--system-site-packages`), без `sudo` й без docker | системний Python не мав жодної з трьох, а без них падають `radio/util/encode-bitmap.py` і `generate_datacopy.py`. `libclang` узято самодостатнім PyPI-пакетом (носить `libclang.so` в собі) замість системного `clang`/`llvm` — сотні МБ і `sudo`. **Один скрипт, а не два** (рішення асистента): середовище тут недовговічне, а два скрипти означають, що колись запустять лише один. **Версії піновані числами** — інакше збірка стає заручником майбутнього релізу будь-якого з трьох, і ламатиметься на пульті без екрана | задача 0014, ворота A |
| 2026-08-01 | ✅ **Заміна компілятора регресії не дала: пульт на нативно зібраній прошивці поводиться так само, як на docker-збірці** | заміряно через міст при одному клієнті: канал насичений на **89.4%** (5 023 744 Б за 61 с при стелі 92.2 КБ/с) проти 90.1% і 90.5% до заміни; `Tmix max` **0.46 мс із 4 мс** під навантаженням проти 0.46 (0010) і 0.47 (0012); нуль на всіх лічильниках втрат — `crc_errors`, `dropped`, `oversized`, `tiles_dropped`, `send_dropped`. Відпускання вводу при розриві працює: міст помітив втрату сокета **сам** (`silence_timeouts` не зросло), пульт після обриву стояв нерухомо | задача 0014, ворота C |
| 2026-08-01 | ✅ **Дріт більше не вузьке місце: 2 625 000 бод тримаються, потреба під прокруткою 172 КБ/с — це 67% ємності, кадрів 9–10.5 замість ~5, втрат нуль** | ціль проєкту, поставлена ще в 0006 і підтверджена в 0010 і 0012, досягнута. Пульт прошивався **один раз** — щоб установити саму можливість; далі швидкість міняється командою за секунди, і повернення теж командою. ⚠️ Наслідок, який треба знати: вузьке місце переїхало, і де воно тепер — відкрите питання (захоплення, стиснення чи перемальовка EdgeTX) | `tasks/0017-runtime-baudrate.md` |
| 2026-08-01 | ⚠️ **Вузьке місце переїхало з дроту на Wi-Fi і на пам'ять моста**: дріт за 28 хв дав нулі на всіх лічильниках, а до телефона за той самий сеанс не доїхало 1059 плиток і 133 пачки, вільної купи в мінімумі 20 КБ | два роки міркувань про «ширшу трубу» закінчились: труба є. 20 КБ вільної купи — це вже не про якість картинки, а про стійкість моста, і воно наступне за важливістю | `tasks/0017-runtime-baudrate.md` |
| 2026-08-01 | ⚠️ **Плавності 25 кадр/с на цьому транспорті не буде, і це арифметика, а не недоробка**: повний кадр ≈ 25.4 КБ, ємність дроту 256 КБ/с, стеля ≈ 10 кадр/с. Заміряно 3.5 кадр/с на 921600 (88.6% дроту) і 7.5 на 2 625 000 (71%) | для 25 кадр/с треба у 2.5 раза більше за **апаратну** межу AUX1, тобто це не питання налаштувань. Мета проєкту — керувати пультом із розбитим екраном, а не дивитись відео; 7–10 кадр/с для меню достатньо. Записано, щоб очікування були чесні, а не щоб виправдати | `tasks/0017` |
| 2026-08-01 | **Поправка асистента до рядка нижче: аргумент «дрібніша плитка вкорочує серії для RLE» — хибний.** Лишається один аргумент: учетверо більше заголовків | подивився в `rle16.cpp`: серії йдуть наскрізь через увесь масив плитки (`pixels[i+run]`), а не по рядках, і обмежені лише лічильником 255. Однотонна плитка 32×32 — це 1024 пікселі, тобто 5 серій; 16×16 — 256 пікселів, 2 серії. Питомої втрати немає. Решта рядка лишається чинною | `firmware/edgetx-patch/remote_ui/rle16.cpp` |
| 2026-08-01 | ⛔ **Мітка кадру в плитці (навіть безкоштовна, у вільних бітах байта методу) — закрито: вона не купує нічого** | заперечення команди, сильніше за початкову підставу асистента «не міняти дві речі одночасно». Під правилом показу «малюй, коли лічильник нуль» обидві гілки штампа марні: за **моментом відправлення** залишки кадру A виходять із міткою B (бо вміст уже B), тож A не збереться ніколи; за **приналежністю** мітка бреше про вміст, і клієнт зібрав би формально цілий кадр, зшитий із двох митей. Причина глибша за формат: **пульт закриває кадр раніше, ніж віддає його плитки**, тож кадр A недосяжний за побудовою. Умова оживлення — зміна семантики закриття кадру, а її чіпати не збираємось: саме вона рятує картинку від застигання в русі | `tasks/0019-frame-completeness.md`, критерій 1.1-біс |
| 2026-08-01 | ⚠️ **Причина розламу картинки — не втрата, а те, що кадр закривається, поки половина його плиток ще в черзі.** `FRAME_END` спрацьовує за подією «LVGL домалював», а це не «ми все віддали» | доведено трьома незалежними способами в 0018: розлам є при **нулі втрат**; не залежить від смуги (є і на 38, і на 187 КБ/с); і найсильніше — знімок показує **дві рамки виділення одночасно**, хоча в живому інтерфейсі виділений пункт один. Гілку закриття не чіпаємо, вона навмисна: без неї картинка застигала б у русі. Виправляється тим, що клієнту кажуть правду | `tasks/0018-bridge-losses.md`, `tasks/0019-frame-completeness.md` |
| 2026-08-01 | **`FRAME_END` несе `captureDirtyCount()` — скільки плиток кадру лишилось у карті. Клієнт показує одразу при нулі й чекає зі строком при більшому** | пропозиція команди, **краща за кандидат асистента**: прошивка це число вже має, а «кількість плиток кадру» довелося б вести окремо. Строк обов'язковий: цілим кадр не буває ніколи, тож правило «показувати лише цілий» дало б застиглий екран | [задача 0019](../tasks/0019-frame-completeness.md) |
| 2026-08-01 | ⚠️ **Потужність Wi-Fi опущена до 8.5 дБм за причиною, якої вже немає — перший підозрюваний у стелі 123 КБ/с** | `BRIDGE_WIFI_TX_POWER` = 34 (проти стелі 21 дБм, тобто ~втридцятеро менше) стоїть від часів, коли головним ризиком вважались завади внутрішньому модулю 2.4 ГГц. Ризик **знято 2026-07-25**: використовується зовнішній модуль через JR-відсік. Передавач задушений заради небезпеки, якої немає | `tasks/0018`, критерій 3.1 |
| 2026-08-01 | **Помилка асистента: задача 0018 поставлена на хибному підозрюваному** — причину розламу виведено з лічильників 0012 і записано в мету як факт | правильна поведінка команди — зупинитись на першому ж замірі, що суперечить меті, і не міняти механізм навмання. Мета задачі — теж гіпотеза, і вона так само підлягає спростуванню | `tasks/0018`, проміжна рецензія |
| 2026-08-01 | **Правило вказівника: у повідомленні людині — адреса, у файлі — зміст. Момент зміни сесії називає асистент** | рішення людини. Людина не поштар: вона не має переносити абзаци між сесіями, і жодне рішення не має існувати лише в промпті — наступна сесія читає **репозиторій**, а не історію розмови. Обидві сесії пишуть коротко («візьми `tasks/NNNN`, критерії 1 і 2»), усе змістовне спершу лягає у файл. Друга половина: сесію ріжуть свідомо — після прийнятої задачі, перед незворотним кроком на залізі, після двох-трьох задач поспіль — і асистент **сам** каже, коли `/handoff` і нова сесія з `/catchup` | `CLAUDE.md`, `docs/ROLE-assistant.md` |
| 2026-08-01 | **Кандидат на розв'язок розламу: `FRAME_END` несе кількість плиток у кадрі (2 байти), клієнт показує кадр цілим, якщо все доїхало, і за строком (~100 мс) — як є, з проханням оновити** | ідея людини «буфер на цілий кадр» правильна, але сама по собі не працює: «цілий» зараз невизначуване, а при втраті пачки кадр не збереться **ніколи** — правило «показувати лише цілий» дало б застиглий екран. Лічильник у `FRAME_END` дає клієнту вперше знати, чи все доїхало, і дешевший за нумерацію кожної плитки. Наслідок: поки канал устигає, розламів не видно взагалі; коли не встигає — картинка не завмирає. Робити **після** 0018, бо втрати може прибрати вже вона | питання людини 2026-08-01, `tasks/0018-bridge-losses.md` критерій 4.2 |
| 2026-08-01 | ⚠️ **Згладжувати розлам засобами CSS — не братись.** Форму на смуги міняти можна, косметику поверх неї — ні | CSS уміє перетікати між двома картинками, але не знає, що вміст **поїхав**: замість чіткого зсуву вийде подвійний розмитий текст, а для інтерфейсу з дрібними літерами це гірше за чесний розлам. Плюс перехід додає затримки рівно там, де її щойно виборювали. Згладжування мало б сенс лише зі знанням зсуву в пікселях, а пульт його не повідомляє | ідея людини 2026-08-01 |
| 2026-08-01 | ⚠️ **«Картинка розвалюється квадратами» — не наслідок форми плитки. Дві причини: кадр зшивається з двох митей (`FRAME_END` навмисно не чекає черги) і **втрачені плитки** (1059 за сеанс) лишають старе зображення до наступного `REFRESH`, який придушений до разу на 2 с** | питання людини 2026-08-01. Форма при цьому таки важить, але **перцептивно, не в байтах**: горизонтальна смуга на всю ширину рве картинку як застаріле відео — рядок тексту лишається цілим; квадрат розрізає літери посередині слова, і те саме зміщення читається як поламаний інтерфейс. Порядок лікування: спершу перестати губити плитки (пам'ять моста), потім нумерація кадрів (байт у заголовку + обробка «кадр не зібрався»), форма — косметика, яку можна змінити заразом | `docs/02-architecture.md`, `tasks/0017` |
| 2026-08-01 | **Сітка плиток більше ні на чому не тримається — форма одиниці передачі стала вільним параметром** | первісно сітка була **способом шукати зміни** (порівняння двох кадрових буферів). Після переходу на гачок `remoteUiOnFlush` пошук зник — зміни приносить сам гачок, — і 2026-07-26 записано, що сітка лишається «як одиниця стиснення й передачі». Наслідок, який доти ніхто не проговорив: квадрат більше не обов'язковий, смуга чи будь-що інше так само законні | `docs/02-architecture.md` |
| 2026-08-01 | ⚠️ **Дрібніша плитка (16×16) при прокрутці найімовірніше зробить гірше, а не краще — але дослід усе одно вартий, бо це одне число** | пропозиція команди спиралась на «при прокрутці міняється 80 плиток із 91». Саме тому вона й не спрацює: при прокрутці змінюються **всі** пікселі, тож дрібніша сітка не економить нічого, а додає вчетверо більше заголовків (510 плиток замість 91) і вкорочує серії для RLE. Виграш дрібної плитки — на **локальних** змінах (підсвітка рядка меню), де 32×32 шле вчетверо більше пікселів, ніж треба. Тобто вимірювати треба **обидва** випадки, і ймовірний висновок — не «менша краще», а «розмір має залежати від того, що змінилось» | `tasks/0017`, пропозиції команди |
| 2026-08-02 | ⛔ **Гіпотеза «наша задача транспорту витісняє малювання» — відхилена наявними числами, прошивку міняти не треба** | двічі суперечить даним 0020. (1) На **921 600** транспорт крав би менше часу, тож малювання мало б прискоритись — заміряно 4.2 проти 4.3 кадр/с, не зрушило. (2) У плитках, а не кадрах: енкодер дає 13.6 кадр/с × 11 = **150 плиток/с**, протяг 4.3 × 44 = **190**. Тобто в режимі, де транспорту роботи **більше**, малювання встигає **більше** — протилежне до витіснення | `tasks/0020-wait-by-input-source.md`, рецензія |
| 2026-08-02 | ⚠️ **Переформулювання: пропускна здатність малювання приблизно стала (150–190 наших плиток/с), а частота кадрів = ця здатність / розмір кадру. «Пульт малює повільно під протягом» — хибне формулювання** | під протягом рухається весь список, тож кадр учетверо більший, а не малювання вчетверо повільніше. Це геометрія, а не вада — ні в нашому коді, ні в EdgeTX. **Наслідок: плавний протяг пальцем недосяжний з боку клієнта.** Зрушити можна лише піднявши пропускну здатність малювання EdgeTX на F429 або зменшивши площу перемальовки — обидва за межами етапу 2 | `tasks/0020`, рецензія |
| 2026-08-02 | **Правило очікування за джерелом вводу лишається ввімкненим, попри те що сліпе порівняння різниці не виявило** | рішення людини: «числа покращились учетверо, тоді на них орієнтуйся». Медіана затримки 463 → 115 мс, показаних кадрів 39 → 86 за 20 с. Покращення виміряне й стане видимим, щойно зникне обмеження з боку пульта; вимикач лишається приладом для порівняння, не налаштуванням | `tasks/0020`, критерій 2.1 |
| 2026-08-03 | ⛔ **Задачі 0015 і 0018 закрито: предмет зник, поки їх не робили** | 0015 ставилась при завантаженні каналу 90% і сторожі, що спрацював посеред сеансу; після 2 625 000 завантаження 16–71% і симптом не відтворювався жодного разу. 0018 лікувала втрати, які перестали відтворюватись (137 цілих кадрів зі 137), і шукала винуватця стелі 123 КБ/с, яку сама ж 0019 назвала **станом, а не межею** (пізніше 178–190 КБ/с без втрат), а після 0020 попит упав до 41–54 КБ/с, бо межею стало малювання в пульті. ⚠️ Умови відкриття наново записані в `tasks/INDEX.md`, розділ «Ревізія переліку» | `tasks/INDEX.md` |
| 2026-08-03 | ⛔ **Поправка до рядка нижче, того ж дня: вліво-вправо НЕ стають сторінками — ні на джойстику, ні на клавіатурі.** Правило «намір розкладається за `HELLO`» лишається чинним, змінюється лише розкладка горизонталі | рішення людини. Усередині сторінки горизонтального руху на цьому пульті немає, тож стрілка вліво не має природного сенсу; а той, хто звик до пульта, знає `PAGE<` і `PAGE>` як **окремі кнопки** — розмивати їх стрілками означає псувати саме ту звичку, заради якої відтворюється розкладка. Сторінки перемикаються лише своїми кнопками, на клавіатурі — `PageUp`/`PageDown` | [задача 0022](../tasks/0022-side-panels.md), розділ 3 |
| 2026-08-03 | **Органи керування в клієнті виражають *намір* (вгору/вниз/вліво/вправо/вибрати/назад), а чим його виконати — вирішує перелік клавіш із `HELLO`** | спостереження людини: сторінки в меню гортаються **горизонтально**, тобто `PAGE<`/`PAGE>` це «сторінка вліво/вправо». Отже на TX16S вліво-вправо не треба ховати — вони стають сторінками, і джойстик дає навігацію однією рукою. Правило: є справжні стрілки — шлемо їх; немає, але є сторінки — вліво/вправо стають `PAGE<`/`PAGE>`; немає нічого — напрямок не показується. Так усі чотири напрямки робочі на нашому пульті, а рядків про TX16S у коді нуль | [задача 0022](../tasks/0022-side-panels.md), розділ 3 |
| 2026-08-03 | **Наступне — третя ціль MVP: бокові панелі з клавішами й енкодером на телефоні. Задача 0022, етап 3** | півтора десятка задач поспіль покращували картинку; ця робить пульт **придатним до вжитку** без свого екрана. Зараз керувати можна лише тиканням у зображення: колесо миші й права кнопка з 0019 працюють тільки в браузері на ПК. Ключова вимога — **нуль рядків специфіки TX16S**: клавіші, підписи й наявність сенсора беруться з `HELLO` | [задача 0022](../tasks/0022-side-panels.md) |
| 2026-08-02 | ✅ **Пам'ять моста повернуто: 2.5 КБ → 43 КБ. Винен був не потік пікселів, а віддача сторінки клієнта — ціна сторінки в пам'яті дорівнює її розміру на дроті. Ліки: клієнт стискається gzip на збірці (122 → 41 КБ), розпаковує браузер** | 20 с протягу пальцем не забирають із купи **жодного байта**; усе просідання належить кільком сотням мілісекунд віддачі сторінки. Постійного зайняття немає зовсім — купа повертається на 105–106 КБ. Витоку немає: рівень повернення однаковий у 1-шу і 11-ту хвилину під навантаженням 19.8 МБ | `tasks/0021-bridge-heap.md` |
| 2026-08-02 | **Пояснено 1.9 КБ із 0020: `esp_get_minimum_free_heap_size()` монотонний від старту, тож число описувало найгіршу **мить**, а не стан сеансу** | панель у ту саму хвилину чесно показувала 106 КБ — опитування раз на секунду просідання тривалістю в частку секунди не застає. Наслідок для методики: показувати треба **мінімум у вікні** зі скиданням, а не «вільно зараз» | `tasks/0021`, прилад `heap_watch` |
| 2026-08-02 | ⛔ **`CONFIG_LWIP_TCP_SND_BUF_DEFAULT` не чіпаємо.** Якщо пам'яті колись забракне, перший важіль — **обмежити кількість одночасних сокетів**, а не розмір буфера | ціль досягнута без цього (38 976 проти 32 768, нулі на всіх лічильниках за 11 хв). Головне: важіль не б'є по названій небезпеці — небезпека це **множення на кількість сокетів** (чотири паралельні завантаження доводять купу до 2 148 Б), а зменшення буфера множник пом'якшує, але не скасовує. Міст **однокористувацький за задумом** і вже витісняє другий клієнт — дозволяти чотири паралельні завантаження суперечить його ж будові. Умови перегляду: заміряна відмова через брак пам'яті + обмеження сокетів уже зроблене й не вистачило + замір пропускної здатності до і після на тому самому стенді | `tasks/0021`, рецензія |
| 2026-08-02 | **Виправлення критерію асистента: витік перевіряється за «рівнем повернення», а не за мінімумом у вікні** | правило «мінімум за 10 хв проти мінімуму за 1-шу» назвало б витоком звичайне завантаження сторінки. Мінімум у вікні каже, **чи трапилась подія**, а не чи тане запас; витік видно тоді, коли купа перестає **повертатись** на попередній рівень | `tasks/0021`, розділ «Відхилення» |
| 2026-08-02 | ⚠️ **Вільної пам'яті в мості 1.9 КБ проти 20 КБ у 0017, з одним клієнтом — причина невідома. Наступне за важливістю, важливіше за плавність** | це вже не про якість картинки, а про те, чи міст доживе до кінця сеансу. Знайдено в 0020 і записано рядком у «Що не вийшло» — саме так такі речі й губляться | `tasks/0020` |
| 2026-08-02 | ✅ **`Tmix max` при зміненій прошивці 0019 — 0.44 мс, 6.3% періоду, циклів не пропущено. Борг-блокер закрито** | знято під найбільшим навантаженням, яке взагалі колись давали (145 КБ/с проти 80–90 у 0010/0012/0014). Скидання доведене **прямо** — `Lua Duration` 20→0, `Interval` 730→50 — а не немонотонністю, як і вимагала рецензія. ⚠️ Період тепер 7 мс, не 4: його задає протокол радіомодуля, тобто залежить від моделі. Тому висновок узятий у частках періоду, а не в абсолютних мілісекундах | `tasks/0019`, розділ «Закриття боргів» |
| 2026-08-02 | ⚠️ **«Показано за строком = 0» вірне лише на 2 625 000. На 921 600 строк спрацьовує на кожному третьому кадрі, і шви там є** | гілку, яку прогін 2026-08-01 оголосив мертвою, просто ніколи не міряли на базовій швидкості. Підтверджено **сліпим** розпізнаванням: людині показали два прогони не кажучи який з чим, і вона назвала фризи рівно там, де їх бачили лічильники. **Практичний наслідок:** після кожного перезапуску стенда пульт удома на 921 600, і поки клієнт не замовив високу — шви є, і це **не регресія**. Перед кожною наступною скаргою «знову рве» перевіряти швидкість, а не код | `tasks/0019`, `state/PROGRESS.md` |
| 2026-08-02 | **Сліпе порівняння режимів людиною — робочий інструмент проєкту, а не разова вигадка** | у 0019 воно двічі дало те, чого не давали лічильники: підтвердило вибір режиму очікування і викрило залежність від швидкості. Людині не кажуть, що ввімкнено, — вона називає, що бачить. Застосовувати скрізь, де критерієм є око | `tasks/0019`, `tasks/0020` критерій 2.3 |
| 2026-08-02 | ✅ **Розлам вилікувано: `FRAME_END` несе `dirtyTiles`, клієнт чекає. Типовий режим — «чекати завжди», поріг знято** | заміром на залізі: 100% показаних кадрів цілі проти 75% із порогом 45, ціна 8.8 → 6.6 показів/с. Узгоджується з рішенням від 2026-07-26 «кадри рідшають, але кожен показаний цілісний» | `tasks/0019-frame-completeness.md`, `docs/03-protocol.md` |
| 2026-08-02 | ⛔ **Поріг очікування як важіль — закрито числами: `dirtyTiles` ніколи не наближається до розміру сітки (стеля ~96 зі 135), а всі режими керування лежать у діапазоні 80–96 і порогом не розділяються** | скасовує гілку, виведену **арифметикою асистента** до заміру. Показ за строком не стався **жодного разу** ні за якого порога, тобто загроза «~4 кадр/с зшитими кадрами» на цьому залізі не справдилась. Питання закрите **даними**, а не думкою — до нього не повертатись | `tasks/0019`, розділ «Поріг як важіль» |
| 2026-08-02 | ⚠️ **Ривки при протягу пальцем додає наше очікування, а не грубий вхід LVGL** | спершу пояснювались `LV_INDEV_DEF_READ_PERIOD` = 30 мс. Уточнення людини вбило цю версію: **на робочому екрані пульта фризів немає** — той самий вхід, та сама перемальовка. Отже 30 мс дають повільність позиції, а не ривок. Звідси важіль: клієнт знає, чим щойно керували (сам надіслав `ENC` або `TOUCH`), тож правило має бути «після енкодера чекаю, після дотику — ні» — при протягу людина стежить за рухом, а не за чіткістю кадру | `tasks/0019`, розділ «Ціна, записана свідомо» |
| 2026-08-02 | ⚠️ **`Tmix max` при зміненій прошивці 0019 не знято — головна перевірка безпеки з ТЗ лишається боргом-блокером** | пульт носить прошивку, для якої її не проходили. Зміна мізерна (два байти в `FRAME_END`) і число майже напевно не зрушить, але правило проєкту — міряти, а не міркувати. Спосіб: обнулити лічильник на очах; немонотонність як методику не використовувати | `tasks/0019`, рецензія |
| 2026-08-02 | **Кожна правка `webui/` коштує прошивання моста** — клієнт живе в його бінарнику | у прогоні 0019 вийшло чотири прошивання замість двох: пульт один раз і міст тричі. Закладати в оцінку будь-якої задачі, що чіпає клієнт | `tasks/0019`, прогін на стенді |
| 2026-08-01 | ⚠️ **Крок 0 (зчитування пам'яті через DFU перед записом) більше не обов'язковий — стає умовним. Рішення людини, повторене двічі** | доводить лише «зв'язок із завантажувачем є зараз», а це видно з першої ж команди запису, і нічого страшного при відмові не стається. Перевірка, яка справді щось доводить, — **перечитування записаної області після запису**: вона ловить обірваний і зіпсований запис, тобто єдине, що здатне лишити пульт мертвим. Читання перед записом лишається у двох випадках: (1) затираємо щось невідтворюване — після 0014 майже не трапляється; (2) не знаємо, що в пульті — після обірваного запису, чужого втручання, довгої перерви. У решті випадків це хвилина на ритуал | `tasks/0014-native-firmware-build.md`, уточнення до B1 |
| 2026-08-01 | **Крок 0 дисципліни 0014 лишається, але як передпольотний огляд, а не як доказ** | зауваження людини правильне: успішне зчитування не доводить, що вдасться запис. Цінність кроку в іншому й вона лишається: провал читання **гарантує** провал запису (дешевий негативний фільтр), і він показує, що в пульті лежить те, що ми думаємо, перед тим як це затерти. Третя підстава — зберегти невідтворюваний вміст — відпала після 0014 | `tasks/0014`, уточнення до B1 |
| 2026-08-01 | ✅ **Виправлення рядка нижче: `Tmix max` на 2 625 000 **не зріс** — як був 0.46 мс, так і лишився. Обмеження «тримати лише в дослідах» знімається** | спостереження людини на стенді. Доказ сильніший за той, що вимагався: `Tmix max` — **монотонний максимум**, тож якщо він не піднявся за весь сеанс на високій швидкості під прокруткою, то й не піднімався. Скидання потрібне, щоб довести **свіжість** числа, а не щоб довести **неперевищення** — для верхньої межі воно зайве. Механізм асистент перебільшив: передавання йде через DMA (саме тому й обрано AUX1), процесор бачить переривання **раз на пакет**, а не на байт — при 172 КБ/с це близько 80 переривань за секунду, утричі більше від дуже малого. Лишається формально записати умови заміру в «Результат» задачі | `tasks/0017`, критерій 5.4 |
| 2026-08-01 | ⚠️ **`Tmix max` на 2 625 000 не знято — головна перевірка безпеки з ТЗ на цій швидкості не проходилась** | швидкість зросла в 2.85 раза, тобто стільки ж разів побільшало переривань і роботи DMA на тому самому процесорі, який крутить мікшер. Число 0.46–0.47 мс із 4 мс знімалося на 921600 і **не успадковується**. До зняття високу швидкість тримати лише в дослідах, не в польоті | `tasks/0017`, критерій 5.4 |
| 2026-08-01 | ⚠️ **Власний висновок — так само не доказ, як і чужий звіт: у одній сесії тричі поспіль «працює» сказано до перевірки** | самозвіт команди, вартий запису. Три випадки: успіх на 2 625 000 оголошено, коли статистика показувала 921600 і два відкоти; інструмент брав **живий зв'язок** за доказ швидкості, хоча після відкоту кадри йдуть так само справно; «дріт ні до чого» сказано за заміром на нерухомому екрані (0.2% проти 67% під прокруткою). Спільне одне: числа дивились **після** заяви, а не замість неї. Доповнює рядок про звіти субагентів — правило те саме, джерело інше | `tasks/0017`, розділ «Результат» |
| 2026-08-01 | ⚠️ **Звіт субагента про збірку — не доказ збірки. Перевіряти шлях і час зміни артефакту** | у задачі 0017 агент збірки двічі відзвітував успіх там, де його не було: зібрав прошивку в **інший** каталог (без пінованого файлу тулчейна) і назвав розмір виконуваного файла симулятора, якого не зібрав узагалі. Спіймано лише звіркою часу зміни `firmware.bin`. Правило проєкту «рецензія за доказами, а не за звітом» писалося про звіт **команди** — виявилось, що воно так само потрібне для звітів **субагентів**, і там перевірка дешевша: `ls -l` шляху, який очікуєш | `tasks/0017-runtime-baudrate.md`, розділ «Результат» |
| 2026-08-01 | ⚠️ **Симулятор EdgeTX зараз не збирається: у системі немає `clang`, а генератор `datacopy.inc` потребує `libclang` разом із його заголовками (версія з `pip` дає лише `.so`)** | наслідок перевстановлення системи 2026-08-01, не наших змін. Втрата не косметична: симулятор — єдиний спосіб перевіряти зміни протоколу **без** пульта, і саме він закривав етап 1 цілком. Лагодиться однією командою рівня системи, тобто дією людини | `tasks/0017`, розділ «Результат» |
| 2026-08-01 | ⚠️ **Стеля швидкості AUX1 — 2 625 000 бод, а не 2 812 500. Шина PCLK1 в EdgeTX тактується 42 МГц, не 45** | помилка асистента, знайдена командою при проєктуванні 0017: 45 МГц — це паспортний *максимум* APB1 для STM32F429, а EdgeTX тактує нижче. Доказ подвійний і незалежний: `LL_RCC_SetAPB1Prescaler(LL_RCC_APB1_DIV_4)` при SYSCLK 168 МГц (`f4/system_clock.c:85`) і `PERI1_FREQUENCY 42000000` (`targets/horus/hal.h:25`). Наслідок не косметичний: 2 625 000 = PCLK1/16 — рівно межа, за якою драйвер переходить на дискретизування ×8, тобто перелік швидкостей за стелю перетворив би «один наш рядок» на запис `CR1.OVER8` при увімкненому USART. **Стеля тут — умова дозволу на гачки, а не запас** | [ADR-0005](../docs/adr/0005-runtime-baudrate.md), `tasks/0017` |
| 2026-08-01 | ⚠️ **Приймальний буфер моста `BRIDGE_UART_RX_BUF = 16384` замалий для будь-якої швидкості вище ~1 Мбод — це передумова задачі 0017, а не тонке налаштування** | команда підняла це як питання про стелю (62 мс запасу проти заміряних у 0013 132 мс затримки задачі), але те саме число для 2.5 Мбод — 65 мс, для 2 Мбод — 82 мс. На 921600 буфер давав 178 мс і тому переживав ті 132 — це і є вся причина, чому досі працювало. Розмір має рахуватись **від заміряної затримки**, а доказом служить `uart.dropped` = 0 під насиченим екраном | `tasks/0017-runtime-baudrate.md`, пункт 1-біс |
| 2026-08-01 | Лічильників відкоту швидкості **два**: «пульт не почув моста» і «міст не почув пульта» | пропозиція команди, прийнято: різні події з різними причинами, і злиття втратило б рівно ту половину діагностики, яка каже, на чиєму боці шукати | `tasks/0017`, критерій 4.3 |
| 2026-08-01 | Похибка базової 921600 (**+1.273%** через подвійне обрізання в макросі LL) — знахідка команди, кандидат в окрему задачу; поруч є точне 933 333 | у цій задачі базову швидкість чіпати заборонено навмисно: вона спільна з мостом, і зміна поламала б сумісність саме тоді, коли перевіряють інше. Емпірично запас є — 17 МБ трафіку без жодної помилки CRC | `tasks/0017`, розділ «Результат» |
| 2026-08-01 | **Швидкість каналу міняється на ходу командою нашого протоколу; орган керування — на сторінці моста, не в меню пульта. Обов'язковий запобіжник: пульт сам вертається на попередню швидкість, якщо не почув осмисленого пакета** | випадайка в меню пульта вимагала б розширити формат збереження налаштувань — у EdgeTX на порт відведено **один байт** (`SERIAL_CONF_BITS_PER_PORT = 8`), вільного місця немає. Це зачепило б структури даних, розбір `radio.yml` і міграцію, тобто єдиний спосіб керувати пультом без екрана. Натомість `serialSetBaudrate()` в EdgeTX **уже є**, а рядок швидкості нашого режиму вже наш — тож варіант коштує нуль змін у чужому коді й одне прошивання назавжди. Швидкість навмисно **не зберігається**: пульт завжди піднімається на 921600, невдала спроба не успадковується | [ADR-0005](../docs/adr/0005-runtime-baudrate.md), `tasks/0017-runtime-baudrate.md` |
| 2026-08-01 | ⚠️ **Пояснення «ввід чекає позаду пікселів в одній трубі TCP» прийнято як гіпотезу, не як висновок. Зміну протоколу до пришпилення механізму не проєктуємо** | не сходиться з власними числами задачі 0013 у двох місцях: пікселі йдуть міст→клієнт, а ввід клієнт→міст, і в TCP це незалежні напрямки одного з'єднання; а 356 мс максимуму на `ping` у локальній мережі — не «ефір вільний», а стократне роздування. Альтернативна картина пояснює все: насичений ефір → втрата висхідного пакета → повторна передача з тайм-аутом від роздутого RTT → секунди тиші → прибуття **пачкою**, після чого лічильники застосунку збігаються. Вирішальний і дешевий доказ — `tcpdump` на клієнті у вікні навантаження: повторні передачі видно прямо | `tasks/0013-bench-debts.md`, рецензія |
| 2026-08-01 | **Якщо повторні передачі підтвердяться, розв'язок — дейтаграмний канал для вводу, а не другий WebSocket** | `INPUT_STATE` за побудовою є *рівнем*, а не переходом, і повторюється кожні 250 мс — тобто втрата самолікується за один період. Повторна передача йому не потрібна взагалі, а саме вона й створює секундні паузи. Другий потік TCP успадкував би ту саму ваду | `tasks/0013-bench-debts.md`, рецензія |
| 2026-08-01 | ⚠️ **Підстава рішення від 2026-07-27 «сторож не рве сокет» скасована заміром: телефон із згаслим екраном втрачає сокет за хвилину і не повертає його.** Саме рішення лишається — рвати нічого не дає; наслідок переїжджає в клієнт | берегти з'єднання заради фонової вкладки нема сенсу, бо його немає. Вимога до етапу 3: клієнт **сам** перепідключається після пробудження і просить `REFRESH`. Це вимога, а не побажання | `tasks/0013-bench-debts.md`, критерій 5 |
| 2026-08-01 | **Уточнення до рядка про немонотонність: умовивід у 0012 лишається чинним, непридатним є його узагальнення до методики** | максимум може лише зростати, тож спостережене зменшення 0.48 → 0.46 справді доводить скидання. Але спрацювало воно лише тому, що дореєстроване значення випадково стояло вище за робоче; коли максимум уже на робочому рівні, скидання не змінює нічого видимого — і спосіб мовчить замість сказати «не знаю». Як доказ у конкретному випадку — чинний; як спосіб перевіряти — ні | `tasks/0014-native-firmware-build.md`, рецензія |
| 2026-08-01 | ⚠️ **Сторож мовчання разово спрацював посеред живої сесії при 89% каналу, хоча клієнт слав ввід кожні 250 мс — розбиратися до переходу на 2 Мбод** | хибне спрацювання = відпущений ввід під пальцем людини, тобто збій того самого механізму, лише з протилежного боку. 2 Мбод зменшать завантаження приблизно вдвічі й симптом найімовірніше сховають, а причина лишиться й повернеться на важчому екрані. Перша гіпотеза до перевірки: потік передачі морить голодом задачу приймання вводу, тобто «мовчання» — це невичитаний буфер, а не мовчання телефона | `tasks/0013-bench-debts.md`, пункт 4-біс |
| 2026-08-01 | ⚠️ **Немонотонність не доводить скидання `Tmix max` — спосіб із 0012 не узагальнюється** | мікшер крутиться раз на 4 мс, тобто після скидання максимум набігає наново 250 разів за секунду і нуль неможливо побачити на екрані, що оновлюється повільніше. У 0012 пощастило: там максимум був 0.48 і впав до 0.46. Замість цього доводиться двома іншими доказами: на тій самій сторінці на очах обнуляється **інтервал**, а миттєве повернення до 0.46 доводить, що це **поточне** значення, а не давній викид. Правило: немонотонність придатна лише коли лічильник наповнюється повільніше за оновлення екрана | задача 0014, критерій C3 |
| 2026-08-01 | **Уточнення до рядка вище про немонотонність:** умовивід 0012 лишається **чинним**, непридатне лише його узагальнення до методики | максимум може тільки зростати, тож спостережене зменшення 0.48 → 0.46 справді доводить скидання — там доказ був правильний. Хиба в іншому: він спрацював лише тому, що дореєстроване значення випадково було вищим за робоче. Коли максимум уже стоїть на робочому рівні, скидання не змінює нічого видимого, і спосіб **мовчить замість сказати «не знаю»**. Як доказ у конкретному випадку — чинний; як методика — ні | задача 0014, рецензія асистента |
| 2026-08-01 | ⚠️ **«Навантажити канал» на пульті без екрана означає назвати екран, а не назвати дію** | перша спроба C1 дала +6 926 Б замість +5 млн, бо агент попросив ворушити стіками, вважаючи, що це перемальовує головний екран. **Головний екран TX16S положень стіків не малює** — за 60 с змінилися 4 плитки. Виправила людина за столом: потрібен **монітор каналів**. Той самий клас помилки, що й у 0012 з витісненням клієнтів: замір, який виглядає проведеним, а насправді міряв тишу | задача 0014, критерій C1 |
| 2026-08-01 | До звірки тарбола компілятора додано **SHA256 поруч із MD5**; `CPATH` на заголовки пінованого компілятора — обов'язкова змінна збірки | MD5 — єдине, що публікує ARM, але він ламається на колізіях; SHA256 рахується з архіву, який уже пройшов MD5, і пінується в скрипті. Обидві звірки перевірені на справді свіжому завантаженні, не лише кодом. Окремо: `libclang` з PyPI не знає, де лежать стандартні заголовки компілятора (`stdbool.h`), тому `generate_datacopy.py` падає без `CPATH` на `toolchain/.../lib/gcc/arm-none-eabi/14.2.1/include` | задача 0014, ворота A |
| 2026-08-01 | ⚠️ **Хибне спрацювання сторожа мовчання — це не міст і не Wi-Fi. Ввід і пікселі поділяють одне з'єднання TCP, і насичений потік пікселів затримує ввід на секунди** | Заміряно в 0013, 10 хв під аналізатором спектра, рівно один клієнт. Чотири числа з різних боків: паузи, які **бачить міст** — до **4196 мс**, 8 разів понад поріг; ті самі паузи, які **бачить клієнт** — максимум **301 мс**, понад поріг нуль; биття задачі, що чує ввід (`input_task.beat_interval_max_ms`, це інтервал між пробудженнями, тобто період биття плюс тремтіння) — **132 мс** проти 129 у спокої, тобто процесор вона отримувала весь час; `ping` у тому самому вікні — 1142 відгуки, максимум **356 мс**, понад 750 жодного. Пакетів не втрачено — лічильники обох боків збігаються, отже вони приїжджали пачкою. Клієнт слав вчасно, ефір був вільний, задача моста не голодувала — лишається сама труба. Сторож поводиться **правильно**: він справді не чув телефона 750 мс | `firmware/esp32/BRINGUP.md`, розділ «2-біс», задача 0013 |
| 2026-08-01 | Пріоритети задач моста **лишаються як були** (приймання 12, пікселі 6, сторож 4), хоча пікселі формально стоять вище за задачу httpd (5), яка чує ввід | Це був перший підозрюваний, і його **спростовано заміром**, а не міркуванням: биття в задачу сервера показало затримку 132 мс під тим самим навантаженням, за якого паузи вводу сягали 4196 мс. Міняти робочий розклад реального часу без підстави — гірше, ніж лишити його. Зв'язок із чужим числом більше не невидимий: пріоритети зведені в `bridge_cfg.h`, друкуються на старті, а `input_task.beat_interval_max_ms` лишається приладом, яким це перевіряють знову — насамперед після переходу на 2 Мбод | `firmware/esp32/main/bridge_cfg.h`, задача 0013 |
| 2026-08-01 | ⚠️ **Передумова рішення від 2026-07-27 «сторож мовчання сокет не рве» на справжньому телефоні не виконується: телефон із заблокованим екраном втрачає сокет сам, приблизно за хвилину, і не повертається** | Рішення ухвалювалось саме заради згаслого екрана — щоб не платити перепідключенням і повним `REFRESH`. Заміряно в 0013: вкладка відкрита, телефон у кишені; 45 с ввід ішов рівно (4 пакети/с), потім сокет помер (`lost` +1, `releases` +6, сторож +5), і за наступні **4.5 хвилини** перепідключення не було жодного. Тобто зберігати сокет нема чого — його однаково не буде. Безпека при цьому втрималась: ввід відпущено. Саме рішення **не скасовую** — воно за асистентом; але його підстава спростована й підлягає перегляду | задача 0013, критерій 5 |
| 2026-08-01 | Тример для заміру відпускання треба тримати **не менше 4 с**, а напрямок обирати найрухливіший із усіх | Авто-повтор EdgeTX розганяється не одразу: утримання **2 с давало нуль** подій руху каналу на всіх 12 напрямках, 12 с — від 60 до 415. Через це інструмент виглядав зламаним («тример не рухає каналів»), хоча тример працював — це коштувало сесії окремого розслідування з участю людини. Роздільність усього заміру дорівнює періоду повтору, а той різниться між напрямками всемеро (подія раз на 29 мс проти раз на 200 мс), тому вибір найрухливішого напрямку прямо покращує точність | `tools/release_watch.py`, задача 0013 |
| 2026-08-01 | ⚠️ **Стеля швидкості каналу — 2 625 000, і це умова дозволу на гачки, а не просто межа** | AUX1 це `USART3` на PCLK1, а EdgeTX тактує цю шину **42 МГц**, не 45 (`f4/system_clock.c:85`, `LL_RCC_APB1_DIV_4` при SYSCLK 168; `targets/horus/hal.h:25` — те саме число незалежно). 45 було паспортним максимумом APB1 для F429, а не фактичною частотою. 2 625 000 = PCLK1/16: за цією межею `_calc_best_oversampling()` вертає ×8, і `stm32_usart_set_baudrate()` пише `CR1.OVER8` **при увімкненому USART**, чого RM0090 не дозволяє. Тобто перелік за стелею вимагав би гасити й піднімати порт — це вже не один рядок у чужому коді, а нова ADR. Тримається двома `static_assert`, один звіряє стелю з `PERI1_FREQUENCY` цілі | `remote_ui/baudrate.h`, задача 0017 |
| 2026-08-01 | Відкат швидкості — **завжди на `REMOTE_UI_BAUDRATE`**, ніколи «на попередню» | «Попередня» — це стан, якого міст після перезавантаження не має взагалі. Ланцюг 921600 → 1 М (успіх) → 2 М (міст помер) лишив би пульт на 1 М, а міст піднявся б на власній константі — вони не зустрілись би. Домашня швидкість це дно, тому **вкладених відмов не існує за побудовою** і другого рівня відкату не потрібно. Ціна названа: після втрати моста удала сесія на 2 Мбод падає до 921600, а не до 1 Мбод. Перевірено на залізі скиданням `EN` у сліпому вікні — обидва боки зустрілись удома без втручання | ADR-0005, задача 0017 |
| 2026-08-01 | Перший перемикається **пульт**, міст іде слідом через `switch_delay_ms` із самого підтвердження; міст мусить озватися **негайно** після перемикання | Пульт робить це в мить, яку сам контролює (коли DMA дожував пачку), а підтвердження — останній кадр на старій швидкості. ⚠️ Ціна помилки заміряна: у першій реалізації міст мовчав перші 500 мс, чекаючи періоду нагадування, а вікно пульта спливає на 505-й — обидва боки перемикались бездоганно й **обидва йшли додому**, а виглядало це як «швидкість не тримається» | `esp32/main/baud_follow.c`, задача 0017 |
| 2026-08-01 | Приймальний буфер моста виводиться **із заміряної затримки задачі**, а не вибирається на око: 48 КіБ = 187 мс на 2 625 000 проти заміряних 132 мс | Старі 16 КіБ давали на 921600 **178 мс** — і це вся причина, чому доти працювало; на 2 Мбод той самий буфер дає 82 мс, на 2 625 000 — 62 мс, тобто **менше за затримку**, і `uart_dropped` перестав би бути нулем. Зв'язок числа з заміром тримає `_Static_assert`, тож підняти стелю переліку, не піднявши буфер, більше не можна мовчки. Ціна: вільної купи в мості з 72 КБ у мінімумі лишилось **20 КБ** | `esp32/main/bridge_cfg.h`, задача 0017 |
| 2026-08-01 | ⚠️ **Крок 0 дисципліни 0014 (зчитування до запису) здебільшого зайвий** | Заперечення людини сильніше за обґрунтування в 0014: успішне **зчитування** не доводить, що вдасться **запис** — це різні операції з різними режимами відмови. Доводить лише звірка після запису, і саме вона лишається обов'язковою завжди. Другий заявлений сенс («у пульті те, що ми думаємо») теж не діяв: писали годину тому в тій самій сесії, побайтова копія вже лежала на диску. Лишається один випадок — вміст пульта **невідтворюваний** (заводська прошивка); у нас прошивка своя, з git. Формулювання B1 у `tasks/0014` стало неточним | задача 0017, журнал прошивання |
| 2026-08-01 | ⚠️ **Плавності на цьому транспорті не буде, і це арифметика, а не якість реалізації** | Повний кадр — 91 плитка × 286 Б після RLE ≈ **25.4 КБ**; ємність дроту на апаратній стелі — 256 КБ/с. Отже межа ≈ **10 повних кадр/с**, а для 25 потрібно **2.5× понад стелю AUX1**. Заміряно на двох швидкостях, тричі, відтворювано: 921600 → 3.5 кадр/с при 88.6 % завантаження дроту (дріт і був стелею); 2 625 000 → 7.5 кадр/с при 71 % (дріт стелею бути перестав). Апаратного стиснення немає ніде: у F429 JPEG з'явився лише з F76x, апаратний CRC є, але поліном не наш; DMA2D є і вже задіяний EdgeTX, але це чужий код. Далі важать лише стиснення й втрати пачок, не швидкість дроту | задача 0017, заміри на стенді |
| 2026-08-01 | `FRAME_END` несе `dirtyTiles` (два байти, LE) — правду про неповноту кадру; момент закриття кадру не міняється | Доведено читанням коду (`capture.cpp`), не наміром: при `dirtyTiles=0` буфер клієнта дорівнює екрану пульта на цю мить, а не «майже цілий кадр». Клієнт при `N>0` чекає доїзду решти `50+3N` мс, стеля 250 мс від початку низки очікування (не від останнього показаного кадру — інакше після паузи запас був би вже вичерпаний). Жодного нового біта в `HELLO`: ознака вже в самому пакеті | ADR-0006 (не оформлено), задача 0019 |
| 2026-08-01 | Номер кадру в плитці (вільні біти байта методу чи окремим байтом) — **відхилено**: не змінює жодного рішення клієнта | Прошивка тримає одну версію пікселя, минулий кадр не існує й позначками не відтворюється; штамп моментом відправлення дублює `dirtyTiles`, штамп за приналежністю бреше про вміст або кадр не набереться ніколи. Ціна була б реальною: версія протоколу 2, чорний екран у неоновленого клієнта. Команда й асистент дійшли цього незалежно | задача 0019, коміт `002e329` |
| 2026-08-01 | Запобіжник очікування — поріг 45 плиток на 2 625 000, виведений із фізики (плитка ≈286 Б, дріт 262 500 Б/с, кадр пульта 50 мс), рахується зі швидкості каналу | Без нього безперервне гортання давало б ~4 кадр/с зі зшитими кадрами — гірше, ніж до задачі. Поріг стоїть на дроті (1.1 мс/плитку, середня плитка), а не на наскрізному шляху (3 мс/плитку, найгірша плитка) — обидва числа навмисно різні, бо поріг порівнює доїзд залишку з доїздом наступного кадру, а строк оцінює ризик. Прошивка пульта не змінена; запобіжник цілком у клієнті, вимикається кнопкою «чек» для порівняння оком | задача 0019, коміт `c06a0f1` |
| 2026-08-02 | ✅ **`Tmix max` при прошивці 0019 — 0.44 мс, мікшер циклів не пропускає. Борг-блокер рецензії закрито** | Скидання доведене **прямо**, без немонотонності: `Lua Duration` 20 → **0**, `Interval` 730 → **50**, і сам `Tmix max` 0.46 → 0.43 (максимум не може впасти без скидання). Навантаження — 145 КБ/с до телефона на 2 625 000 (гортання 8 клацань/с) і 131 КБ/с на перескоках сторінок; це **найбільший потік, при якому `Tmix max` колись знімався** — у 0010/0012/0014 дріт віддавав 80–90 КБ/с. ⚠️ `Period` цього разу **7 мс**, а не 4: період планувальника мікшера задає протокол радіомодуля (`mixer_scheduler.cpp`), тобто залежить від завантаженої моделі. Тому «0.44 краще за 0.46» — не висновок; висновок від періоду не залежить: найгірший цикл — 6.3 % періоду | задача 0019, `docs/img/radio/0019-32-debug-after-load.png` |
| 2026-08-02 | ⚠️ **«100 % цілих кадрів» — властивість швидкості 2 625 000, а не механізму 0019. На 921 600 стеля очікування спрацьовує на кожному третьому кадрі** | Відтворено чисто: свіжий клієнт на кожну швидкість, той самий жест, 20 с, той самий екран, підряд. 2 625 000 — 137 кадрів, **137 цілих, 0 за строком**; 921 600 — 121 кадр, 75 цілих, **46 за строком (38 %)**, і шов видно оком. Тобто твердження прогону 2026-08-01 «показано за строком = 0 при будь-якому порозі» вірне лише на високій швидкості — гілку просто ніколи не міряли на базовій. Практичний наслідок: після **кожного** перезапуску стенда пульт удома на 921 600, і поки клієнт не замовив 2 625 000, людина бачить шви. Швидкість каналу — умова цілісності картинки, а не зручність | задача 0019, `docs/img/client/0019-41…42-counters-*.png` |
| 2026-08-02 | Браузерний клієнт можна **водити з ПК без рук людини** — Playwright + дотики по канві | Знімає найдорожче обмеження стенда: раніше кожен замір потребував людини біля пульта. ⚠️ Дві межі: дотик коротший за ~100 мс пульт не помічає (`LV_INDEV_DEF_READ_PERIOD` = 30 мс, LVGL не потрапляє опитуванням у вікно) — робочий дотик 150 мс; і клавіші `ENTER` у браузерному клієнті **немає** (бокові панелі — етап 3, миша дає лише енкодер і «назад»), тому скидання лічильника все одно натискає людина. Кнопка `[ENTER] to reset` на екрані пульта — підпис до клавіші, а не сенсорний елемент: на дотик не реагує | задача 0019, `tools/webui_drive.py` |
| 2026-08-02 | ⚠️ **Під протягом пальцем пульт закриває лише 2.6–4.3 кадри за секунду — вузьке місце в малюванні, не в дроті й не в клієнті** | Заміряно лічильником моста `uart.frames`, який від нашої політики показу не залежить: протяг — 2.6–4.3 кадр/с при 41 КБ/с (16 % ємності дроту), енкодер 8 клацань/с — 13.6 кадр/с при 54 КБ/с. Плиток за секунду пульт віддає **приблизно однаково** (114 проти 149), але під протягом складає їх у вчетверо більші кадри (44 плитки проти 11): рухається весь список, а не одна рамка виділення. Наслідок: **жодна політика показу в клієнті не зробить плавним те, чого пульт не виробляє**, і передумова задачі 0020 («ривок додає наше очікування») вірна лише для медіани — провали (90-й відсоток ≈920 мс) однакові в обох режимах. Дві гіпотези, не розрізнені: EdgeTX справді так малює на F429, або винен наш патч (задача транспорту має пріоритет 2 і витісняє малювання) | `tasks/0020-wait-by-input-source.md`, розділ «Результат» |
| 2026-08-02 | **Очікування показу вирішує джерело вводу: протяг пальцем — показ негайно, енкодер / клавіша / тик / тиша — чекаємо цілого кадру.** Вікно «не чекати» — 1000 мс після останнього пакета дотику | Виведено з коду LVGL, а не з відчуття: інерція гасне як `v ← v·0.9` на кожному опитуванні сенсора, `scroll_throw` = 10 (`lv_hal_indev.h:33`), опитування раз на 30 мс (`colorlcd/lv_conf.h:105`), увімкнена типово (`lv_obj.c:452`) — найшвидший правдоподібний поштовх їде ≈900 мс. Перебрати майже безкоштовно: на вгамованій картинці `dirtyTiles` = 0 і до правила не доходить. Тик відокремлено від протягу порогом `LV_INDEV_DEF_SCROLL_LIMIT` = 10 px — тим самим, яким пульт відрізняє гортання від натискання | `webui/wait.js`, задача 0020 |
| 2026-08-02 | ⚠️ **Правило протягу лишається увімкненим типово, хоча сліпе порівняння різниці не показало — рішення людини «числа покращились учетверо, тоді на них орієнтуйся»** | Медіана затримки 463 → **115 мс**, показаних кадрів за 20 с 39 → **86**. Відчуття не розрізнило їх тому, що хвіст (90-й відсоток ≈920 мс) належить пульту й однаковий в обох режимах. Тобто механізм працює, а видимим стане, щойно зникне обмеження з боку пульта. Вимикач (`window.remoteUiDragRule`) лишається як прилад порівняння, кнопки в панелі не має навмисно: критерій сліпого досліду вимагає, щоб людина не знала режиму | задача 0020, критерії 2.1–2.3 |
| 2026-08-02 | Політика показу кадру винесена в окремий модуль без DOM (`webui/wait.js`) із близнюком на Python; дзеркальність стереже перехресна перевірка на спільних сценаріях | Заготовлені очікування перевіряють кожну реалізацію проти себе самої — правку в одному клієнті досить продублювати в його ж очікуваннях. У `tools/wait_crosscheck.py` очікуваних значень **немає взагалі**: еталон одного клієнта — другий клієнт, і розійтись потай неможливо за побудовою. Перевірено зламом: зміна одного числа в `wait.js` дала 87 розбіжних рядків зі 121 | `webui/wait.js`, `tools/wait_cases.json` |
| 2026-08-02 | ⚠️ **Виносити правила в окремий модуль недостатньо: обгортка навколо нього теж помиляється, і перехресна перевірка її не бачить за побудовою** | Рецензія 0020 знайшла очима справжню ваду: браузер гасив таймер відкладеного кадру, але не низку очікування в автоматі (клієнт ПК гасив обидва). Наслідок тихий — після кожного перепідключення перший неповний кадр ішов зшитим у лічильник «за строком», тобто домішка саме в те число, на якому стоять рішення про очікування. Звідси `webui/app_test.js`: заглушка DOM під node, 22 перевірки обгортки. Перевірено прямо — повернув ваду, тест упав трьома рядками | `webui/app_test.js`, рецензія задачі 0020 |
| 2026-08-02 | ⛔ **Плавний протяг пальцем недосяжний з боку клієнта — це геометрія прокрутки, а не вада коду. Питання закрито на етапі 2** | Гіпотезу «винен наш патч, бо задача транспорту витісняє малювання» відхилено **наявними числами**, без зміни прошивки: (1) на 921 600 транспорт мав би красти менше часу, отже малювання мало б прискоритись — частота не зрушила (4.2 проти 4.3 кадр/с); (2) під протягом пульт віддає **більше** плиток за секунду (190), ніж під енкодером (150), тобто там, де транспорту роботи більше, малювання встигає більше — протилежне до передбачення гіпотези. Правильне формулювання: пропускна здатність малювання приблизно стала (150–190 плиток/с), а частота кадрів = ця здатність, поділена на розмір кадру; під протягом кадр учетверо більший, бо рухається весь список, а не рамка виділення. Зрушити можна лише піднявши швидкість малювання EdgeTX на F429 або зменшивши площу перемальовки — обидва за межами етапу 2 | `tasks/0020-wait-by-input-source.md`, розділ «Рецензія» |
| 2026-08-02 | ⚠️ **Правило 0021 «ціна сторінки в купі моста = її вага на дроті» чинне лише до вбудовування gzip. Заміряно: сторінка 57.8 КБ з'їла 85.2 КБ купи** | Три холодні завантаження справжнім браузером дали `min_page` **21 276 / 21 276 / 20 504 Б** при межі тривоги 32 768 (передбачення минулої сесії — ≈29 000, тобто оптимістичне на третину); сторож спрацював у всіх трьох. Різницю 57.8 → 85.2 КБ пояснює не сума байтів, а **паралелізм**: браузер тягне файли шістьма з'єднаннями одночасно, і кожне бере власний буфер TCP. Наслідок на майбутнє: будь-яку оцінку «скільки коштує сторінка» рахувати через кількість одночасних з'єднань, а не через вагу | `tasks/0022-side-panels.md`, розділ «Додано 2026-08-02, сесія на стенді» |
| 2026-08-02 | Клієнт **мініфікується на збірці** (esbuild 0.25.10, закріплено точним числом + lockfile); джерело лишається читабельним і з українськими коментарями | 57 842 → **18 645 Б** gz (−68 %; `index.html` esbuild не чіпає — лише копіює, тож його 2 772 Б входять у суму без змін), тобто сторінка стала **легшою, ніж була до появи панелей** (43 380 Б). `min_page` 20 504 → **62 512 Б** у найгіршому з п'яти прогонів, сторож мовчить, запас майже подвійний. Прошивка моста схудла на 38 КБ, завантаження сторінки 331 → 175 мс. Збірка **без тихого відкату**: немає інструмента — `FATAL_ERROR` із назвою команди лікування, бо мовчазний відкат провалив би критерій так само тихо, як провалив його вперше | `webui/minify.mjs`, `firmware/esp32/main/CMakeLists.txt` |
| 2026-08-02 | Прогін клієнта **проти зібраної сторінки** став постійним інструментом, а не разовою перевіркою | `webui/*_test.js` читають джерело, а у флеш їде мініфікований файл — зламане мініфікатором проходило б повз зелені тести. `tools/webui_built_check.py` сам мініфікує, сам піднімає стенд без заліза (`fake_radio.py` + `webui_serve.py --dir`), проганяє справжній Chromium і гасить стенд у `finally`; 16 перевірок. Це четвертий за проєкт випадок шаблону «перевірка, яка не може провалитись», і перший, закритий інструментом, а не обіцянкою | `tools/webui_built_check.py` |
| 2026-08-02 | ⚠️ **Множення на кількість з'єднань лишилось нечіпаним і зараз спить: `max_open_sockets` = 4 × `CONFIG_LWIP_TCP_SND_BUF_DEFAULT` = 32 768** | Мініфікація зменшила **доданок**, а не множник; запас подвійний лише тому, що кожен файл тепер менший за буфер. Порядок важелів із рецензії 0021 (спершу обмежити з'єднання) лишається чинним і **свідомо відкладений**, а не скасований: той самий `SND_BUF` тримає й потік пікселів (78–88 КБ), і його ніхто не міряв від часу, коли він ставився. Тобто це заміряне питання з A/B пропускної здатності, а не побічна правка при нагоді | `firmware/esp32/main/ws_bridge.c:821`, рецензія 0021 |
| 2026-08-02 | ✅ **Утримання клавіші 10 с не розтискається само — доведено з екрана пульта, але не тим спостереженням, яке просили** | Автоповтору на `PAGE` в EdgeTX **немає взагалі**: утримання 9.8 с дало рівно те саме, що дотик 150 мс (дві зміни екрана), тож запропонована ознака двох пояснень не розрізняє. Сильніший доказ дав той самий дослід: `onInputState()` (`remote_ui/input.h`) заміщує маску клавіш **рівнем**, а клієнт шле `INPUT_STATE` безумовно раз на 250 мс — отже одне самовільне відпускання дало б перехід 0 → 1, тобто **нове** натискання й ще одне перемикання сторінки. За 9.8 с бачили рівно одне | `tasks/0022-side-panels.md`, критерій 4.2 |
| 2026-08-02 | **Введення тексту й чисел із фізичної клавіатури — аж у кінці етапу 3**, коли основа буде доведена | Рішення боса. Вимога не критична, а реалізація може зачепити прошивку пульта, тобто це не косметика клієнта, і починатись вона має з розвідки. Змішувати її з доведенням вигляду означало б поховати серйозне питання серед кольорів і відступів | `tasks/0023-panel-layout-and-look.md`, розділ «Обмеження» |
| 2026-08-02 | Розібрано за кодом, **скільки з'єднань буває насправді**: до 4 одночасно під час завантаження сторінки, 2 під час роботи (WebSocket + опитування раз на секунду) | Питання боса. Браузер хоче шість з'єднань на шість файлів, міст дає чотири (`ws_bridge.c:821`), решта чекає черги. Телефон обслуговується рівно один: новий WebSocket витісняє попередній. ⚠️ `SND_BUF` = 32 768 — це стеля невідправленого на з'єднання, а не заброньована пам'ять, тож найгірший випадок = 4 × найбільший файл: до мініфікації ≈ 115 КБ, після ≈ 35 КБ. **Умова, за якої небезпека прокидається: будь-який один файл знову підбирається до 32 КБ** | `docs/12-pipeline.md`, розділ «Скільки з'єднань буває насправді» |
| 2026-08-02 | Сцена клієнта — **одна сітка** з чотирьох учасників (панель, зображення, панель, смужка), а зсув групи рахує JS числом | Правило боса складається з двох половин, які CSS разом не виражає: панель стоїть **упритул** до зображення (тобто колонка сітки дорівнює самій картинці, а не вільному місцю) і зображення стоїть **по центру вікна**, доки місця вистачає. Вирівнювання групи дає другу половину лише поки панелі однакові: варто згорнути одну, і зображення поїхало б убік на пів різниці при цілком вільному місці. Тому `fitCanvas` рахує зсув і кладе його в `margin-left` лівої панелі | `webui/app.js`, `webui/style.css`, `tasks/0023`, критерії 2.4–2.5 |
| 2026-08-02 | ⚠️ **`min-width: max-content` у `.pad` — лікування заміряної вади, а не причісування** | Знайдено рецензією, відтворено заміром. Колонка панелі в сітці — `auto`, ширину сусідньої колонки ставить `fitCanvas` числом; поки те число ще старе (а воно старе рівно в мить, коли вікно щойно звузили), сітці нема звідки взяти місце, і вона стискає панель — `overflow: auto` у `.pad-body` обнуляє її власний мінімум. Далі `fitCanvas` читає стиснуту ширину й лишає зображення завеликим. Заміряно 900 → 760: клавіші **14 px** замість 84; на 640 — панелі по нулю й горизонтальна прокрутка | `webui/style.css`, `tasks/0023`, розділ «Результат» |
| 2026-08-02 | **Смужка стану згорнута за замовчуванням**, розгортається ручкою з обох боків; вимога 6.2 задачі 0022 переформульована з «видно завжди» на «видно за одне торкання» | Найдорожчий ресурс на телефоні — висота, і смужку зверху бос назвав першою серед незручностей. ⚠️ Сторож пам'яті моста з 0021 при цьому не сховався: при падінні нижче межі тривоги смужка розгортається **сама**, рівно один раз на подію. ⚠️ Засувка знімається, коли пам'ять повернулась вище межі **або коли міст мовчить п'ять періодів опитування поспіль** (≈5 с). Спокуслива й хибна проміжна правка — знімати її за першою пропущеною відповіддю: низька купа береться від тиску трафіку, і той самий тиск зриває опитування, тож смужка ставала б незакривною саме під навантаженням. Заміряно рецензією на першому виправленні | `webui/app.js`, `tasks/0023`, критерії 1.1 і 1.3 |
| 2026-08-02 | Запуск без браузерної обгортки — **описом застосунку зі значком**, а кнопка «Весь екран» лише там, де браузер це вміє | Наказати браузерові сховати адресний рядок сторінка не може за побудовою. Опис (`display: standalone`) працює і на Android, і на iPhone; ⚠️ **значок обов'язковий**: без нього Chrome робить не застосунок, а закладку, і вона відкривається в тій самій обгортці. На пристроях Apple повноекранного режиму немає взагалі, тому там кнопка **зникає**: мертва кнопка гірша за відсутню. Ціна — 512 Б значка й 193 Б опису на дроті | `webui/manifest.webmanifest`, `webui/README.md`, `tasks/0023`, критерії 1.4–1.5 |
| 2026-08-02 | ⚠️ **Усі кнопки в панелі однакової ширини; різницю між `SYS`/`MDL` і рештою несе колір, а не ширина** | Відхилення від слова «широкі» в критерії 4.4, і воно вимушене: критерії 2.1 (панелі рівні), 2.3 (кнопка джойстика завширшки як `MDL`) і 3.3 (усі кнопки однакової ширини) разом означають, що ширшої породи бути не може. Різниця лишилась помітною: вузькі — темні ребристі з неоновим написом, широкі — суцільні помаранчеві з чорним. Якщо бос мав на увазі саме різну ширину, це правка однієї змінної в CSS, але тоді скасовується 3.3 | `webui/style.css`, `tasks/0023`, розділ «Відхилення» |
| 2026-08-02 | Стеля кількості шляхів моста рахується **з самого переліку** (`sizeof(uris)/sizeof(uris[0])`), а не окремим числом | Доти поруч стояла примітка «додаєш шлях — звір із цим числом»; звіряти руками — те саме, що не звіряти, а перебір упав би не при збірці, а на старті моста в полі | `firmware/esp32/main/ws_bridge.c` |
| 2026-08-02 | ⛔ **Вирішувати за вільною пам'яттю «віддати файл зараз чи поставити в чергу» — не робимо. Ідея боса, відхилена за будовою сервера, а не за смаком** | `esp_http_server` створює на весь сервер **рівно одну** задачу (`httpd_main.c:526`, `httpd_os_thread_create(… "httpd" …)`), і вона по черзі обслуговує всі сокети — включно з WebSocket, яким ідуть пікселі й ввід. Отже «поставити в чергу» = приспати обробник = зупинити **весь** сервер, у тому числі ті передачі, завершення яких і мало б звільнити пам'ять, і потік пікселів заразом. Друга вада: перевірка перегони не знімає — чотири обробники можуть пройти її по черзі, поки жоден ще нічого не відправив, бо пам'ять витрачається **під час** відправлення, а не в мить рішення. Третя: відмова браузерові на файл сторінки не має безпечної відповіді — сценарій не перезапитують, сторінка лишиться напівзавантаженою. ⚠️ **Те, чого хотів бос, уже існує в готовому вигляді: `max_open_sockets`.** Черга при цьому робиться самим стеком — зайві запити чекають у черзі приймання, — без нового коду, без перегонів і без ризику зупинити сервер | `docs/12-pipeline.md`, розділ «Скільки з'єднань буває насправді» |
| 2026-08-02 | Критерій 3.2 задачі 0022 (пунктів меню за секунду) доміряється **заодно** з перевіркою вигляду в 0023, окремого підходу до стенда не варт | Рішення боса. Число ні на що не впливає: прокрутка впирається не в клацання клієнта (14/с), а в швидкість малювання пульта. Не вийде заодно — питання йде в кінець черги й може не знадобитись узагалі | `tasks/0023-panel-layout-and-look.md`, критерій 6.3 |
| 2026-08-03 | ⚠️ **Мінімуми за етапами (`min_page`, `min_stream`) монотонні від старту моста; `POST /api/heap/reset` їх не скидає** | Знайдено при закритті критерію 5.4 задачі 0023 читанням коду, а не припущенням: `window_reset_now()` (`heap_watch.c:279`) чіпає лише вікно й перевірку на витік, а `s_min_phase[]` обнуляється тільки в `heap_watch_init`. Наслідок для замірів: число за етапом — це «найгірше, що бачив міст від увімкнення», і порівнювати його можна лише з таким самим. Заразом це означає, що для чистого заміру етапу міст треба **перезавантажити**, а не скинути вікно | `firmware/esp32/main/heap_watch.c`, `tasks/0023`, розділ «Заміри» |
| 2026-08-03 | ✅ **Сторінка з новими панелями коштує мостові менше, ніж коштувала до них: `min_page` 66 324 Б проти 62 512 у 0022** — при межі тривоги 32 768 | Заміряно трьома холодними завантаженнями справжнім браузером на щойно перезавантаженому мості. ⚠️ Сторінка при цьому **поважчала** на 10 % (18 645 → 20 517 Б gz), тобто в купі відкладається не сума байтів, а найбільший **окремий** файл на з'єднання — а він не виріс. Сусідні етапи: один клієнт із потоком 80 768, два клієнти, що витісняють одне одного, 78 956, завантаження поверх живого потоку 82 376 | `tasks/0023`, розділ «Заміри» |
| 2026-08-03 | ⛔ **Просідання купи моста до 22 160 Б лишилось непоясненим; гіпотезу «винні два клієнти» спростовано заміром** | Спостерігалось один раз, у першому сеансі після прошивання (міст 1128 с, вкладка боса плюс прогони приладу), сторож спрацював. Три навмисні спроби відтворити дали 78 956–82 376, тобто вдвічі більше за межу тривоги. Записано як невідоме, а не заметено. ⚠️ **Умова відкриття наново:** `min_stream` або `min_window` знову нижче 32 768 при **одному** клієнті; дивитись тоді в розклад «хто з'їв» у ту саму мить | `tasks/0023`, розділ «Заміри» |
| 2026-08-03 | ⚠️ **Прилад перевірки клієнта має ганятись і проти живого моста, а не лише проти стенда: дві його вади не могли проявитись без заліза** | Перша: опис застосунку не розбирався, бо міст ставить `Content-Encoding: gzip` **безумовно**, а стенд віддає нестиснене. Друга: прилад відкривав свіжі вікна, не відпустивши головного, — на мості, що обслуговує рівно одного клієнта, вони витісняли одне одного, і жодне не доживало до `HELLO`; двійник пульта це ховав, бо відповідає миттєво. Після виправлень 72 перевірки проти моста, нуль невдалих | `tools/webui_built_check.py` |
| 2026-08-03 | ✅ **Пунктів меню за секунду рівно стільки, скільки клацань: ≈6 за першу секунду утримання, ≈14/с усталено** — закриває критерій 3.2 задачі 0022, останній її відкритий | Заміряно через клієнта на живому пульті (сторінка `Hardware`, курсор ходить без редагування): п'ять окремих натискань — п'ять пунктів, колесо з проміжками 500/200/120/70 мс — п'ять із п'яти в кожному, утримання 1 с — 6 клацань і 6 пунктів. Отже прискорення EdgeTX додає крок **значенню**, яке редагують, а курсору в списку — ні. ⚠️ **Пастка заміру:** перший дослід (утримання 2 с, 21 клацання) показав зсув на **один** пункт і виглядав як важка вада — насправді список **закільцьований** і клацання обійшли його по колу. «Положення до / після» в кільцевому списку не є заміром, поки кроків менше за довжину списку | `tasks/0023`, розділ «Заміри» |
