# Поточний стан

> Оновлюється командою `/handoff`. Це знімок стану, а не журнал.
> Історія — у `state/handoffs/`.

**Етап:** 0 — закрито (обидві задачі прийняті рецензією). Тепер етап 1
**Статус:** задача 0003 (симулятор) створена, роботи над нею ще не було
**Оновлено:** 2026-07-25

## Фокус зараз

Етап 0 пройдено повністю й прийнято. Пульт працює на **EdgeTX 2.12.2
(3673c518)** — тій самій версії, проти якої пишеться патч. Картка — пакет
2.12.1. Калібрування, моделі й налаштування пережили оновлення без втрат.
Екран пульта не знадобився жодного разу за обидві задачі.

Шляхи назад перевірені й лежать на диску: еталон 2.8.0
(`bab1d2a3…`) з копією старої картки і еталон 2.12.2 (`7b6d1ce1…`).

Побічно виміряно USB-джойстик на 2.12.2: 8 осей, 24 кнопки, діапазон
0..2048 (`tools/jsinfo.py`, `tools/axprobe.py`). Придатний як незалежний
канал читання стану пульта з ПК — деталі в `state/DECISIONS.md`.

Наступний крок — задача **[0003](../tasks/0003-simulator-build.md)**: зібрати
симулятор цілі `X10` (SDL2) і переконатися, що він працює. `upstream/edgetx`
уже стоїть на тезі `v2.12.2` (коміт `3673c51` — той самий хеш, що в
прошивці пульта). Роботи над задачею ще не починали.

## Прогрес по етапах

- [x] Етап 0 — прошивка наосліп і процедура відкату
- [ ] Етап 1 — розробка в симуляторі
- [ ] Етап 2 — перенесення на залізо (MVP 1 і 2)
- [ ] Етап 3 — бокові панелі керування (MVP 3)
- [ ] Етап 4 — стабілізація і безпека польотів
- [ ] Етап 5 — розширення

## Заблоковано

Нічого.

## Перевірити пізніше (не блокує)

- **Етап 2.6:** у системі, ймовірно, стоїть і розширення ESP-IDF для
  VS Code, і PlatformIO. Перед написанням моста пройти чек-ліст у
  `docs/10-arch-linux.md`, прибрати глобальний `IDF_PATH` і обрати одного
  власника збірки.

## Відкриті питання до людини

Немає. Усі стартові питання закрито 2026-07-25.

**Зафіксовано:**
- EdgeTX на пульті — **2.12.2 (3673c518)**, залито 2026-07-25 замість
  заводської 2.8.0. `upstream` і `setup-upstream.sh` закріплені на тезі
  `v2.12.2`, не на рухомій гілці.
- Роз'єм AUX1 — крок 2.54 мм, підходять звичайні перемички Dupont.
- Середовище моста — чистий ESP-IDF (ADR-0004, замінює ADR-0001).
- Плати — DevKit v1 для розробки, C3 SuperMini кандидат у корпус (ADR-0003).
- Швидкість каналу — 921600 бод.
- Ризик завад знято: використовується зовнішній модуль через JR-відсік.
- ОС — Arch Linux, середовище описане в `docs/10-arch-linux.md`.
