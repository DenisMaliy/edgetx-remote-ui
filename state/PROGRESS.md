# Поточний стан

> Оновлюється командою `/handoff`. Це знімок стану, а не журнал.
> Історія — у `state/handoffs/`.

**Етап:** 3 (бокові панелі — MVP 3)
**Статус:** ⚠️ **Пауза: бос користується пультом і повернеться з
враженнями.** Задачі 0022–0025 прийняті всі, три цілі MVP досягнуті. Нових
задач до повернення боса не оформлювати — деталі й перелік того, що варто
помітити, у `state/ASSISTANT.md`, розділ «Пауза: бос користується пультом».
**Оновлено:** 2026-08-03

---

# ✅ Місце під екран тримається до першого кадру

Задача [0025](../tasks/0025-reserve-screen-before-hello.md). Розмір екрана
приходить лише в `HELLO`; до нього `fitCanvas()` виходила одразу, і
розкладка схлопувалась до приходу першого кадру — панелі збивались до
краю, вікно черги висіло у вузькій смужці.

Клієнт тепер **пам'ятає** останній відомий розмір (`localStorage`), а не
припускає його: зашити 480×272 заборонено головним правилом проєкту. Коли
не пригадав нічого (перший у житті візит) — під зображення йде все вільне
місце, без вигаданої пропорції.

| | до з'єднання | з пультом |
|---|---:|---:|
| альбомна, друге відкриття | 680×385 | 680×385 |
| книжкова, друге відкриття | 420×238 | 420×238 |

Було до задачі: **16×9** замість 680×385.

⚠️ **Рецензія коду знайшла ваду, гіршу за ту, яку лікували, — до коміту.**
Сміття у `localStorage` (число на кшталт «999999999999») давало розкладку
на 33 мільйони пікселів, і виходу з неї не було: значення лишається у
сховищі й повертається при кожному відкритті, доки не почистити дані
сайту вручну. Виправлено: приймаються рівно цифри в межах правдоподібності
`HELLO` (`uint16`).

Перевірок 157 на стенді (було 136), 151 проти живого моста — невдалих 0.
`app_test.js` 70 (було 56). Сторінка на дроті 21 931 → 22 148 Б gz (+1 %).

⚠️ **Лишились два питання, обидва — не вади.** У книжковій орієнтації
зарезервоване місце стоїть на 11 px вище, ніж стане екран (панель порожня
до `HELLO`, тобто нижча за ту, що буде з клавішами) — асистент прийняв
пояснення й лишив як є. На перший у житті візит у книжковій площа помітно
схлопнеться, щойно прийде `HELLO` (420×826 → 420×238) — свідомий вибір,
чекає ще одного погляду боса.

⚠️ **Побічне спостереження, не про цю задачу.** Перед прошиванням міст
після 1 год 49 хв роботи (кілька клієнтів, прохід боса на двох пристроях)
показував `min_window` 23 336 при межі тривоги 32 768 — сторож спрацював.
Це вже після черги з 0024. Лічильники сеансів до прошивання не зняли, тож
число не розрізняє «один клієнт» і «багато». Умова відкриття наново та
сама: нижче 32 768 при **одному** клієнті.

---

# ✅ Гойдалки більше немає: 57 витіснень за хвилину → нуль

Задача [0024](../tasks/0024-single-active-client.md). Домовленість «хто
господар» переїхала з мовчазного витіснення в **явну заявку**: прибулець
називає себе в рядку запиту `/ws`, а зайнятий міст відповідає словом
`{"busy":1}` і гасить сокет. Клієнт після цього **не повертається** — показує
«Є активне підключення до пульта» з кнопкою «Перейняти керування» і чекає,
опитуючи `/api/status` раз на дві секунди й тільки поки вкладку видно.

| два вікна поруч, 60 с | сеансів | витіснень |
|---|---:|---:|
| стара прошивка | 58 | **57** |
| нова | **0** | **0** |

⚠️ **Черга сама по собі вади не лікувала, і це найдорожча знахідка задачі.**
Перший прогін нової прошивки провалився так само, як стара: **одне вікно
клієнта тримало всі чотири гнізда сервера** (браузер лишає з'єднання
відкритими, тайм-ауту простою в `esp_http_server` немає). Другому вікну гнізд
не лишалось, `httpd` при `lru_purge_enable` гасив найдавніший сеанс — тобто
WebSocket першого клієнта. Лікування: сторінка й `/api/status` віддаються з
`Connection: close`. Одне вікно тепер тримає два гнізда замість чотирьох, а
клієнт у черзі — **жодного**.

⚠️ **`resume` («це знову я») діє лише коли господаря не чути 750 мс.** Без цієї
половини умови клієнт витісняв сам себе: ім'я живе одне завантаження сторінки,
тож збіг при живому господарі означає власний другий сокет. Знайдено
рецензією, не заміром.

⚠️ **На дріт пускається тільки господар** — один рядок у `ws_handler`. Доти
негосподарських сокетів не існувало взагалі; тепер їх два види, і обидва
встигали дописати пультові, зокрема повернути натиснуту клавішу вже **після**
обнулення вводу.

Час до першого кадру в одинокого клієнта не змінився: **0.29–0.39 с**.
Сторінка 20 974 → **21 821 Б** gz. Перевірок: прилад 136 на стенді й 130
проти живого моста, `app_test.js` 56 (було 38).

---

# ✅ Панелі знову ростуть від зображення, а не від краю вікна

Розділ 9 задачі [0023](../tasks/0023-panel-layout-and-look.md), за третім колом
відгуку боса. ⚠️ **Це повернення поведінки, яку розділ 8 переписав, хоч ніхто
не просив** — саме той випадок, заради якого в `CLAUDE.md` з'явився розділ
«Виправлення не переписує погоджене».

| стан панелей | верх ручок, було | стало |
|---|---:|---:|
| усі згорнуті | 816 (діра **267 px**) | під картинкою, діра **0** |
| смужка розгорнута | 588 (діра 39) | діра **0** |
| усі розгорнуті | 520 | діра **0** |

Надлишок місця тепер лежить **під** панелями. Центрування з 8.1 ціле: при всіх
згорнутих 311 px згори і 311 знизу — те саме число, що до правки. Товщина
бруска ручки зведена до однієї змінної `--rail` = 26 px на обидві орієнтації.

Перевірок **120** (було 110), невдалих 0. **Проти живого моста — 114, невдалих
0.** Сторінка 20 900 → **20 974 Б** gz.

⚠️ **Природне рішення виявилось вадою в обчисленні.** «Обидва ряди `auto`,
вільне місце під ними» на вигляд працює, але в двох `auto` рядах сітка стискає
саму панель (`min-height: 0` + `overflow: auto` у `.pad-body`), і `fitCanvas`
читає стиснуту висоту замість природної — обчислення стає самозалежним і
розкладка гуляє без упину. Прогін падав **тайм-аутом «element is not stable»**,
а не червоною перевіркою. Робоче рішення лишає сітку недоторканою: надлишок
віддається нижньому ряду як `margin-bottom`, рахований числом.

⚠️ **Три прогалини в перевірках знайшла мутація, не око.** Дві мої власні
перевірки не могли спрацювати взагалі (плашки смужки обрізаються, а не
переносяться; зонд ішов у ліву панель, тоді як висоту ряду задає права — у ній
джойстик), а мутація «зсув не знімається при поверненні в альбомну» проходила
зеленою через увесь прогін.

⚠️ **Прогін проти живого моста впав п'ять разів поспіль, і жоден раз не через
клієнта.** Міст обслуговує рівно одного клієнта, і слот звільняється не миттєво:
свіже вікно падало за 25 с очікування `HELLO`, тоді як одиночне отримувало його
за **0.3 с**. Стан моста деградує з накопиченням сесій — при `evicted` 215
прогін не дійшов і до половини, при 60 пройшов цілком. Окремо: **сторож пам'яті
сам розгортає смужку** (так задумано в 0021), і це валило «смужка згорнута за
замовчуванням», доки `min_window` не повернувся з 32 428 до 51 816 після
перезавантаження моста.

⚠️ **Wi-Fi ПК після перезавантаження моста тримає стару асоціацію** — SSID і
адреса на місці, жоден пакет не проходить. Лікується перепідключенням.

⚠️ **Прошивання знову пішло лише з `-b 115200` — сьомий випадок поспіль.**
Кабель не міняли, тож гіпотеза про хибно виміряний кварц лишається
неперевіреною.

---

# ✅ Зображення екрана стоїть по центру вікна — і в книжковій теж

Розділ 8 задачі [0023](../tasks/0023-panel-layout-and-look.md), за другим
колом відгуку боса. ⚠️ **Головне тут не нові вимоги, а невиконаний критерій
3.1:** центрування в альбомній рахував `fitCanvas` числом, а в книжковій його
не рахував **ніхто** — стояв один рядок CSS «притиснути до панелей». Доки
панелі розгорнуті, притиснуте й відцентроване збігаються з точністю до
пікселя, тому вада була видима рівно в одному стані — коли всі панелі
згорнуті, — а перевірка міряла типовий.

| стан панелей | згори | знизу |
|---|---:|---:|
| **усі згорнуті** | **311** | **311** |
| розгорнуті | 282 | панель упритул |

**Ряд ручок згортання:** три ручки в книжковій в один ряд, спільне правило
оформлення, ширина від панелі, а не від вмісту (80 / 248 / 80 при панелях
84 / 252 / 84 — і в згорнутому стані те саме). ⚠️ **Ручка вужча за панель на
4 px**: буквально «ширина ручки = ширина панелі» (8.2) і «між ручками є
відступ» (8.4) разом нездійсненні, поки панелі впритул (3.5). Вибрано видиму
межу.

Перевірок **110** (було 90), невдалих 0. Сторінка 20 859 → **20 900 Б** gz.

⚠️ **Рецензія коду знайшла чотири блокери, і всі чотири — у приладі, а не в
клієнті.** Найгірший: друга перевірка 8.1 була **алгебраїчним переказом
першої** — червоної при зеленій першій не існувало. Ще дві трималися на
запасі в **29 пікселів**: стара 3.1 стверджувала «панель упритул завжди», що
критерій 8.1 якраз і скасував, і зеленою була випадково — чужий пульт із на
одну клавішу коротшою панеллю зробив би її червоною на справному коді.

⚠️ **Прошивання моста на типовій швидкості впало вшосте поспіль — але тепер є
число.** У журналі esptool: `Detected crystal freq 15.32MHz ... normalized
freq 26MHz`. Кварц вимірюється **до** зміни швидкості, на вже робочому
з'єднанні 115200, тож помилка в цьому числі й пояснює, чому будь-яка зміна
швидкості мовчить: дільник рахується від хибної частоти. Кабель це пояснити
не може — на 115200 пройшло 834 КБ із звіреним хешем. Гіпотезу боса це не
знімає остаточно, але тепер у неї є перевірка на одне число: якщо після
заміни кабелю в журналі лишиться ті самі 15.32 МГц — кабель ні до чого.

---

# ✅ Панелі стали схожі на пульт, а висота повернулась сторінці

Задача [0023](../tasks/0023-panel-layout-and-look.md) — за зауваженнями боса
з 0022. Сцена клієнта тепер **одна сітка** з чотирьох учасників: панель,
зображення, панель і смужка стану.

- **Смужка згорнута за замовчуванням**, у згорнутому стані займає **0 px**.
  Розгортається ручкою `≡` з обох боків — вимога 6.2 задачі 0022
  переформульована з «видно завжди» на «видно за одне торкання».
- Окремої кнопки під смужку немає: у ручок бічних панелей відібрано **чверть**
  (256.5 / 85.5 px, рівно втричі).
- Панелі **однакові** (110 = 110) і стоять **упритул** до зображення (0 px),
  а зображення — по центру вікна, доки місця вистачає.
- Книжкова: усі кнопки в одну колонку, три панелі в сумі **84 + 252 + 84 =
  420** при вікні 420.
- Кольори: тло `rgb(0,0,0)`, написи `rgb(255,125,26)`, `SYS`/`MDL` навпаки —
  помаранчеві з чорним написом.
- **Запуск без браузерної обгортки:** `manifest.webmanifest`, значок 512 Б,
  мітки для iPhone; кнопка «Весь екран» **зникає** там, де браузер цього не
  вміє.

**Дописане босом після проходу (розділ 7):** синьої рамки фокуса навколо
зображення немає — про фокус каже плашка «клавіатура ✓»; у книжковій смужка
має власну ручку, а бічні ручки більше не діляться на чверті й обидві по
44 px; відступи мінімальні — 4 px.

Сторінка на дроті **18 645 → 20 859 Б** gzip (+11.9 %). Перевірок у
`tools/webui_built_check.py` — **90** (було 16), усі зелені; проти живого
моста — 42.

⚠️ **Три перевірки розділу 7 спершу не могли провалитись** — знайшла рецензія,
не я. Кожну виправлено й перевірено мутацією. Це вже **четверта** залежність
цього приладу від порядку кроків: перевірка справджувалась не тому, що код
правильний, а тому, що попередня перевірка залишила потрібний стан.

⚠️ **Дві заміряні вади знайшла рецензія, не мої перевірки**, і обидві мали
той самий вигляд — **коментар обіцяв гарантію, якої в коді немає**:

1. **Звуження вікна розчавлювало панелі.** Сітка стискала колонку панелі під
   ще не виправлену ширину зображення, `fitCanvas` читав стиснуте число й
   лишав зображення завеликим. 900 → 760 давало клавіші по **14 px**, 640 —
   панелі по нулю й горизонтальну прокрутку. Лікування — `min-width:
   max-content` у `.pad`.
2. **Сторож пам'яті замовкав від блимання зв'язку.** Засувка «тривога один
   раз на подію» гасла від будь-якої пропущеної відповіді моста — а низька
   купа береться від тиску трафіку, і той самий тиск зриває опитування. Тепер
   засувка знімається лише після п'яти мовчазних періодів поспіль.

**Міст прошито, звірено байтами, і критерій 5.4 закритий заміром** (2026-08-03).
⚠️ Прошивання знову пішло лише з `-b 115200` — **третій випадок поспіль**.

| три холодні завантаження | `min_page` |
|---|---:|
| прогін 1 | 78 464 |
| прогін 2 | **66 324** |
| прогін 3 | 66 324 |

Межа тривоги 32 768, у 0022 було 62 512 — тобто **краще, ніж було**, хоча
сторінка поважчала на 10 %. Сторож мовчить. ⚠️ Числа монотонні за побудовою:
мінімуми за етапами живуть від старту моста, `POST /api/heap/reset` їх не
чіпає (`heap_watch.c:157`).

Сусідні етапи, яких критерій не просив: один клієнт із потоком — 80 768,
два клієнти, що витісняють одне одного — 78 956, завантаження сторінки поверх
живого потоку — 82 376.

⚠️ **Одне просідання лишилось непоясненим.** У першому сеансі після
прошивання `min_stream` показав **22 160 Б** і сторож спрацював. Три навмисні
спроби відтворити (рядок вище) не дали нічого — тобто найпростіше пояснення
«винні два клієнти» **заміром спростоване**. Умова відкриття наново:
`min_stream` або `min_window` знову нижче 32 768 при **одному** клієнті.

⚠️ **Прогін проти живого моста знайшов дві вади в самому приладі**, і жодна не
могла проявитись на стенді: опис застосунку не розбирався (міст ставить
`Content-Encoding: gzip` безумовно), а сам прилад відкривав свіжі вікна, не
відпустивши головного, — на мості, що обслуговує рівно одного клієнта, вони
витісняли одне одного до нескінченності. Після виправлень: **72 перевірки
проти живого моста, нуль невдалих**, панелі побудовані зі справжнього `HELLO`.

⚠️ **Пастка стенда:** одразу після перезавантаження моста перший клієнт
`HELLO` не отримує, наступний — отримує. Спіймано двічі з двох.

---

# ✅ Скільки пунктів меню за секунду — заміряно; 0022 закрита цілком

Критерій 6.3 задачі 0023, він же **3.2 задачі 0022**, останній її відкритий.
Заміряно через клієнта (екран пульта видно в браузері), сторінка `Hardware` —
курсор по ній лише ходить, значень не змінює.

| дослід | клацань | пунктів меню |
|---|---:|---:|
| п'ять окремих натискань джойстика | 5 | **5** |
| колесо миші, проміжки 500 / 200 / 120 / 70 мс | 5 | **5** у кожному |
| **утримання 1 с** (наш розгін) | **6** | **6** |

**Пунктів за секунду стільки ж, скільки клацань:** ≈6 за першу секунду
утримання, ≈14/с усталено. Прискорення EdgeTX додає крок **значенню**, яке
редагують, а курсору в списку — ні.

⚠️ **Пастка заміру, у яку я встиг потрапити.** Перший дослід — утримання 2 с,
21 клацання — показав зсув на **один** пункт і виглядав як важка вада клієнта.
Насправді **список закільцьований**, і клацання обійшли його по колу плюс
один. «Положення до / положення після» в кільцевому списку не є заміром, поки
кроків менше за довжину списку; коротше утримання розрізнило два пояснення за
хвилину.

---

# ✅ Третя ціль MVP досягнута: пультом користуються з телефона

Задача [0022](../tasks/0022-side-panels.md). Джойстик і клавіші в клієнті,
правило «намір, а не клавіша»: панель каже «вгору», виконавця (клавіша чи
енкодер) дає `HELLO`. Вліво-вправо не виконує ніхто — рішення людини від
03.08 виконане буквально.

Сценарій пройдено босом з телефона й з ПК (критерії 7.1–7.3, коміт
`eed90eb`, відгук — у розділі «Рецензія» задачі). ✅ **Останній відкритий
критерій 3.2 закритий 2026-08-03** — розділ вище: пунктів меню за секунду
стільки ж, скільки клацань.

**Критерій 0.1 закритий відповіддю, протилежною до припущення задачі:**
регресії зі стрілками не було — клавіатури в браузерному клієнті не існувало
ніколи (`git log --all -S 'keydown'` = нуль комітів). Стрілки, які пам'ятала
людина, — з `tools/test_client.py` (клієнт ПК), і вони й досі працюють.

Побічний продукт: `tools/fake_radio.py` — програмний двійник **пульта**;
разом із `webui_serve.py` дає повний стенд без заліза.

---

# ⚠️ Ціна сторінки в купі моста більше не дорівнює її вазі на дроті

Задача 0022, критерій 8.3, заміряно на стенді. Правило з 0021 **чинне лише
до вбудовування gzip**:

| | вага на дроті | з'їла з купи |
|---|---:|---:|
| сторінка з панелями | 57.8 КБ | **85.2 КБ** |

Причина не в сумі байтів, а в тому, що браузер тягне файли **кількома
з'єднаннями одночасно**, і кожне бере свій буфер TCP.

**Критерій 8.3 спершу провалено, потім закрито.** Три холодні завантаження
справжнім браузером дали **20 504 Б** вільної купи при межі тривоги 32 768 —
гірше за передбачення (≈29 КБ), і сторож спрацював щоразу.

**Лікування — мініфікація клієнта на збірці** (коміт `4ab1ff1`, esbuild
0.25.10, версія закріплена точним числом). Джерело лишається читабельним:
українські коментарі не ріжуться, вони зникають лише з примірника у флеші.

| | вага gz | min_page |
|---|---:|---:|
| до 0022 | 43 380 | — |
| з панелями | 57 842 | **20 504** ⛔ |
| **після мініфікації** | **18 645** | **62 512–85 764** ✅ |

⚠️ **Виняток — `index.html`:** мініфікатор його лише **копіює** (esbuild HTML
не вміє), тож коментарі саме цього файлу їдуть у флеш і коштують місця в
купі. Розлогий абзац пояснення коштував там 236 Б на дроті.

⚠️ **Збірка не має тихого відкату:** немає `node`, немає esbuild або
розійшлася його версія — `FATAL_ERROR` із командою лікування. Мовчазний
відкат зібрався б і провалив 8.3 так само тихо, як провалив його вперше.
**Для збірки моста тепер потрібні `node` і `pnpm -C webui install`.**

⚠️ **Зелені тести перевіряють не той файл.** `panels_test.js`, `app_test.js`,
`proto_test.js` читають джерело, а у флеш їде мініфікований примірник. Для
зібраного є `tools/webui_built_check.py` — звіряє байти всіх шести файлів із
тим, що віддає ціль; працює і проти стенда без заліза, і проти живого моста
(`--url`).

**Наступний важіль, якщо запас знадобиться:** `max_open_sockets = 4` ×
`CONFIG_LWIP_TCP_SND_BUF_DEFAULT = 32768`. Мініфікація зменшила доданок,
множник цілий. Не чіпалось свідомо: `SND_BUF` тримає й потік пікселів, а
заміряного приводу зараз немає.

---

# ⚠️ Автоповтору на `PAGE` в EdgeTX немає — і критерій 4.2 доводиться інакше

Задача 0022. Утримання `PAGE>` **9.8 секунди** дало рівно те саме, що дотик
150 мс: дві зміни екрана й тиша далі. Тобто спостереження «дивись, чи веде
EdgeTX автоповтор» двох пояснень не розрізняє.

**Доказ узято з того самого досліду, іншим міркуванням.** `onInputState()`
заміщує маску клавіш **рівнем** кожні 250 мс. Якби пульт хоч раз відпустив
клавішу, наступний пакет виставив би її знову — а це для `keysPollingCycle()`
перехід 0 → 1, тобто **нове натискання**, і сторінка перемкнулася б удруге.
Було рівно одне перемикання за 10 с при щільності зразків 200 мс.

**Отже пульт тримав клавішу всі 10 секунд без жодного самовільного
відпускання.** Критерій 4.2 закрито каналом, який просив бос, — з екрана
пульта.

---

# ✅ Затримка й кадри не погіршали, сторож мовчання не спрацював

Критерії 8.1 і 8.2, ті самі жести, що в 0020, швидкість 2 625 000:

| орган керування | кадр/с (0020) | кадр/с (зараз) |
|---|---|---|
| спокій | 0.0 | 0.0 |
| протяг пальцем | 2.6–4.3 | **4.3** |
| енкодер 8 клац/с | 13.6 | **13.3** |

Затримка «ввід → показаний кадр» на енкодері: медіана **131 мс** проти 115
(⚠️ на 14 % гірше), 90-й відсоток **229 мс** проти ≈920 (учетверо краще),
максимум 260. Кадрів цілих 148 зі 148.

`silence_resets` 0, `silence_timeouts` 0, найбільший проміжок вводу 266 мс.
⚠️ **Умова відкриття закритої задачі 0015 не настала.**

---

# ✅ Пам'ять моста: винна була сторінка клієнта, а не пікселі

Задача [0021](../tasks/0021-bridge-heap.md). Просідання виявилось **не
поступовим стисканням, а сходинкою на частку секунди**, і належала вона тим
кільком сотням мілісекунд, коли міст віддає браузеру 122 КБ сторінки.

⚠️ **Ціна сторінки в пам'яті моста дорівнює її розміру на дроті.** Заміряно
на незміненій прошивці, кожен дослід із чистого завантаження:

| дослід | віддано | мінімум купи |
|---|---:|---:|
| нічого | 0 | 102 388 |
| `proto.js` | 30 323 | 67 080 |
| `app.js` | 67 358 | **41 724** |
| сторінка цілком у браузері | ~122 500 | **2 500** |
| **+ 20 с протягу пальцем** | 3 631 плитка | **2 500** (не зрушив) |

**Лікування — стиснути клієнта, а не буфери.** Вбудовується gzip на збірці
(122 КБ → 41 КБ), розпаковує браузер; у мості нуль рядків на розпакування.
Заразом прошивка схудла на 80 КБ.

| | мінімум вільної купи |
|---|---:|
| до | **2 500 Б** |
| після | **38 976 Б** |
| ціль (одне вікно передачі TCP) | 32 768 Б |

⚠️ **Найгірший випадок переїхав.** Після стиснення найглибша точка — вже не
сторінка (43 428), а **потік пікселів під енкодером** на 85 КБ/с (38 976).
Наступний важіль, якщо запас знадобиться, — `CONFIG_LWIP_TCP_SND_BUF_DEFAULT`
(32 КБ на кожен сокет); його ніхто не міряв від часу, коли він ставився.

**Витоку немає:** за 11 хвилин під навантаженням рівень, на який купа
повертається, — 106 408 → 106 712 Б, тобто не тане. `crc_errors`,
`uart.dropped`, `ws.tiles_dropped` — нулі весь прогін.

⚠️ **Перевірка на витік зроблена не так, як просила задача, і це навмисно.**
Мінімум у хвилині каже, **чи трапилась у ній подія**, а не чи тане запас: у
першому ж прогоні мінімум упав зі 103.9 до 40.0 КБ просто тому, що
завантажилась сторінка. Судиться за **рівнем повернення** (найбільше вільне
за хвилину). Обидва числа лишились у `/api/stats`.

**Сторож, щоб це не повторилось тихо:** плашка «пам'ять» у смужці клієнта
(не в панелі — число, по яке треба лізти, знаходять запізно), розклад «хто
з'їв» за етапом, `POST /api/heap/reset` для нового вікна без перезавантаження
моста, і одноразова скарга в журнал нижче 32 КБ.

---

# ⛔ Плавний протяг пальцем недосяжний з боку клієнта. Питання закрито

Задача [0020](../tasks/0020-wait-by-input-source.md), заміри на стенді.
Число з моста (`uart.frames`) від нашої політики показу не залежить:

| орган керування | пульт віддає | плиток/с | КБ/с | плиток на кадр |
|---|---|---|---|---|
| спокій | 0.0 кадр/с | 0 | 0.5 | — |
| **протяг пальцем** | **2.6–4.3** | 114 | 41 | **44** |
| енкодер 8 клацань/с | **13.6** | 149 | 54 | 11 |

Дріт при цьому на **16–21 %** ємності. Плиток за секунду пульт віддає
приблизно однаково, але під протягом складає їх у вчетверо більші кадри —
рухається весь список, а не рамка виділення. **Впирається малювання.**

⚠️ **Гіпотезу «винен наш патч» відхилено рецензією 0020 наявними числами** —
міняти прошивку для цього не знадобилось. Якби транспорт крав час у
малювання, на 921 600 він крав би менше, а частота не зрушила (4.2 проти
4.3 кадр/с). І сильніше: під протягом пульт віддає **більше** плиток за
секунду (190), ніж під енкодером (150) — там, де транспорту роботи більше,
малювання встигає більше.

**Правильне формулювання:** пропускна здатність малювання приблизно **стала**
(150–190 плиток/с), а частота кадрів = ця здатність, поділена на розмір
кадру. Під протягом кадр учетверо більший, бо рухається весь список. Це не
вада ні в нашому коді, ні в EdgeTX — **це геометрія прокрутки**.

⚠️ **Наслідок для планування:** плавний протяг пальцем **недосяжний з боку
клієнта**. Зрушити можна лише піднявши пропускну здатність малювання в EdgeTX
на F429 або зменшивши площу перемальовки — обидва за межами етапу 2.

**Швидкість каналу впливає на два органи по-різному.** Для протягу майже
байдужа (4.2 на 921 600 проти 4.3 на 2 625 000). Для енкодера вирішальна: на
921 600 дріт іде на 79 % і **34 % кадрів показуються зшитими**, на 2 625 000 —
жодного.

**Клієнт тепер сам просить найвищу швидкість** при під'єднанні, одна спроба.
Стабільне джерело хибних скарг «знову рве» закрите: після перезапуску стенда
міст піднімається на 921 600 і мовчки переїжджає на 2 625 000.

✅ **Пастка стенда закрита в 0021.** `webui_drive.py` тепер зачиняє браузер
через `try/finally` — хоч по `quit`, хоч по Ctrl-C, хоч через помилку. Раніше
виходу, крім `quit`, не існувало, і драйвер від попередньої сесії прожив
1 год 14 хв, мовчки ділячи дріт із наступним заміром.

✅ **Питання про 1.9 КБ вільної пам'яті закрите в 0021** — розділ вище. Винною
була віддача сторінки клієнта, не потік пікселів.

---

# ⚠️ Швидкість каналу — умова цілісності картинки, а не зручність

Задача [0019](../tasks/0019-frame-completeness.md), закриття боргів.
Заміряно свіжим клієнтом на кожну швидкість, тим самим жестом, підряд:

| швидкість | потік | кадрів | цілих | за строком |
|---|---|---|---|---|
| 2 625 000 | 142 КБ/с | 137 | **137** | **0** |
| 921 600 | 69 КБ/с | 121 | 75 | **46 (38 %)** |

**«100 % цілих кадрів» — властивість 2 625 000, а не механізму 0019.**
Підтверджено оком людини **наосліп**: із двох прогонів вона назвала фризливим
рівно той, що йшов на базовій швидкості.

⛔ **Після кожного перезапуску стенда пульт удома на 921 600.** Поки клієнт не
замовив 2 625 000, людина бачить шви — і це не регресія. Дивитись у плашку
**перед** заміром і перед скаргою «знову рве».

`Tmix max` під навантаженням 145 КБ/с — **0.44 мс**, циклів не пропущено.
⚠️ `Period` тепер 7 мс, а не 4: його задає протокол радіомодуля
(`mixer_scheduler.cpp`), тож давні заміри порівнюються лише як частка періоду.

**Клієнт тепер водиться з ПК без рук людини** — `tools/webui_drive.py`, дотики
по канві. Дві межі: дотик має тривати ≥100 мс (LVGL опитує сенсор раз на
30 мс), і `ENTER` із браузера недосяжний — `[ENTER] to reset` на екрані пульта
не сенсорна кнопка, а підпис до клавіші.

---

# ⛔ Поріг «скільки плиток чекати» — важіль мертвий, і це заміряно

Задача [0019](../tasks/0019-frame-completeness.md), прогін на стенді.
Запобіжник із порогом 45 плиток, поставлений напередодні з арифметики,
**не підтвердився заміром** і відрізав саме той випадок, заради якого задача
існує.

| поріг | цілих кадрів | за строком | показів/с |
|---|---|---|---|
| 45 (був) | 75 % | **0** | 8.8 |
| 100 і вище | **100 %** | **0** | 6.6 |

**`показано за строком` = 0 при будь-якому порозі й будь-якій стелі** — тобто
загроза «безперервне гортання дасть ~4 кадр/с зшитими кадрами», заради якої
запобіжник і ставили, на цьому залізі не існує.

⚠️ **Поправка від 2026-08-02, див. розділ вище:** це вірно **лише на
2 625 000**. На 921 600 строк спрацьовує на кожному третьому кадрі — гілку,
яку вважали мертвою, просто не міряли на базовій швидкості.

⚠️ **Головне число, яке варто пам'ятати:** `dirtyTiles` **ніколи не
наближається до розміру сітки** (135) — стеля залишку ≈96, бо пульт устигає
віддати решту ще до закриття кадру. Максимуми по режимах керування: енкодер
неспішно 83, швидко 94, **перескок сторінки 89**. Отже режими керування
порогом **не розділяються**, і шукати «правильне число» більше не треба.

Кнопка «чек» стала трипозиційною (`вимк` / `до N` / `завжди`); типове —
**`завжди`**, рішення людини після порівняння оком. Ціна: −25 % частоти показу
за те, що жоден показаний кадр не зшитий (це і є записане рішення проєкту від
26.07 «кадри рідшають, але кожен цілісний»).

**Наступний важіль — не число, а джерело вводу:** клієнт сам щойно надіслав
`ENC` або `TOUCH`, тож правило може бути «після енкодера чекаю, після дотику —
ні». Підстава: людина каже, що на **робочому** екрані пульта фризів немає
взагалі, отже ривки при протягу пальцем додає наше очікування, а не грубий
вхід EdgeTX (`LV_INDEV_DEF_READ_PERIOD` = 30 мс).

⚠️ **Клієнт живе в бінарнику моста:** кожна правка `webui/` коштує прошивання.
У цій сесії вийшло **шість** прошивань — пульт один раз, міст п'ять.

**Дрібниці повз міст:** у браузері з'явилися колесо миші як енкодер і права
кнопка як клавіша «назад» (шукається за міткою з `HELLO`, не числом).
Порядок обходу пунктів енкодером **дивний з вини EdgeTX**, не нашої —
підтверджено фізичним енкодером.

---

# 🔧 FRAME_END каже правду про кадр — код готовий, черга за залізом

Задача [0019](../tasks/0019-frame-completeness.md): `FRAME_END` несе
`dirtyTiles` — скільки плиток лишилось незасланими на мить закриття кадру.
Клієнт при `N=0` показує негайно, при `N>0` чекає доїзду решти зі строком і
запобіжником (докладно — розділ «Результат» задачі й handoff
[2026-08-01-19](handoffs/2026-08-01-19.md)).

**Доведено читанням коду, не наміром:** при `dirtyTiles=0` буфер клієнта
дорівнює екрану пульта на цю мить — не «майже цілий кадр». Це спростувало
припущення, що шов лише «зменшиться»: він або зникає повністю, або лишається
таким, як був, третього стану немає.

Прошивка пульта змінена **один раз** (коміт `055fd97`); запобіжник
(`c06a0f1`) — цілком у клієнті, пульта не чіпає. ⚠️ **Прошивань на стенді все
одно два**: клієнт живе в бінарнику моста.

**Наступний крок:** прошивання за дисципліною 0014, потім критерії 3–4 —
детально в `handoffs/2026-08-01-19.md`.

---

# ✅ Розлам картинки — не втрата. Причина знайдена, лікування в 0019

Задача [0018](../tasks/0018-bridge-losses.md) поставила три причини втрат
трьома числами (критерій 1 — прийнято) і закрила критерій 4.2 відповіддю,
протилежною до власної мети: **розлам при прокрутці — не втрата пачок**.

Доказ сильніший за лічильники: на знімку екрана в мить гортання видно **дві
рамки виділення одночасно**, хоча в живому інтерфейсі виділений пункт рівно
один. Причина — `firmware/edgetx-patch/remote_ui/transport_uart.cpp:1032`:
кадр закривається за подією «LVGL домалював», не чекаючи, доїхали всі
плитки цього кадру чи ні. На великій перемальовці лишається в дорозі
приблизно половина.

Побічно виправлено мовчазне псування потоку WebSocket (`httpd` віддавав
обрізаний кадр як успіх) і поставлено прилад у клієнта — скільки головного
потоку браузера з'їдають прийом і малювання (2–3% при 190 КБ/с — клієнт
поза підозрою).

⚠️ Втрати з першого прогону (19.7% плиток на телефоні) **перестали
відтворюватись** під важчим навантаженням. Причина не встановлена, і це
записано прямо, а не приховано.

**Наступний крок:** [0019](../tasks/0019-frame-completeness.md) —
`FRAME_END` понесе число плиток, яких ще бракує кадру. Протокол міняється,
тож перед кодом — `protocol-architect`.

---

# 🎉 Пульт працює на 2 625 000, і швидкість тепер міняється за секунди

Задача [0017](../tasks/0017-runtime-baudrate.md): швидкість каналу перестала
бути сталою часу збірки. Клієнт замовляє, пульт дозволяє, міст виконує —
**без жодного перепрошивання** на дослід.

| Було (921600) | Стало (2 625 000) |
|---|---|
| 3.5 кадр/с, дріт завантажений на **88.6 %** | 7.5 кадр/с, дріт на **71 %** |
| 90 КБ/с ємності | 256 КБ/с |

Дріт **перестав бути вузьким місцем**: 28 хвилин на максимумі дали нулі на
всіх лічильниках — `crc_errors`, `uart_dropped`, `oversized`, `silence_resets`.

## Запобіжник повернення — доведений відмовою, а не успіхом

Не почувши осмисленого пакета за 500 мс, пульт **сам** вертається на 921600.
Три прогони: провал 995 / 1005 / 1004 мс. Скидання моста в сліпому вікні —
обидва боки зустрілись удома без втручання. Лічильник відкотів **26 = 3 + 23**,
рівно за кількістю прийнятих перемикань.

⚠️ Відкат **завжди на `REMOTE_UI_BAUDRATE`**, ніколи «на попередню»: після
перезавантаження моста спільного стану не існує, і константа — єдине місце,
де вони гарантовано зустрінуться.

## Стеля 2 625 000 — це умова дозволу на гачки

AUX1 тактується **42 МГц**, не 45 (`system_clock.c`, `APB1_DIV_4`), тож
стеля = PCLK1/16. За нею драйвер пише `CR1.OVER8` при увімкненому USART — і
одного рядка в чужому коді вже мало. Тримається двома `static_assert`.

## ⚠️ Межа переїхала: плавності на цьому транспорті не буде

Повний кадр — 91 плитка × 286 Б ≈ **25.4 КБ**; ємність дроту 256 КБ/с. Отже
стеля ≈ **10 кадр/с**, і для 25 потрібно **2.5× понад апаратну межу AUX1**.
Жодна швидкість переліку цього не дасть.

За той самий сеанс до телефона **не доїхало 1059 плиток і 133 пачки**, а
вільної пам'яті в мості лишалось **20 КБ** у мінімумі. Це вже не про якість
картинки, а про стійкість моста — і це предмет задачі
[0018](../tasks/0018-bridge-losses.md).

## Що лишилось у 0017

Три дрібниці, дві потребують очей людини: вимкнути-увімкнути пульт (6.1),
перемикання з утримуваною клавішею (3.1), рядок у `DECISIONS.md` (6.2, 6.3).

---

## Як зібрати

**Прошивка пульта** — нативно, пінованим `arm-none-eabi-gcc 14.2.1`, без
docker. Тека збірки — **`build-fw-14`** (не `build-fw`: там інша
конфігурація, без файлу тулчейна).

```sh
tools/setup-toolchain.sh          # ідемпотентний: компілятор + Python-залежності
export PATH="$PWD/toolchain/arm-gnu-toolchain-14.2.rel1-x86_64-arm-none-eabi/bin:$PATH"
cmake --build upstream/edgetx/build-fw-14 --target firmware -j"$(nproc)"
```

**Міст** — `idf.py build && idf.py -p /dev/ttyUSB0 flash` після
`. ~/esp/esp-idf/export.sh`. Прошивається по USB, руки людини не потрібні.

⚠️ **`/dev/ttyUSB0` — це порт самої ESP32**, а не перетворювач на AUX1.
Перетворювача на лінії пульта на стенді немає взагалі.

**Симулятор наразі не збирається:** у системі немає `clang`, а пакет
`libclang` із pip дає бібліотеку без вбудованих заголовків. Падає на
генераторі `datacopy.inc`. `tools/setup-toolchain.sh` це ловить і називає
команду.

## Прошивання: поправка до дисципліни 0014

Крок 0 (зчитування **до** запису) **пропускається** в межах однієї сесії:
успішне зчитування не доводить, що вдасться запис. Поза сесією робиться
тільки якщо вміст пульта невідтворюваний. **Звірка після запису лишається
обов'язковою завжди.**

## Прогрес по етапах

- [x] Етап 0 — прошивка наосліп і процедура відкату
- [ ] Етап 1 — розробка в симуляторі (лишився крок 1.7 — інші пульти)
- [ ] Етап 2 — перенесення на залізо (2.1–2.6a зроблені; 2.7 у роботі)
- [ ] Етап 3 — бокові панелі керування (MVP 3)
- [ ] Етап 4 — стабілізація і безпека польотів
- [ ] Етап 5 — розширення

## Заблоковано

**Фаза 5 (автономне живлення від пульта) — некупленими деталями.** Потрібні
конденсатори (електроліт 16 В, керамічний 10 мкФ) і корпус-роз'єм Dupont 1×4.
Документ `docs/09-bom.md` виправлений ще 26 липня — стримує саме купівля.

## Дії людини, не агента

- `sudo pacman -S clang` — без нього симулятор не збереться.
- `rm -rf build/py-venv` — 102 МБ, дублікат `toolchain/py-fw-build`; видалити
  агенту заборонили дозволи.
- **Чи прийнятні ~10 кадр/с** для сліпого пульта з робочим меню. Від
  відповіді залежить, чи потрібна окрема задача про стиснення.

## Перевірити пізніше (не блокує)

- ⚠️ **Пароль точки доступу — до етапу 3.** Лежить у відкритому репозиторії,
  тобто мережа фактично відкрита, а через неї керують пультом.
- **Плитка 16×16 замість 32×32** — пропозиція команди, відхилена асистентом;
  один із двох його аргументів він сам зняв (серії RLE йдуть наскрізь через
  плитку). Варте повторного розгляду **після** 0018.
- **Фізичні клавіші пульта при живому клієнті** — доведено будовою коду, а не
  натисканням.
- **Меню Lua-скрипта ELRS** — повне ходіння, у симуляторі скрипт застигає на
  `Loading…`.
- **Гачок енкодера:** `*rotencDt += pending` не атомарний; вікно — три
  інструкції, найгірший наслідок — одне клацання із завищеним прискоренням.
  Рішення лишити так свідоме.
- **Крок 1.7:** гачок захоплення має отримати крок рядка — на цілях із прямим
  режимом LVGL пікселі лежать із кроком `LCD_W`. Перевірити **за кодом LVGL**,
  а не припустити.
- **Етап 2:** тіньовий кадр (261 КБ) лежить у SDRAM; на кольоровій цілі без
  SDRAM підхід не поїде.

## Зафіксовано

- EdgeTX на пульті — **2.12.2 (3673c518)**; `upstream` закріплений на тезі
  `v2.12.2`, не на рухомій гілці.
- У пульті зараз збірка **`86eb282d…`** (0017, `HELLO` з переліком швидкостей).
  Дорога назад — `sd-backups/task-0017/verify-after-0017.bin`.
- Початкова швидкість у прошивці — **921600**; вона ж домашня для відкату.
- Перелік швидкостей: 921600, 1 000 000, 1 500 000, 2 000 000, 2 625 000.
- Роз'єм AUX1 — крок 2.54 мм, звичайні перемички Dupont.
- Середовище моста — чистий ESP-IDF (ADR-0004, замінює ADR-0001).
- Плати — DevKit v1 для розробки, C3 SuperMini кандидат у корпус (ADR-0003).
- Ризик завад знято: зовнішній модуль через JR-відсік.
- ОС — Arch Linux, середовище описане в `docs/10-arch-linux.md`.
