# Handoff 2026-08-02-24

## Зроблено

Сесія відкрилась уже після рецензії 0021 (коміт `762c34b`, «прийнято; SND_BUF
не чіпаємо, важіль інший») — коду не писала, working tree чистий від початку
до кінця.

- **Почищено `.claude/settings.local.json`**: 284 → 90 дозволів. Прибрано:
  - усе, що вже покриває глобальний `.claude/settings.json` (wildcard-и
    `python3:*`, `node:*`, `git add:*`, `grep:*` тощо — 30 записів, дублі);
  - шляхи мертвого минулого: старий комп'ютер `/mnt/D/Works/My/…` і сім
    UUID-ів scratchpad давно закритих сесій (103 записи);
  - 37 буквальних одноразових команд: тексти комітів (`git commit -m '…'`),
    `sed -i` з номером конкретної задачі, одноразове завантаження/розбір
    релізу EdgeTX 2.12.2, пакет `python-lz4`;
  - `Bash(git config *)` — блокуючий дозвіл суперечив власному правилу
    `CLAUDE.md` («ніколи не оновлювати git config»); хтось дав його один раз
    для локального автора коміту, а він лишився чинним назавжди.
  - `.claude/settings.json` (трекований) чіпати не було потреби — уже чистий.

## Не завершено

Нічого — сесія не відкривала жодної правки коду чи задачі.

## Рішення, прийняті в цій сесії

Немає архітектурних рішень. Прибирання дозволів — механічне виконання
правила людини від 2026-07-26, не рішення.

## Наступний крок

Дивитись `tasks/INDEX.md`. 0021 закрита повністю (код, результат, рецензія).
0018 і 0015/0017 (частини) лишаються «у роботі»/«нова» — вибір теми за
людиною або асистентом, не тут.

## Відкриті питання до людини

Немає.

## Пастки й спостереження

- **`.claude/settings.local.json` не трекується git-ом** (глобальний
  `~/.config/git/ignore`), тож чищення сюди не потрапляє в коміт — це
  нормально, файл персональний для машини.
- Дозволи накопичувались із **вересня минулої машини** (`/mnt/D/Works/My/…`)
  через перенесення репозиторію на `/home/balkotron/Project/…` — жодного разу
  не чистились відтоді. Якщо звичка «дозволяти одноразово» триватиме, файл
  знову наросте; вартувало б звички чистити після кожних кількох задач, а не
  чекати, доки набереться три сотні рядків.
