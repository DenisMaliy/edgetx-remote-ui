# TX16S Remote UI — контекст проєкту

> Цей файл читає кожна сесія Claude Code. Тримай його коротким і актуальним.
> Детальні документи — у `docs/`. Поточний стан роботи — у `state/PROGRESS.md`.

## Що робимо

Заміна фізично розбитого екрана RadioMaster TX16S на смартфон/браузер.
Прошивка EdgeTX віддає **справжній кадровий буфер** назовні і приймає назад
**емульований ввід** (сенсор, клавіші, енкодер). Клієнт нічого не знає про
внутрішню логіку EdgeTX — він показує пікселі й шле натискання.

Наслідок: працює **все** без винятку — системне меню, налаштування моделі,
меню Lua-скриптів, майстри, спливні вікна. (⚠️ ELRS-конфігуратор саме на
цьому пульті неможливий — внутрішній модуль TX16S це MULTI; для перевірки
брали аналізатор спектра.)

## Короткострокова ціль (MVP)

1. Зображення екрана пульта на смартфоні.
2. Обробка натискань — емуляція сенсорного екрана.
3. Бокові панелі з дублюванням фізичних клавіш і енкодера.

## Довгострокова ціль

Підтримка більшості пультів на EdgeTX (кольорові й ЧБ) і, за можливості,
прийняття у upstream EdgeTX під окремим прапорцем збірки.

## Залізо

- Пульт: RadioMaster TX16S, STM32F429, екран 480×272 RGB565, ціль збірки `X10`/`RADIO_TX16S`.
- Прошивка на пульті: **EdgeTX 2.12.x**. Наша гілка базується на релізній
  гілці **2.12**, не на `main` — вміст SD-картки прив'язаний до версії, і
  збірка від `main` (2.13-dev) викличе попередження про невідповідність
  картки. `main` перевіряємо тільки командою `/rebase-check`.
- Транспорт: **AUX1** (зовнішній 4-пін роз'єм знизу: GND / 5V / TX / RX).
  Саме AUX1, бо в нього є DMA на передачу. Деталі — `docs/06-hardware.md`.
- Міст: ESP32 DevKit v1 для розробки, C3 SuperMini — кандидат у корпус (ADR-0003).
- Середовище моста: **чистий ESP-IDF, збирач `idf.py`** (ADR-0004 + доповнення
  2026-07-26). Не PlatformIO: там стоїть лише Arduino, і він дотягнув би третю
  копію IDF, до того ж старішу за наявну 5.5.4.
- Швидкість каналу: **2 625 000** бод — апаратна стеля AUX1 (`USART3` на шині
  42 МГц, дискретизування ×16). **921600 лишається початковою**: пульт завжди
  піднімається на ній, а високу замовляє клієнт на ходу командою протоколу
  ([ADR-0005](docs/adr/0005-runtime-baudrate.md)), із поверненням додому за
  тишею в каналі.
- Другий транспорт: USB CDC (той самий протокол, інша прив'язка драйвера).

## ⚠️ Головне обмеження проєкту

**Екран пульта не працює.** Жодна інструкція виду «зайди в меню і вибери»
не застосовна. Будь-яка зміна налаштувань пульта на етапах 0–2 робиться
**наосліп**: через режим завантажувача (bootloader) пульт стає USB-накопичувачем,
і файли `RADIO/radio.yml` та `MODELS/*.yml` редагуються з ПК.

Якщо агент пропонує крок «відкрий налаштування на пульті» — це помилка,
крок треба переписати.

## Дві сесії: асистент і команда

Проєкт ведеться у **двох окремих сесіях Claude Code** в одному репозиторії.

- **Асистент** — розмова з босом, рішення, задачі, рецензії. Коду не пише.
  Запуск: `/assistant`. Опис ролі — `docs/ROLE-assistant.md`.
- **Команда** — власне робота. Запуск: `/catchup`.

Міст між ними — файли задач у `tasks/`, а не копіпаст:

```
асистент ──► tasks/NNNN.md   Мета / Критерії / Обмеження
команда  ──► tasks/NNNN.md   Результат
асистент ──► tasks/NNNN.md   Рецензія (за доказами з репозиторію)
```

**Один артефакт — один автор.** Повна таблиця власності в
`docs/ROLE-assistant.md`. Коротко: асистент володіє `docs/` і `tasks/`
(крім розділу «Результат»); команда володіє кодом, `PROGRESS.md` і
`handoffs/`.

Дрібниці йдуть повз міст без оформлення — перелік у `tasks/INDEX.md`.

### Правило вказівника: у повідомленні — адреса, у файлі — зміст

⚠️ **Бос не поштар.** Переносити між сесіями абзаци тексту — не його робота, і
жодне рішення не має існувати тільки в повідомленні.

Обидві сесії пишуть босу **коротко й вказівником**:

```
Промпт для команди: візьми tasks/0018-bridge-losses.md, критерії 1 і 2.
Промпт для асистента: подивись розділ «Результат» tasks/0018, потрібне
                      рішення щодо розміру пачки.
```

Усе, що має пережити сьогоднішній день, пишеться **у файл** — у задачу, у
рецензію, у `state/DECISIONS.md` — і **потім** називається адреса. Якщо
рішення виявилось тільки в промпті, воно втрачене: наступна сесія його не
побачить.

Дозволено додати в повідомлення один-два рядки про те, **з чого почати** і
**чого не робити**, якщо це не записано у файлі. Усе інше — у файл.

### Як звертатись

⚠️ **Бос.** Так називається людина, яка веде цей проєкт, і так до неї
звертаються обидві сесії — у розмові, у промптах і в документах. Правило
від 2026-08-02; доти в документах стояло слово «людина», і в старих задачах,
передачах змін та журналі рішень воно лишається — це записи про минуле, їх
не переписують.

⚠️ **Стать боса невідома, і вгадувати її не треба.** Слово «бос» —
граматично чоловічого роду, але про саму людину воно нічого не каже. Фрази
будуються так, щоб рід не знадобився: «рішення боса», «бос має», «босу
потрібно», а не «бос сказав» чи «бос сказала».

### Кому адресовано — видно з першого рядка

Правила боса від 2026-08-02. Частина з них озвучувалась і раніше, але
жила лише в розмові й тому швидко переставала виконуватись. Тепер вони тут —
у файлі, який читає кожна сесія.

Три адресати, три різні позначки:

| Позначка | Кому | Як оформити |
|---|---|---|
| `ПРОМПТ ДЛЯ КОМАНДИ:` | сесії команди | текст **у блоці коду** |
| `ПРОМПТ ДЛЯ АСИСТЕНТА:` | сесії асистента | текст **у блоці коду** |
| `Текст для боса` | босу | звичайним текстом |

Блок коду обов'язковий: бос копіює його однією кнопкою, замість того щоб
виділяти текст руками.

Змішувати адресатів не можна. Бос не має вгадувати, що з написаного треба
комусь передати, а що прочитати самій.

⚠️ **У промпті — адреса, у файлі — зміст.** Промпт каже: така-то робота
завершена, дивись такий-то документ. Він не переказує ані задачу, ані
рішення, ані числа.

### Порядок у часі, а не за логікою викладу

У повідомленні босу кроки стоять **у тому порядку, у якому їх буде
виконає**.

⚠️ Найчастіша помилка: спершу подати промпт, а нижче дописати, що треба
зробити перед ним. Бос копіює перше, що бачить, і робить кроки навпаки.

Порядок зміни сесії команди — незмінний:

1. промпт на закриття старої сесії;
2. бос відправляє коміти в сховище;
3. промпт для нової сесії, з адресою задачі.

⚠️ **Дозволено не міняти сесію**, якщо її пам'ять цінна саме для цієї
роботи, а заповнення пам'яті не перевищує приблизно сорока-п'ятдесяти
відсотків — залежно від обсягу того, що лишилось. Але тоді це треба сказати
прямо, а не змовчати.

### Промпт має працювати таким, як його скопіювали

Бос копіює блок однією кнопкою й одразу відправляє. Отже блок мусить бути
завершеним сам по собі.

⚠️ **Не лишати відкриту двокрапку під дописування рукою.** Рядок на кшталт
«далі мій відгук:» наприкінці блока виглядає як запрошення дописати, але на
ділі просто доїжджає порожнім, і сесія на тому боці отримує обірване
повідомлення. Помилка зроблена 2026-08-02 і спіймана командою.

Якщо для наступного кроку потрібен текст **самої боса** — це не місце в
блоці, а **окремий промпт, який подається після того, як цю справу
зроблено**. Один блок — одна завершена думка.

⚠️ **І тим більше — жодних заготовок із порожніми пунктами під заповнення.**
Бос приносить відомості асистентові; асистент вирішує, що з них іде в
промпт, а що у файли, і пише готовий блок сам. Дві причини: дописане босом
в чужий блок обминає репозиторій — спрацює один раз і зникне, а згодом
виявиться, що важливих відомостей у документах немає; і бос не тримає в
пам'яті план та вже ухвалені рішення, тож текст повз асистента може
суперечити вирішеному.

⚠️ **Промпт не пишеться наперед.** Текст, складений під очікуваний результат
сесії, яка ще працює, доводиться правити знову і знову: обставини
змінюються швидше, ніж бос встигає його відправити. Спершу стає відомо,
що сталося, — потім промпт.

### Що має бути в кожному звіті про зроблену роботу

Однаково для команди й для асистента. Простими реченнями, без скорочень і
без запозичених слів там, де є свої:

- **Яка була ціль.**
- **Який результат.**
- **Знахідки** — несподіванки, проблеми, те, що виявилось не таким, як
  думали, спростовані припущення.
- **Хто що робив** — які саме помічники і які моделі працювали
  **насправді**, а не за роллю з таблиці нижче.

Без води й без зайвих подробиць. Подробиці живуть у файлах, у звіті — суть
і адреса.

### Життя сесії: коли закривати й відкривати

Сесія не вічна: контекст заповнюється, і сесія починає гірше пам'ятати
власний початок. Тому робота ріжеться на сесії свідомо, а не тоді, коли
стало погано.

**Закривати сесію варто:**

- після того, як задачу **прийнято** рецензією;
- перед фізично незворотним кроком (прошивання наосліп, перше підключення) —
  свіжий контекст помиляється рідше;
- коли сесія вже провела **дві-три задачі** поспіль.

**Порядок незмінний:**

```
кінець сесії  →  /handoff   (команда фіксує зроблене й наступні кроки)
нова сесія    →  /catchup   (піднімає стан, останній handoff, git)
```

⚠️ **Називати цей момент — обов'язок асистента.** Бос не має вгадувати,
коли сесія «втомилась»: асистент сам каже «тут закриваємо, зроби handoff,
далі нова сесія з catchup». Не сказав — вважай, що забув.

**Асистент стежить за трьома речами, і жодну з них бос не має тримати в
голові сама:**

1. **Стара сесія закрилась передачею зміни, нова почалась підняттям
   контексту.** Обидва кроки називаються в промптах, які пише асистент.
2. **Усі учасники закомітили своє, а коміти боса пішли в сховище.**
   Нагадувати не абиколи, а саме в ту мить, коли це доречно: між закриттям
   старої сесії й відкриттям нової.
3. **Документація ведеться.** Свою асистент веде сам, а після кожної
   переданої зміни перевіряє, чи команда вела свою — знімок стану, журнал
   рішень, запис передачі.

### ⚠️ Поки сесія відкрита, асистент планує вперед мовчки

Порядок дій між сесіями — послідовний, не паралельний:

```
команда  → /handoff             минуле записане, сесія закрита
асистент → рецензія, нові задачі   і тільки тепер майбутнє
```

**Що асистенту можна, поки сесія ще відкрита:** рецензія на **зроблене**.
Вона дивиться назад, і handoff може просто послатись на неї.

**Чого не можна:** створювати наступні задачі, переписувати критерії інших
задач, записувати рішення про «що далі». Це майбутнє, і воно **пересуває
ґрунт під сесією, яка ще пише свій handoff**: та змушена стежити, щоб не
порушити план, якого не бачила, коли працювала.

⚠️ **І тим більше — не правити задачу, за якою сесія працює просто зараз.**
Правило ширше за handoff: воно про будь-яку відкриту сесію. Файл задачі
читається **на початку**, і дописане пізніше сесія не побачить ніколи.

Якщо додати таки треба (від боса прийшла ідея, знайшлась вада в критерії):

1. дописувати **тільки додаванням**, з видимою позначкою дати — ніколи не
   переписуючи те, з чим сесія почала;
2. **одразу сказати босу**, що файл змінився і що саме там з'явилось,
   щоб бос переказав це відкритій сесії;
3. якщо зміна скасовує вже зроблене — не дописувати, а зупинити сесію.

Наслідок для handoff-у: його розділ про наступні кроки — це **вказівник на
`tasks/INDEX.md`**, а не власний план. Плани живуть у задачах, стан — у
handoff-і.

## Структура репозиторію

```
docs/           ТЗ, архітектура, протокол, план, точки дотику, залізо
tasks/          Міст асистент ↔ команда: одна задача — один файл
state/          Живий стан: PROGRESS.md, DECISIONS.md, ASSISTANT.md, handoffs/
firmware/edgetx-patch/   Наш код для EdgeTX (нові файли + патчі гачків)
firmware/esp32/          Прошивка моста
webui/                   Клієнт (браузер)
tools/                   Скрипти збірки, тестовий клієнт, перевірка rebase
upstream/edgetx/         Клон EdgeTX (НЕ в git, ставиться tools/setup-upstream.sh)
```

## Правила роботи з кодом EdgeTX

Це найважливіше правило проєкту. Порушення = біль при кожному оновленні EdgeTX.

1. **Нові файли — вільно. Чужі файли — по краплині.**
   Повний дозволений перелік місць у коді EdgeTX — `docs/05-hooks.md`.
   Хочеш змінити щось поза цим переліком — спочатку ADR, потім код.
2. **Два коміти.** Комміт A = тільки нові файли. Комміт B = тільки гачки.
   Так `git rebase` на новий upstream конфліктує максимум у десятку рядків.
3. **Ніякої залежності від LVGL API там, де можна її уникнути.**
   Захоплення екрана робимо порівнянням двох кадрових буферів, а не гачком
   на `flush_cb`. LVGL 8 → 9 зламає flush; порівняння буферів переживе.
4. **Усе під прапорцем збірки `REMOTE_UI`.** Флеш на F4-цілях майже вичерпаний.
5. **Специфіка конкретного пульта в коді — нуль рядків.** Роздільність,
   набір клавіш, наявність сенсора віддаються клієнту в пакеті `HELLO`
   через наявні API (`keysGetSupported`, `keysGetLabel`, `LCD_W/LCD_H`).

## Команди

- `/assistant` — увійти в роль асистента (окрема сесія).
- `/task-new <тема>` — асистент створює задачу.
- `/task-report <N>` — команда записує результат.
- `/task-review <N>` — асистент рецензує за доказами.
- `/catchup` — на початку сесії команди: стан, останній handoff, git.
- `/handoff` — наприкінці сесії: зафіксувати зроблене, рішення, наступні кроки.
- `/build <target>` — зібрати прошивку або симулятор.
- `/rebase-check` — перевірити гілку на свіжому upstream EdgeTX.
- `/adr <тема>` — оформити архітектурне рішення.

## Агенти

| Агент | Модель | Коли |
|---|---|---|
| `protocol-architect` | Opus | зміни протоколу, крос-компонентні рішення |
| `edgetx-firmware` | Opus | будь-який C++ усередині EdgeTX |
| `hw-integration` | Opus | електрика, розпіновка, живлення, RF |
| `code-reviewer` | Opus | обов'язковий перед кожним комітом у firmware |
| `esp32-firmware` | Sonnet | прошивка моста |
| `webui-dev` | Sonnet | клієнт у браузері |
| `upstream-watcher` | Sonnet | rebase, стеження за змінами EdgeTX |
| `build-runner` | Haiku | механічний запуск збірки й вичавлювання помилок |
| `explorer` | Haiku | пошук по великій кодовій базі EdgeTX |

## Мова проєкту

**Українською:**
- спілкування з босом;
- коментарі в коді (усі, включно з `//` і doxygen);
- повідомлення комітів;
- документація, ADR, handoff-и, журнал рішень;
- рядки логів і повідомлення про помилки, які бачить бос.

**Англійською лишаються** (це не переклад, а частина коду):
- назви змінних, функцій, файлів, типів, констант;
- назви пакетів протоколу (`HELLO`, `TILE`, `TOUCH`);
- назви гілок git і префікси комітів (`feat:`, `fix:`);
- усе, що приходить із EdgeTX або ESP-IDF.

**Між собою агенти можуть спілкуватися як завгодно.** Мовна вимога
стосується того, що потрапляє в репозиторій або до боса, а не внутрішніх
міркувань і проміжних повідомлень між агентами.

Простими словами; англомовний термін — українською, у дужках англійською
при першій згадці.

### Формат комітів

Conventional Commits із українським описом:

```
feat: захоплення кадру порівнянням буферів
fix: втрата плитки при переповненні буфера передачі
docs: уточнено розпіновку AUX1
refactor: винесено RLE в окремий файл
chore: оновлено upstream EdgeTX
```

Тіло коміту — теж українською, якщо воно потрібне.

### ⚠️ Виняток на майбутнє

Якщо дійде до PR у upstream EdgeTX (етап 5.5), туди йде **англійська**:
коментарі в коді, повідомлення комітів, опис PR. Це вимога чужого проєкту,
не наша. Переклад робиться окремою задачею перед подачею PR — див.
`docs/04-plan.md`, пункт 5.5.
