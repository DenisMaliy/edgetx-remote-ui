diff --git a/radio/src/CMakeLists.txt b/radio/src/CMakeLists.txt
index a89c178..0cd7f2e 100644
--- a/radio/src/CMakeLists.txt
+++ b/radio/src/CMakeLists.txt
@@ -61,6 +61,7 @@ else()
 endif()
 option(AUDIO "Enable audio" ON)
 option(EXFAT "Enable exFAT filesystem support in FatFs" OFF)
+option(REMOTE_UI "Remote UI over serial" OFF)
 
 if(NATIVE_BUILD)
   option(ALL_LANGUAGES "Enable runtime language selection" ON)
@@ -515,6 +516,11 @@ if(USBJ_EX)
   )
 endif()
 
+if(REMOTE_UI)
+  add_subdirectory(remote_ui)
+  add_definitions(-DREMOTE_UI)
+endif()
+
 if(POWER_LED_BLUE)
   add_definitions(-DPOWER_LED_BLUE)
 endif()
diff --git a/radio/src/dataconstants.h b/radio/src/dataconstants.h
index 372e38c..6b4f0c6 100644
--- a/radio/src/dataconstants.h
+++ b/radio/src/dataconstants.h
@@ -258,6 +258,9 @@ enum UartModes {
   UART_MODE_DEBUG,
   UART_MODE_SPACEMOUSE,
   UART_MODE_EXT_MODULE,
+#if defined(REMOTE_UI)
+  UART_MODE_REMOTE_UI,
+#endif
   UART_MODE_COUNT SKIP,
   UART_MODE_MAX SKIP = UART_MODE_COUNT-1
 };
diff --git a/radio/src/gui/colorlcd/LvglWrapper.cpp b/radio/src/gui/colorlcd/LvglWrapper.cpp
index f169abd..41a3948 100644
--- a/radio/src/gui/colorlcd/LvglWrapper.cpp
+++ b/radio/src/gui/colorlcd/LvglWrapper.cpp
@@ -29,6 +29,10 @@
 #include "view_main.h"
 #include "keyboard_base.h"
 
+#if defined(REMOTE_UI)
+#include "remote_ui/remote_ui.h"
+#endif
+
 LvglWrapper* LvglWrapper::_instance = nullptr;
 
 static lv_indev_drv_t touchDriver;
@@ -180,6 +184,22 @@ extern "C" void touchDriverRead(lv_indev_drv_t *drv, lv_indev_data_t *data)
 #if defined(HARDWARE_TOUCH)
   static lv_indev_data_t touch_data_backup;
 
+#if defined(REMOTE_UI)
+  // До фізичного сенсора й до перевірки підсвітки: інакше клієнт не розбудив
+  // би пульт зі згаслим екраном (docs/05-hooks.md, пункт 7).
+  {
+    int16_t rx, ry;
+    bool rpressed;
+    if (remoteUiPopTouch(&rx, &ry, &rpressed)) {
+      data->point.x = rx;
+      data->point.y = ry;
+      data->state = rpressed ? LV_INDEV_STATE_PRESSED : LV_INDEV_STATE_RELEASED;
+      reset_inactivity();
+      return;
+    }
+  }
+#endif
+
   if(!touchPanelEventOccured()) {
     memcpy(data, &touch_data_backup, sizeof(lv_indev_data_t));
     return;
@@ -261,6 +281,10 @@ static void rotaryDriverRead(lv_indev_drv_t *drv, lv_indev_data_t *data)
   static uint32_t lastDt = 0;
 
   rotenc_t newPos = rotaryEncoderGetValue();
+#if defined(REMOTE_UI)
+  newPos += remoteUiGetEncoderOffset();
+  remoteUiAddEncoderDt(&rotencDt);
+#endif
   rotenc_t diff = newPos - prevPos;
 
   data->enc_diff = (int16_t)diff;
diff --git a/radio/src/gui/colorlcd/lcd.cpp b/radio/src/gui/colorlcd/lcd.cpp
index e554305..a6378ef 100644
--- a/radio/src/gui/colorlcd/lcd.cpp
+++ b/radio/src/gui/colorlcd/lcd.cpp
@@ -29,6 +29,9 @@
 #if !LV_USE_GPU_STM32_DMA2D && !defined(SIMU)
 #include "dma2d.h"
 #endif
+#if defined(REMOTE_UI)
+#include "remote_ui/remote_ui.h"
+#endif
 
 #if LV_MEM_CUSTOM == 0
 char LVGL_MEM_BUFFER[LV_MEM_SIZE] __SDRAM __ALIGNED(16);
@@ -71,6 +74,11 @@ static lv_disp_drv_t* refr_disp = nullptr;
 static void flushLcd(lv_disp_drv_t* disp_drv, const lv_area_t* area,
                      lv_color_t* color_p)
 {
+#if defined(REMOTE_UI)
+  remoteUiOnFlush(area->x1, area->y1, area->x2, area->y2,
+                  (const uint16_t*)color_p, lv_disp_flush_is_last(disp_drv));
+#endif
+
 #if !defined(LCD_VERTICAL_INVERT) || defined(RADIO_F16)
 #if defined(RADIO_F16)
   if (hardwareOptions.pcbrev > 0)
diff --git a/radio/src/gui/gui_common.cpp b/radio/src/gui/gui_common.cpp
index b71f9c3..01f22d1 100644
--- a/radio/src/gui/gui_common.cpp
+++ b/radio/src/gui/gui_common.cpp
@@ -541,6 +541,16 @@ bool isSerialModeAvailable(uint8_t port_nr, int mode)
     return false;
 #endif
 
+#if defined(REMOTE_UI)
+  // Прямий доступ до пам'яті на передачу є лише в AUX1; VCP наш транспорт
+  // поки не вміє (docs/05-hooks.md, пункт 4).
+#if !defined(AUX_SERIAL_DMA_TX)
+  if (mode == UART_MODE_REMOTE_UI) return false;
+#else
+  if (mode == UART_MODE_REMOTE_UI && port_nr != SP_AUX1) return false;
+#endif
+#endif
+
 #if !defined(AUX_SERIAL_DMA_TX) || defined(EXTMODULE_USART)
   if (mode == UART_MODE_EXT_MODULE)
     return false;
diff --git a/radio/src/keys.cpp b/radio/src/keys.cpp
index af4a93f..3a03e3b 100644
--- a/radio/src/keys.cpp
+++ b/radio/src/keys.cpp
@@ -32,6 +32,10 @@
 #include "edgetx.h"
 #endif
 
+#if defined(REMOTE_UI) && !defined(BOOT)
+#include "remote_ui/remote_ui.h"
+#endif
+
 // long key press minimum duration (x10ms),
 // must be less than KEY_REPEAT_DELAY
 #define KEY_LONG_DELAY              32
@@ -491,6 +495,11 @@ bool keysPollingCycle()
   trims_input = READ_TRIMS();
 #endif
 
+#if defined(REMOTE_UI) && !defined(BOOT)
+  keys_input |= remoteUiGetKeys();
+  trims_input |= remoteUiGetTrims();
+#endif
+
   for (int i = 0; i < MAX_KEYS; i++) {
     event_t evt = keys[i].input(keys_input & (1 << i));
     if (evt) {
diff --git a/radio/src/serial.cpp b/radio/src/serial.cpp
index 1f09287..3050440 100644
--- a/radio/src/serial.cpp
+++ b/radio/src/serial.cpp
@@ -50,6 +50,10 @@
   #include "thirdparty/Segger/SEGGER/SEGGER_RTT.h"
 #endif
 
+#if defined(REMOTE_UI) && !defined(BOOT)
+  #include "remote_ui/remote_ui.h"
+#endif
+
 #define PRINTF_BUFFER_SIZE    128
 
 static void (*dbg_serial_putc)(void*, uint8_t) = nullptr;
@@ -277,6 +281,13 @@ static void serialSetCallBacks(int mode, void* ctx, const etx_serial_port_t* por
     break;
 #endif
 
+#if defined(REMOTE_UI)
+  case UART_MODE_REMOTE_UI:
+    // ctx == nullptr — це від'єднання (порт іде під інший режим).
+    remoteUiSetSerialDriver(ctx, drv, port);
+    break;
+#endif
+
 #endif
   }
 }
@@ -346,6 +357,13 @@ static void serialSetupPort(int mode, etx_serial_init& params)
     break;
 #endif
 
+#if defined(REMOTE_UI)
+  case UART_MODE_REMOTE_UI:
+    params.baudrate = REMOTE_UI_BAUDRATE;
+    params.direction = ETX_Dir_TX_RX;
+    break;
+#endif
+
 #endif // BOOT
   }
 }
diff --git a/radio/src/storage/yaml/yaml_datastructs_funcs.cpp b/radio/src/storage/yaml/yaml_datastructs_funcs.cpp
index 0e00393..2f3c708 100644
--- a/radio/src/storage/yaml/yaml_datastructs_funcs.cpp
+++ b/radio/src/storage/yaml/yaml_datastructs_funcs.cpp
@@ -2592,6 +2592,9 @@ static const struct YamlIdStr enum_UartModes[] = {
   {  UART_MODE_DEBUG, "DEBUG"  },
   {  UART_MODE_SPACEMOUSE, "SPACEMOUSE"  },
   {  UART_MODE_EXT_MODULE, "EXT_MODULE"  },
+#if defined(REMOTE_UI)
+  {  UART_MODE_REMOTE_UI, "REMOTE_UI"  },
+#endif
   {  0, NULL  }
 };
 
diff --git a/radio/src/translations/translation_def.h b/radio/src/translations/translation_def.h
index 9de4cd7..1399eb5 100644
--- a/radio/src/translations/translation_def.h
+++ b/radio/src/translations/translation_def.h
@@ -129,7 +129,14 @@
 
 #define TR_VCELLINDEX               SA11(TR_VCELLINDEX)
 
+#if defined(REMOTE_UI)
+// Рядок лежить тут, а не в i18n/*.h: інакше довелося б додати його в 19 мовних
+// файлів, тоді як docs/05-hooks.md вимагає мінімум англійську.
+#define TR_AUX_SERIAL_MODES_12      "Remote UI"
+#define TR_AUX_SERIAL_MODES         SA12(TR_AUX_SERIAL_MODES)
+#else
 #define TR_AUX_SERIAL_MODES         SA11(TR_AUX_SERIAL_MODES)
+#endif
 
 #define TR_VFORMULAS                SA9(TR_VFORMULAS)
 #define TR_FS_COLOR_LIST            SA9(TR_FS_COLOR_LIST)
