/*
 * TX16S Remote UI — перемикання швидкості каналу на ходу (ADR-0005).
 *
 * Ліцензія: GPLv2 (та сама, що в EdgeTX).
 *
 * Тут — перелік дозволених швидкостей і два пакети протоколу: `0x88 BAUD_SET`
 * (клієнт -> пульт) і `0x06 BAUD` (пульт -> клієнт). Самого перемикання тут
 * немає: воно живе в транспорті, бо залежить від драйвера й від прямого
 * доступу до пам'яті. Тут — те, що можна перевірити тестами без заліза.
 *
 * ============================================================================
 * ⚠️ Головне правило цього файлу: пульт із розбитим екраном не має права
 * оніміти від невдалого досліду.
 * ============================================================================
 *
 * Перемкнувши швидкість, пульт чекає осмисленого пакета від моста. Не почув за
 * вікно повернення — сам вертається. **Завжди на REMOTE_UI_BAUDRATE**, ніколи
 * «на попередню»:
 *
 *   «Попередня» — це стан, якого міст після перезавантаження не має. Ланцюг
 *   921600 -> 1 М (успіх) -> 2 М (міст помер) лишив би пульт на 1 М, а міст
 *   піднявся б на власній константі 921600. Вони не зустрілись би.
 *
 * Домашня швидкість — дно, тому **вкладених відмов не існує за побудовою**, і
 * другого рівня відкату не потрібно. Ціна названа чесно: після втрати моста
 * удала сесія на 2 Мбод падає до 921600, а не до 1 Мбод. Це втрата пропускної
 * здатності, ніколи — втрата зв'язку.
 */

#pragma once

#include <stddef.h>
#include <stdint.h>

namespace remote_ui {

// --- Перелік дозволених швидкостей ------------------------------------------
//
// ⚠️ Стеля 2 625 000 — це **умова, а не просто межа**.
//
// AUX1 на TX16S — це `USART3` на шині PCLK1, а EdgeTX тактує її 42 МГц
// (`LL_RCC_SetAPB1Prescaler(LL_RCC_APB1_DIV_4)` при SYSCLK 168 МГц;
// `PERI1_FREQUENCY 42000000` у `targets/horus/hal.h` — те саме число
// незалежно). 2 625 000 = 42 МГц / 16.
//
// За цією межею `_calc_best_oversampling()` (`stm32_usart_driver.cpp`) вертає
// дискретизування ×8, і `stm32_usart_set_baudrate()` пише `CR1.OVER8` **при
// увімкненому USART**. RM0090 позначає `OVER8` як біт, що пишеться лише при
// `UE = 0`, тобто запис може просто не подіяти: `BRR` порахований під ×8,
// залізо семплює ×16, фактична швидкість удвічі не та.
//
// Тобто перелік, що вилазить за 2 625 000, вимагав би гасити й піднімати порт,
// а це вже не один рядок у чужому коді — це зупинка задачі й нова ADR
// (tasks/0017). Поки перелік не виходить за стелю, весь механізм коштує рівно
// нуль рядків у EdgeTX.
//
// Похибки дільників на обох боках (пульт — 42 МГц із подвійним обрізанням у
// макросі `__LL_USART_DIV_SAMPLING16`; міст — 80 МГц, `K = 1.28e9 / baud`):
//
//   бод        BRR   пульт        похибка   K      міст         розбіжність
//   921 600     45   933 333.3    +1.273%   1388   922 190.2    +1.208%
//   1 000 000   42   1 000 000.0   0.000%   1280   1 000 000.0   0.000%
//   1 500 000   28   1 500 000.0   0.000%    853   1 500 586.2  -0.039%
//   2 000 000   21   2 000 000.0   0.000%    640   2 000 000.0   0.000%
//   2 625 000   16   2 625 000.0   0.000%    487   2 628 336.8  -0.127%
//
// ⚠️ Найважливіший рядок — перший. Базова 921600 має **найбільшу** розбіжність
// з усього переліку (третину бюджету приймача 3.33%) — і сьогодні працює з
// нулем помилок CRC. Тобто арифметично перехід на будь-яку іншу швидкість
// переліку безпечніший за те, що вже працює, у 10–30 разів.
//
// Чого в переліку немає і чому:
//   - 2 100 000 — рівне на пульті, але це 5% понад 2 000 000. Зайвий рядок у
//     випадайці, який нікому не знадобиться.
//   - 2 500 000 — рівне на мості (K = 512), але на пульті BRR = 17 -> 2 470 588,
//     тобто -1.176%. Формально прийнятно, та поруч є **точне** 2 625 000, ще й
//     швидше. Тримати гірше значення поруч із кращим — це запрошення обрати
//     гірше.
//
// Перелік закритий і живе в прошивці, а не приходить ззовні: пульт — той бік,
// який не можна знеструмити, і саме тут перевіряється «це значення представне
// на обох боках». Друкарська помилка (20 000 000) відпала б і так, а от
// правдоподібне хибне число (2 500 000) без переліку проїхало б із похибкою
// 1.18%.
// ⚠️ `inline`, а не просто `constexpr`: без нього масив має внутрішнє
// зв'язування, тобто по копії в кожній одиниці трансляції, що його читає. На
// цілі, де флеш майже вичерпаний (CLAUDE.md, правило 4), платити за це нема
// за що.
inline constexpr uint32_t BAUD_ALLOWED[] = {921600, 1000000, 1500000, 2000000,
                                            2625000};
inline constexpr size_t BAUD_ALLOWED_COUNT =
    sizeof(BAUD_ALLOWED) / sizeof(BAUD_ALLOWED[0]);

// Стеля, за якою драйвер перемкнув би дискретизування (42 МГц / 16).
//
// ⚠️ Це число конкретного пульта, і воно тут **не остаточне**: транспорт
// звіряє його з тактовою шиною цілі, узятою в EdgeTX, і збірка падає, якщо
// вони розійшлися. Тут воно лежить лише тому, що цей файл навмисно нічого не
// знає про EdgeTX і збирається тестами без нього.
constexpr uint32_t BAUD_CEILING = 2625000;

// Перевірка переліку цілком, а не тільки останнього елемента.
//
// ⚠️ Різниця не формальна. `BAUD_ALLOWED[BAUD_ALLOWED_COUNT - 1] <= BAUD_CEILING`
// пропустив би 3 000 000, вставлене в **середину** масиву: упорядкованість
// гарантована лише юніт-тестом, а тести в збірку прошивки не входять. Тобто
// значення, яке зламало б дискретизування, проїхало б у пульт без жодного
// сигналу — саме те, від чого ця стеля й поставлена.
constexpr bool baudListWithinCeiling()
{
  for (size_t i = 0; i < BAUD_ALLOWED_COUNT; ++i) {
    if (BAUD_ALLOWED[i] > BAUD_CEILING) {
      return false;
    }
  }
  return true;
}

static_assert(baudListWithinCeiling(),
              "перелік вилазить за стелю — це вже не один рядок, а нова ADR");

// --- Числа перемикання ------------------------------------------------------
//
// Обидва їдуть **у пакеті `BAUD`**, а не сидять константами в мості. Це той
// самий принцип «нуль специфіки конкретного пульта в клієнті» (CLAUDE.md):
// пульт із іншою тактовою або іншим періодом проходу назве власні числа, і
// міст працюватиме без правки.

// Скільки міст чекає після підтвердження, перш ніж перемкнутись сам.
//
// Пульт перемикається першим (пояснення в transport_uart.cpp), і це число —
// обіцянка мосту: «я перемкнусь не пізніше ніж через стільки».
constexpr uint16_t BAUD_SWITCH_DELAY_MS = 100;

// Скільки пульт чекає осмисленого пакета на новій швидкості, перш ніж піти
// додому.
//
// ⚠️ Складові при відмові: 100 (міст доживає свій таймер) + 10 (він
// переналаштовує порт) + 132 (заміряна в задачі 0013 найгірша затримка задачі
// моста, `beat_interval_max_ms`) ≈ 242 мс. Подвоєння до 500 свідоме: вікно
// платиться **лише при відмові**, а закоротке вікно дає найгірший з можливих
// результатів — відкат при мості, який насправді перемкнувся правильно. Тобто
// «дослід не вдався» там, де він удався, і причину шукали б у дроті.
// Помилятись треба в бік довгого вікна.
//
// При успіху це число не витрачається: вікно закривається першим же валідним
// кадром, приблизно через 105 мс від початку мовчання.
constexpr uint16_t BAUD_REVERT_WINDOW_MS = 500;

// --- Вердикти пакета BAUD ---------------------------------------------------

enum BaudVerdict : uint8_t {
  // Перемкнеться. Міст може готуватись.
  BAUD_ACCEPTED = 0,
  // Швидкості немає в переліку — не сталося нічого.
  BAUD_UNSUPPORTED = 1,
  // Уже триває перемикання — не сталося нічого.
  BAUD_BUSY = 2,
  // Транспорт не має поняття швидкості (USB CDC, TCP симулятора).
  BAUD_NOT_APPLICABLE = 3,
  // Незапрошений звіт: пульт повернувся сам.
  BAUD_REVERTED = 4,
};

// --- Розкладка пакетів ------------------------------------------------------

// `0x88 BAUD_SET`, клієнт -> пульт:
//   0..3  бажана швидкість, бод, LE
//   4     nonce, 1..255
constexpr size_t BAUD_SET_SIZE = 5;

// `0x06 BAUD`, пульт -> клієнт:
//   0     вердикт
//   1     nonce з команди; 0 — незапрошений звіт
//   2..5  швидкість, на яку перемкнеться (при відмові — поточна), LE
//   6..9  поточна швидкість, LE; 0 — поняття не застосовне
//   10,11 switch_delay_ms, LE
//   12,13 revert_window_ms, LE
//   14,15 лічильник відкотів, LE, насичується на 65535
constexpr size_t BAUD_REPORT_SIZE = 16;

// --- Розбір і складання -----------------------------------------------------

// Чи є швидкість у переліку.
bool baudAllowed(uint32_t baud);

// Розбирає PAYLOAD пакета `BAUD_SET`.
//
// Правило довжини те саме, що в `INPUT_STATE` (docs/03-protocol.md): коротший
// вантаж відкидається цілком, довший — обрізається до відомої частини. Так
// майбутнє розширення пакета не ламає цю прошивку.
//
// Повертає false, якщо пакет відкинуто; тоді baud і nonce не чіпаються.
bool parseBaudSet(const uint8_t* payload, size_t length, uint32_t& baud,
                  uint8_t& nonce);

// Складає PAYLOAD пакета `BAUD`. Повертає довжину або 0, якщо не влізло.
size_t buildBaudReport(uint8_t* out, size_t outSize, uint8_t verdict,
                       uint8_t nonce, uint32_t target, uint32_t current,
                       uint16_t switchDelayMs, uint16_t revertWindowMs,
                       uint16_t revertCount);

}  // namespace remote_ui
